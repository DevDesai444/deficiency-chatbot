---
phase: 06-on-prem-verifier-model-weak-model-reliability
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ADR-nemotron-verifier-model.md
autonomous: false
requirements:
  - MODEL-01
  - MODEL-02

user_setup:
  - service: databricks-cli
    why: "GPU hardware enumeration for D-19/D-20 quant/GPU gate — live workspace CLI query"
    env_vars:
      - name: DATABRICKS_HOST
        source: "Already set in .env (https://aip-amn-dev.cloud.databricks.com)"
      - name: DATABRICKS_TOKEN
        source: "Already set in .env (dev.desai@amneal.com token)"
    dashboard_config:
      - task: "Run 'databricks clusters list-node-types --profile amneal-dev' to enumerate GPU hardware class on GPU_XLARGE_8"
        location: "Terminal with databricks CLI v1.6.0 (already authenticated)"

must_haves:
  truths:
    - "GPU hardware class on GPU_XLARGE_8 is confirmed as a named GPU model (H100 or Blackwell or A100)"
    - "A quantization is selected (FP8 for Hopper/H100, NVFP4 only if Blackwell confirmed, BF16 as fallback)"
    - "ADR-nemotron-verifier-model.md contains a 'D-19/D-20 Amendment' section with Confirmed GPU, Quant, and tensor-parallel-size as literals"
    - "Serving build is explicitly NOT started until this gate is recorded and committed"
  artifacts:
    - path: ".planning/ADR-nemotron-verifier-model.md"
      provides: "D-19/D-20 Amendment with confirmed GPU + quant + tensor-parallel-size"
      contains: "D-19/D-20 Amendment"
  key_links:
    - from: ".planning/ADR-nemotron-verifier-model.md"
      to: "Plan 05 vLLM launch command"
      via: "confirmed quant (FP8 vs NVFP4 vs BF16) and tensor-parallel-size feed into --dtype and --tensor-parallel-size flags"
      pattern: "Confirmed GPU|Quant:|tensor-parallel-size"
---

<objective>
D-19/D-20 HARD GATE — the quant/GPU confirmation that must complete before any
vLLM serving build starts. A single CLI query enumerates the exact GPU hardware
class on GPU_XLARGE_8; the result determines whether FP8 (Hopper/H100), NVFP4
(Blackwell ONLY), or BF16 is the correct quantization for the Nemotron-Super-49B
deployment. The decision is recorded as a permanent amendment to
ADR-nemotron-verifier-model.md.

Purpose: Prevent the "loses a week" mid-build hardware surprise (D-20). If the
hardware is Hopper (H100), which is the working assumption from Databricks
documentation (GPU_XLARGE = 1×H100; _8 suffix = 8 units), FP8 is selected. If
Blackwell (B200/GB200) is confirmed, NVFP4 becomes available (but FP8 is still
safe). This gate must complete and be committed BEFORE Plan 05 begins.

Output: ADR-nemotron-verifier-model.md amended with confirmed GPU + quant +
tensor-parallel-size; checkpoint approval gates progression to Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ADR-nemotron-verifier-model.md
@.planning/phases/06-on-prem-verifier-model-weak-model-reliability/06-CONTEXT.md
@.planning/phases/06-on-prem-verifier-model-weak-model-reliability/06-RESEARCH.md

<interfaces>
<!-- D-19/D-20 Decision Procedure from RESEARCH.md -->
GPU hardware → quantization mapping:

| Confirmed GPU       | VRAM total | Recommended Quant | tensor-parallel | Notes                          |
|---------------------|------------|-------------------|----------------|--------------------------------|
| 8×H100-80GB (Hopper)| 640GB      | FP8               | 8              | 49B FP8 ≈ 49GB; fits easily    |
| 8×A100-80GB (Ampere)| 640GB      | BF16 or FP8       | 8              | FP8 supported; BF16 also fits  |
| 1×B200-80GB (Blackwell)| 80GB    | NVFP4             | 1              | ONLY Blackwell; NVFP4 ≈ 25GB   |
| 2×H100-80GB         | 160GB      | FP8               | 2              | Per NVIDIA cookbook for -49B   |

Working assumption: GPU_XLARGE_8 = 8×H100-80GB (Hopper) → FP8, tensor-parallel=8.
This must be confirmed by live CLI query before serving build.

CLI command:
  databricks clusters list-node-types --profile amneal-dev 2>/dev/null | \
    python3 -m json.tool | grep -A5 -B5 "H100\|A100\|Blackwell\|B200"
OR:
  databricks api get "/api/2.0/serving-endpoints/defpredict-suggestor" --profile amneal-dev

vLLM launch flags (FP8 / Hopper path — confirmed if H100):
  --dtype fp8 --quantization fp8 --tensor-parallel-size 8

vLLM launch flags (BF16 fallback — if FP8 unavailable):
  --dtype bfloat16 --tensor-parallel-size 8 (uses all 640GB; tight but fits)

NVFP4 (DO NOT USE unless Blackwell confirmed):
  --tensor-parallel-size 1 (different flags — Blackwell-specific)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate GPU hardware class on GPU_XLARGE_8</name>
  <read_first>
    - .planning/ADR-nemotron-verifier-model.md (current content; will be amended)
    - .planning/phases/06-on-prem-verifier-model-weak-model-reliability/06-RESEARCH.md section "Quant/GPU Decision Procedure (D-19/D-20)" (exact CLI commands + mapping table)
  </read_first>
  <files>.planning/ADR-nemotron-verifier-model.md</files>
  <action>
Run the D-19 Day-0 research CLI query to confirm the GPU hardware class:

```bash
# Primary query — enumerate node types and grep for GPU identifiers:
databricks clusters list-node-types --profile amneal-dev 2>/dev/null \
  | python3 -m json.tool \
  | grep -A5 -B5 "H100\|A100\|Blackwell\|B200\|GPU_XLARGE"

# Secondary query — inspect the proven endpoint's config for hardware hints:
databricks api get "/api/2.0/serving-endpoints/defpredict-suggestor" --profile amneal-dev

# Tertiary — check serving endpoint config for workload_type details:
databricks serving-endpoints get defpredict-suggestor --profile amneal-dev 2>/dev/null \
  || databricks api get "/api/2.0/serving-endpoints/defpredict-suggestor" --profile amneal-dev
```

From the CLI output, determine:
1. What GPU model is associated with workload_type GPU_XLARGE_8?
   - Look for keywords: H100, A100, B200, Blackwell, Hopper, Ampere
   - If ambiguous, treat as H100 (Hopper) — the safe assumption from Databricks docs

2. Apply the mapping table:
   - H100 (Hopper) → **FP8**, tensor-parallel-size=8
   - A100 (Ampere) → **FP8 or BF16**, tensor-parallel-size=8 (prefer FP8)
   - B200/Blackwell → **NVFP4**, tensor-parallel-size=1
   - Unknown/ambiguous → **FP8** (safe default on any Hopper/Ampere; not harmful on Blackwell)

3. Record the decision by appending a `## D-19/D-20 Amendment` section to
   `.planning/ADR-nemotron-verifier-model.md` with the exact format:

```
## D-19/D-20 Amendment (2026-08-XX)

**Status:** Gate passed — serving build may proceed (Plan 05)

**Confirmed GPU class:** [GPU_XLARGE_8 = N×GPUNAME, e.g. "8×H100-80GB (Hopper)"]
**Evidence:** [CLI output excerpt or reasoning from docs, e.g. "databricks clusters list-node-types: node_type_id=Standard_NC96ads_H100_v5 — 8×H100 80GB"]
**Quant selected:** [FP8 / NVFP4 / BF16]
**Rationale:** [1-2 sentences, e.g. "H100 has native FP8 tensor cores; FP8 halves VRAM vs BF16 (49GB vs ~100GB) with no accuracy loss on inference workloads; NVFP4 excluded (Blackwell not confirmed)"]
**tensor-parallel-size:** [N]
**vLLM --dtype flag:** [fp8 / bfloat16]

vLLM launch command amendment:
  python3 -m vllm.entrypoints.openai.api_server \
    --model /Volumes/defpredict/main/artifacts/nemotron-49b \
    --served-model-name nemotron-super-49b-v1_5 \
    --trust-remote-code \
    --tensor-parallel-size [N] \
    --max-model-len 65536 \
    --gpu-memory-utilization 0.95 \
    --enable-auto-tool-choice \
    --tool-parser-plugin "/Volumes/defpredict/main/artifacts/nemotron-49b/llama_nemotron_toolcall_parser_no_streaming.py" \
    --tool-call-parser llama_nemotron_json \
    --host 0.0.0.0 \
    --port 8080 \
    [--dtype fp8 --quantization fp8 if FP8]

**GATE STATUS:** PASSED — Plan 05 (serving build) may begin.
```

If the CLI queries fail or return no GPU-identifying information, apply the safe
default: H100/FP8/tensor-parallel=8. Document "Unable to enumerate specific GPU
model — defaulting to H100 assumption per Databricks docs (GPU_XLARGE=1×H100-80GB;
_8 suffix=8 units). FP8 selected as safe default (works on Hopper, Ampere, and
Blackwell)."
  </action>
  <verify>
    <automated>grep -c "D-19/D-20 Amendment" /Users/DEVDESAI1/dev/deficiency-chatbot/.planning/ADR-nemotron-verifier-model.md && grep -c "GATE STATUS.*PASSED" /Users/DEVDESAI1/dev/deficiency-chatbot/.planning/ADR-nemotron-verifier-model.md && grep -E "Quant selected: (FP8|NVFP4|BF16|fp8|bfloat16)" /Users/DEVDESAI1/dev/deficiency-chatbot/.planning/ADR-nemotron-verifier-model.md | grep -c "." && grep -E "tensor-parallel-size: [0-9]+" /Users/DEVDESAI1/dev/deficiency-chatbot/.planning/ADR-nemotron-verifier-model.md | grep -c "."</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-19/D-20 Amendment" .planning/ADR-nemotron-verifier-model.md` returns 1
    - `grep -c "GATE STATUS.*PASSED" .planning/ADR-nemotron-verifier-model.md` returns 1
    - `grep -E "Quant selected: (FP8|NVFP4|BF16|fp8|bfloat16)" .planning/ADR-nemotron-verifier-model.md` returns a non-empty match (not a placeholder like "[FP8 / NVFP4 / BF16]")
    - `grep -E "tensor-parallel-size: [0-9]+" .planning/ADR-nemotron-verifier-model.md` returns a non-empty match (an integer, not "[N]")
    - The amendment contains a specific quant value (FP8, NVFP4, or BF16) — not a placeholder
  </acceptance_criteria>
  <done>ADR-nemotron-verifier-model.md contains a D-19/D-20 Amendment section with confirmed GPU hardware class, selected quantization (non-placeholder), tensor-parallel-size (non-placeholder integer), and "GATE STATUS: PASSED".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CLI query ran against the live Databricks workspace; GPU hardware class enumerated; quantization decision (FP8 / BF16 / NVFP4) recorded with rationale in ADR-nemotron-verifier-model.md D-19/D-20 Amendment section.</what-built>
  <how-to-verify>
1. Open `.planning/ADR-nemotron-verifier-model.md` and scroll to the `## D-19/D-20 Amendment` section.
2. Confirm it contains:
   - A specific GPU class (e.g. "8×H100-80GB" — NOT a placeholder like "[GPU_XLARGE_8 = N×GPUNAME]")
   - A quant decision (FP8, BF16, or NVFP4) — NOT the literal string "[FP8 / NVFP4 / BF16]"
   - A tensor-parallel-size (an integer, NOT "[N]")
   - The vLLM launch command with the correct --dtype flag
   - "GATE STATUS: PASSED"
3. Verify the rationale is consistent with the mapping table (H100 → FP8; Blackwell → NVFP4; ambiguous → FP8).
4. Confirm no entry says NVFP4 unless the CLI output explicitly confirmed Blackwell/B200 hardware.
5. If the quant decision looks wrong (e.g. NVFP4 selected without Blackwell confirmation), reject and rerun Task 1 with the correction.
  </how-to-verify>
  <resume-signal>Type "gate-passed" to proceed to Plan 05 (serving build), or describe the issue for Task 1 to be corrected.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI output → quant decision | The CLI output for GPU hardware determines the vLLM flags; wrong hardware → wrong quant → serving build fails |
| ADR amendment → Plan 05 inputs | The confirmed quant and tensor-parallel-size from this ADR amendment are the literal inputs to the vLLM launch command in Plan 05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | vLLM quant selection — NVFP4 assumed without hardware confirmation | mitigate | D-20 gate is the hard blocker; the checkpoint:human-verify task prevents Plan 05 from proceeding until the quant is confirmed; default to FP8 (safe on all hardware) if ambiguous |
| T-06-02-02 | Information Disclosure | Databricks CLI token in terminal output | accept | CLI queries use `--profile amneal-dev` (stored credential); token is not echoed; workspace is on-prem (aip-amn-dev.cloud.databricks.com) |
| T-06-02-03 | Tampering | Gate bypassed (Plan 05 starts before this plan passes) | mitigate | Plan 05 frontmatter lists `depends_on: ["06-02"]`; the execute-phase orchestrator enforces depends_on ordering |
</threat_model>

<verification>
Gate complete when:
1. `grep -c "D-19/D-20 Amendment" .planning/ADR-nemotron-verifier-model.md` returns 1
2. `grep -c "GATE STATUS.*PASSED" .planning/ADR-nemotron-verifier-model.md` returns 1
3. `grep -E "Quant selected: (FP8|NVFP4|BF16|fp8|bfloat16)" .planning/ADR-nemotron-verifier-model.md` matches (non-placeholder)
4. `grep -E "tensor-parallel-size: [0-9]+" .planning/ADR-nemotron-verifier-model.md` matches (integer present)
5. Human reviewer has approved via checkpoint:human-verify
</verification>

<success_criteria>
- ADR-nemotron-verifier-model.md contains a D-19/D-20 Amendment with confirmed GPU + quant (non-placeholder) + tensor-parallel-size (integer)
- Checkpoint approved — human reviewer verified the amendment is substantive (not a placeholder)
- Serving build (Plan 05) is explicitly unblocked by this gate passing
</success_criteria>

<output>
After completion, create `.planning/phases/06-on-prem-verifier-model-weak-model-reliability/06-02-SUMMARY.md`
</output>
