---
phase: 07-multi-agent-verification-interpretive-tail
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/verify/__init__.py
  - tests/verify/conftest.py
  - tests/verify/fixtures/labeled_fp_candidates.json
  - tests/verify/test_invariant_no_drop.py
  - tests/verify/test_verifier_readonly.py
  - tests/verify/test_orchestrator_consolidate.py
  - tests/verify/test_coverage_report.py
  - tests/verify/test_decorrelation.py
  - tests/verify/test_panel_family.py
  - tests/verify/test_consensus.py
  - tests/verify/test_grounding_reresolve.py
  - tests/verify/test_tail_gate.py
  - src/evals/baseline/phase5_f1_baseline.json
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03, VERIFY-04]
must_haves:
  truths:
    - "A tests/verify/ directory exists with a ScriptedFleetClient double so consensus/decorrelation/invariant tests run offline and deterministic."
    - "A labeled false-positive fixture exists with enough known-FP candidates that end-to-end precision is measurable (P6 only had 2/115)."
    - "A committed Phase-5 F1/precision baseline reference exists for the 'does not lower F1' comparison."
    - "Every VERIFY-01..04 behavior has a failing (or xfail-marked) test stub that later plans turn green."
  artifacts:
    - path: "tests/verify/conftest.py"
      provides: "ScriptedFleetClient + shared candidate/normalized-text fixtures"
      contains: "class ScriptedFleetClient"
    - path: "tests/verify/fixtures/labeled_fp_candidates.json"
      provides: "labeled FP + TP candidates for precision measurement"
    - path: "src/evals/baseline/phase5_f1_baseline.json"
      provides: "Phase-5 precision/recall/F1 reference the phase gate compares against"
  key_links:
    - from: "tests/verify/conftest.py"
      to: "src/llm/client.ChatTurn"
      via: "ScriptedFleetClient returns ChatTurn per (model) key mirroring chat_completion_tools"
      pattern: "ChatTurn"
    - from: "src/evals/baseline/phase5_f1_baseline.json"
      to: "src/evals/metrics._end_to_end"
      via: "captured Phase-5 precision/recall/F1 numbers the gate reads"
      pattern: "precision|recall|f1"
---

<objective>
Wave 0: close the three research-flagged validation gaps BEFORE any src/verify/ code exists, so
later plans have deterministic offline test doubles, a measurable precision signal, and a frozen
Phase-5 comparison baseline. This plan builds the scaffolding and the RED test stubs that Plans
02–04 turn GREEN. No production code in src/verify/ is written here.

Purpose: Phase 6's precision metric was mis-calibrated (only 2 of 115 candidates were known-false),
and the consensus/decorrelation/invariant logic must be testable without hitting the live fleet.
This plan makes precision measurable and the invariants offline-testable.

Output: tests/verify/ package (conftest + ScriptedFleetClient + 9 test stubs), a labeled-FP
fixture, and a committed Phase-5 F1/precision baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-multi-agent-verification-interpretive-tail/07-RESEARCH.md
@.planning/phases/07-multi-agent-verification-interpretive-tail/07-CONTEXT.md

<interfaces>
<!-- Extracted from the codebase this session. Executor uses these directly — no exploration needed. -->

From src/llm/client.py — the object the fleet double must mimic:
```python
@dataclass
class ChatTurn:
    content: str
    finish_reason: str       # "stop" | "tool_parse_error" | "error" | "length" ...
    tool_calls: list         # OpenAI tool_call objects; [] when no call
    raw_message: dict
    usage_present: bool

def chat_completion_tools(messages, tools, model=..., temperature=..., guided_model_cls=...) -> ChatTurn
```

From src/schemas/llm.py — the verdict model (EXTEND-only, never redefine):
```python
class VerdictChoice(str, Enum):
    KEEP = "KEEP"; DOWNGRADE = "DOWNGRADE"   # case-sensitive; off-value → ParseFailed
class VERDICT(BaseModel):
    verdict: VerdictChoice                    # NEVER coerced
    grounding_span: str = Field(min_length=1) # non-blank verbatim span
class ParseFailed(BaseModel):
    layer: str; reason: str; raw_output: str; validation_error: str = ""
```

From src/schemas/faults.py — the candidate objects verified + consolidated:
```python
class Fault(BaseModel):
    confidence: float                          # 0..1  (a DOWNGRADE lowers this)
    confidence_tier: Literal["full","low"] | None
    submission_span_id: SpanID | None          # deterministic non-absence legs
    absence_anchor: CoverageAbsenceAnchor | None   # absence-family (no submission_span_id)
    leg_tag: Literal["ABSENCE","STRUCTURAL","REFERENCE","PRECEDENT"] | None
    dedup_key: str | None                       # "{doc_id}:{section_id}:{rule_id_or_null}"
    source: str                                 # producer tag, e.g. "reviewer:..." / "oracle:..."
class CoverageAbsenceAnchor(BaseModel):
    claim_span_id: SpanID | None                # narrative-claim CORPUS span, else None
    sub_threshold_hits: list[RetrievalHit]      # top-k below-threshold hits
    manifest_span_ids: list[SpanID]
```

From src/evals/metrics.py — the precision/recall the baseline must capture:
```python
def _precision_recall(tp, fp, fn) -> {"precision","recall","tp","fp","fn"}
def _end_to_end(report, eval_set, doc_id) -> {precision,recall,tp,fp,fn}
def _end_to_end_by_family(report, eval_set, doc_id) -> {family: {...}}
```

Existing double pattern to mirror — tests/agents/review/conftest.py:
```python
class ScriptedChatClient:   # returns ChatTurn from a script list; records seen_messages/seen_tools
    def __call__(self, messages, tools, **kw) -> ChatTurn: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/verify package + ScriptedFleetClient double + shared fixtures</name>
  <read_first>
    - tests/agents/review/conftest.py (the ScriptedChatClient / ForcedRunaway pattern to mirror, lines 28-86)
    - src/llm/client.py (ChatTurn dataclass fields, lines 56-80)
    - src/schemas/faults.py (Fault, CoverageAbsenceAnchor, StructuralAnchor fields, lines 40-210)
    - src/schemas/llm.py (VERDICT, VerdictChoice, ParseFailed, lines 8-57)
  </read_first>
  <action>
    Create tests/verify/__init__.py (empty) and tests/verify/conftest.py.

    In conftest.py define:
    1. `class ScriptedFleetClient` — a deterministic stand-in for chat_completion_tools keyed by
       MODEL. Constructor takes `script: dict[str, list[ChatTurn]]` mapping a model endpoint id to
       the sequence of ChatTurns it returns. `__call__(self, messages, tools, *, model, **kw)`
       appends `copy.deepcopy(messages)` to `self.seen_messages_by_model[model]` and returns the
       next scripted ChatTurn for that model (clamped to the last, exactly as ScriptedChatClient
       does). Expose `self.seen_messages_by_model: dict[str, list[list[dict]]]` so a decorrelation
       test can assert what each verifier was shown. Mirror the deepcopy discipline of
       ScriptedChatClient so message assertions survive later mutation.
    2. Helper builders (pytest fixtures or module-level functions):
       - `make_verdict_turn(verdict: str, grounding_span: str) -> ChatTurn` — builds a ChatTurn
         with one tool_call whose function.arguments is `json.dumps({"verdict": verdict,
         "grounding_span": grounding_span})`, finish_reason="stop". Reuse the tool_call shape from
         tests/agents/review/conftest.make_tool_call if importable; otherwise construct a minimal
         object with `.function.name` / `.function.arguments`.
       - `make_parse_fail_turn() -> ChatTurn` — finish_reason="tool_parse_error", tool_calls=[].
       - `deterministic_candidate()` — a Fault fixture with leg_tag="STRUCTURAL",
         submission_span_id set, dedup_key="docA:sec1:null", confidence=0.6, source="oracle:x".
       - `absence_candidate()` — a Fault with leg_tag="ABSENCE", submission_span_id=None,
         absence_anchor=CoverageAbsenceAnchor(claim_span_id=<a SpanID>, ...), dedup_key set.
       - `tail_candidate()` — a Fault with source starting "reviewer:" / a producer-family marker
         "qwen" (the field later plans will read to exclude the Qwen panel).
    3. A small in-memory NormalizedText + SpanID pair (or reuse an existing test helper) so
       grounding-re-resolution tests can call open_span against real byte-exact substrate. If an
       existing fixture builder exists (grep tests/ for `mint_span`/`NormalizedText`), import and
       reuse it rather than hand-rolling.
    Do NOT import from src/verify/ (it does not exist yet). conftest must import cleanly on its own.
  </action>
  <acceptance_criteria>
    - `grep -q "class ScriptedFleetClient" tests/verify/conftest.py`
    - `grep -q "seen_messages_by_model" tests/verify/conftest.py`
    - `grep -q "def make_verdict_turn" tests/verify/conftest.py`
    - `grep -q "copy.deepcopy" tests/verify/conftest.py`
    - `python3 -c "import tests.verify.conftest" 2>&1 | grep -qiv "cannot import name.*verify\.\|No module named 'verify'"` (conftest imports without importing src/verify)
    - `python3 -m pytest tests/verify/ --collect-only -q` exits 0 (collection succeeds even with zero passing tests)
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/verify/ --collect-only -q</automated>
  </verify>
  <done>tests/verify/ collects; ScriptedFleetClient + verdict/candidate builders exist and import without src/verify/.</done>
</task>

<task type="auto">
  <name>Task 2: Labeled false-positive fixture + committed Phase-5 F1/precision baseline</name>
  <read_first>
    - src/evals/baseline/beta_recall_baseline.json (the existing recall/matched-set baseline — DO NOT overwrite)
    - src/evals/metrics.py (_precision_recall, _end_to_end, _end_to_end_by_family — lines 71-135)
    - src/evals/run.py (cmd_beta_recall_gate + cmd_score — how a captured report is scored, lines 140-160, 989-1072)
    - src/schemas/faults.py (Fault fields, for the fixture candidate shape)
  </read_first>
  <action>
    Two artifacts:

    (A) tests/verify/fixtures/labeled_fp_candidates.json — a JSON list of candidate records, each
    `{"fault": <Fault-serializable dict>, "label": "TP" | "FP"}`. Include at least 6 FP-labeled and
    6 TP-labeled candidates spanning the deterministic legs (ABSENCE, STRUCTURAL, REFERENCE) so
    precision is measurable and not dominated by one family. The FP set must be PLANTED-general (not
    tied to a specific corpus item) — e.g. a structural "aggregate mismatch" whose recompute
    actually agrees (a true non-fault the verifier should DOWNGRADE), and an absence candidate for a
    requirement the fixture doc demonstrably addresses. Each candidate carries a real dedup_key,
    leg_tag, and a re-openable submission_span_id / absence_anchor so grounding re-resolution can run
    on it. Add a top-of-file comment block naming why each FP is a genuine false positive (so the
    RECALL-05 generality guard cannot flag a corpus-specific constant — describe the STRUCTURE, not
    the corpus value).

    (B) src/evals/baseline/phase5_f1_baseline.json — capture the Phase-5 deterministic output's
    end-to-end precision/recall/F1 as the frozen comparison the phase gate reads. Populate it by
    running the existing scorer on the frozen Phase-5 golden report if the corpus is present
    (`python3 -m evals.run score --report <frozen phase5 report>`), else author it from the numbers
    already recorded at Phase-5 close (STATE/ROADMAP: aggregate recall 0.1875 (6/32); precision from
    the ~102-fp over-emit). Shape:
      `{"source": "phase5-frozen", "end_to_end": {"precision": <f>, "recall": <f>, "f1": <f>,
        "tp": <int>, "fp": <int>, "fn": <int>}, "by_family": {...}, "note": "Phase-7 gate floor:
        F1 must be >= this; matched TP set must not shrink."}`
    Compute f1 = 2*p*r/(p+r) with a zero-guard. DO NOT overwrite beta_recall_baseline.json — this is
    a NEW sibling file. If the corpus is absent in the execution environment, author from the
    committed Phase-5 numbers and add `"provenance": "authored-from-phase5-close-numbers"` so it is
    honest; a MAIN-tree re-capture happens in Plan 04's gate task.
  </action>
  <acceptance_criteria>
    - `test -f tests/verify/fixtures/labeled_fp_candidates.json`
    - `python3 -c "import json; d=json.load(open('tests/verify/fixtures/labeled_fp_candidates.json')); fp=[c for c in d if c['label']=='FP']; tp=[c for c in d if c['label']=='TP']; assert len(fp)>=6 and len(tp)>=6, (len(fp),len(tp))"`
    - `python3 -c "import json; d=json.load(open('tests/verify/fixtures/labeled_fp_candidates.json')); legs={c['fault'].get('leg_tag') for c in d}; assert {'ABSENCE','STRUCTURAL','REFERENCE'} <= legs, legs"`
    - `test -f src/evals/baseline/phase5_f1_baseline.json`
    - `python3 -c "import json; d=json.load(open('src/evals/baseline/phase5_f1_baseline.json')); e=d['end_to_end']; assert set(['precision','recall','f1','tp','fp','fn'])<=set(e), e"`
    - `test -f src/evals/baseline/beta_recall_baseline.json && git diff --quiet src/evals/baseline/beta_recall_baseline.json` (existing recall baseline untouched)
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('tests/verify/fixtures/labeled_fp_candidates.json')); assert len([c for c in d if c['label']=='FP'])>=6; e=json.load(open('src/evals/baseline/phase5_f1_baseline.json'))['end_to_end']; assert 'f1' in e"</automated>
  </verify>
  <done>Labeled-FP fixture has >=6 FP + >=6 TP across >=3 legs; phase5_f1_baseline.json exists with precision/recall/f1; existing recall baseline unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: RED test stubs for every VERIFY-01..04 behavior + grounding + tail</name>
  <read_first>
    - .planning/phases/07-multi-agent-verification-interpretive-tail/07-RESEARCH.md (Validation Architecture → Test Map, lines 420-433; Pitfalls 1-6)
    - tests/verify/conftest.py (the fixtures created in Task 1 — reuse them)
    - src/schemas/faults.py + src/schemas/llm.py (assertion targets)
  </read_first>
  <action>
    Create the 9 test files listed in files_modified (minus conftest/fixtures). Each imports the
    to-be-built src/verify/ module under an `pytest.importorskip("verify.<mod>")` OR marks its body
    `@pytest.mark.xfail(reason="src/verify not built until Wave 1/2/3", strict=False)` so the suite
    is GREEN now (collection + xfail) and each stub flips to a real assertion as its module lands.
    Prefer `importorskip` where the module name is known; use xfail only for behaviors whose module
    is built later. Write the ACTUAL assertion body (not `pass`), so turning it on in a later plan
    is a one-line un-skip. Map exactly:
    - test_invariant_no_drop.py (VERIFY-01): given a panel that all DOWNGRADE, orchestrator output
      list length == input length; the DOWNGRADEd Fault is the SAME object mutated (confidence
      lowered), never removed. Also a static assertion: `grep`-style check that no `.pop(`/`del `/
      `.remove(` on a fault list appears in src/verify/ (import the module source and assert). 
    - test_verifier_readonly.py (VERIFY-01): the tool list handed to a verifier call contains no
      emit/write tool (only get_section/read_guideline). Assert on the tools passed to
      ScriptedFleetClient.
    - test_orchestrator_consolidate.py (VERIFY-02): two Faults with the same dedup_key are grouped
      (merged, count preserved), distinct keys stay separate.
    - test_coverage_report.py (VERIFY-02): CoverageReport has no attribute/key named "compliant";
      has reviewed_keep / reviewed_downgrade / could_not_locate.
    - test_decorrelation.py (VERIFY-03): the user message a verifier receives contains ONLY the
      claim + re-opened source + rule, and NOT the producer's reasoning/CoT (assert a known
      producer-CoT sentinel string is absent from seen_messages_by_model).
    - test_panel_family.py (VERIFY-03): for a tail_candidate (producer family qwen),
      panel_for("qwen") contains no model whose MODEL_LINEAGE is "qwen".
    - test_consensus.py (VERIFY-01/03): lone DOWNGRADE → KEEP; split → KEEP; affirmative grounded
      majority → DOWNGRADE; an ungrounded DOWNGRADE does not count toward the majority.
    - test_grounding_reresolve.py (grounding): a candidate whose submission_span_id re-opens
      byte-exact + grounding_span substring present → grounded True; a HashMismatch or absent
      substring → grounded False (candidate still not dropped). Include an ABSENCE-anchor case that
      re-resolves against claim_span_id (Assumption A1 branch).
    - test_tail_gate.py (VERIFY-04): a tail candidate is emitted only through the grounding gate and
      then passed through the SAME consensus verifier; a tail candidate the panel does not
      affirmatively keep-support is not surfaced as grounded (confidence lowered, not dropped).
    Every test file must reference its req id in a top comment (e.g. `# VERIFY-03`).
  </action>
  <acceptance_criteria>
    - `ls tests/verify/test_*.py | wc -l | grep -q 9`
    - `grep -rl "VERIFY-01" tests/verify/ | grep -q test_invariant_no_drop`
    - `grep -rl "VERIFY-03" tests/verify/ | grep -q test_decorrelation`
    - `grep -rl "VERIFY-04" tests/verify/ | grep -q test_tail_gate`
    - `grep -c "importorskip\|xfail" tests/verify/test_invariant_no_drop.py tests/verify/test_consensus.py tests/verify/test_decorrelation.py | grep -qv ':0'` (stubs guarded, not hard-failing)
    - Each test file contains a real `assert` (not only `pass`): `grep -L "assert" tests/verify/test_*.py` prints nothing
    - `python3 -m pytest tests/verify/ -q` exits 0 (all xfail/skip, zero errors)
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/verify/ -q</automated>
  </verify>
  <done>9 guarded RED stubs exist, each with a real assertion body and its VERIFY req id, and `pytest tests/verify/ -q` is green (xfail/skip, no collection errors).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture JSON → test loader | authored fixture data crosses into pydantic Fault validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | labeled_fp_candidates.json corpus-specific tuning | mitigate | FP entries described by STRUCTURE not corpus value; RECALL-05 generality guard runs in Plan 04 |
| T-07-02 | Information disclosure | test doubles must not call live fleet | mitigate | ScriptedFleetClient is pure in-memory; no client import; on-prem guard untouched |
</threat_model>

<verification>
- `python3 -m pytest tests/verify/ -q` green (xfail/skip stubs, no errors).
- Fixture has >=6 FP + >=6 TP across ABSENCE/STRUCTURAL/REFERENCE.
- phase5_f1_baseline.json committed; beta_recall_baseline.json byte-unchanged.
</verification>

<success_criteria>
tests/verify/ scaffolding + ScriptedFleetClient + 9 guarded RED stubs + labeled-FP fixture +
Phase-5 F1 baseline all exist and the suite collects/passes offline. No src/verify/ production code
written. Every VERIFY-01..04 behavior has a named test waiting to be turned on.
</success_criteria>

<output>
After completion, create `.planning/phases/07-multi-agent-verification-interpretive-tail/07-01-SUMMARY.md`
</output>
