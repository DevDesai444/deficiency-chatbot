---
phase: 07-multi-agent-verification-interpretive-tail
plan: 03
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - src/verify/coverage.py
  - src/verify/orchestrator.py
  - src/verify/assemble.py
  - tests/verify/test_orchestrator_consolidate.py
  - tests/verify/test_coverage_report.py
  - tests/verify/test_invariant_no_drop.py
  - tests/verify/test_scored_report_filter.py
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-03]
must_haves:
  truths:
    - "The orchestrator fans out verifiers keyed on Fault.dedup_key, one isolated call per decorrelated panel member."
    - "Consolidation merges Faults sharing a dedup_key and never drops one; output length >= number of unique candidates."
    - "A DOWNGRADE lowers a Fault's confidence/confidence_tier only — no verifier path deletes a candidate (proven by test)."
    - "For each candidate the orchestrator re-opens FULL source (get_section) + rule (read_guideline), not a truncated excerpt, before verifying."
    - "The coverage report has no 'compliant' field; it states reviewed-KEEP, reviewed-DOWNGRADE (with agreeing verifiers), and could-not-locate."
    - "A report-assembly helper surfaces ONLY KEEP-tier (active) faults in the scored FaultReport.faults and moves DOWNGRADEd faults into the CoverageReport — so the frozen F1 path (which scores report.faults as-is) actually SEES the verifier's precision effect (Gap 1). Proven both ways: a DOWNGRADEd FP is NOT scored as an FP; a DOWNGRADEd TP is STILL retained/matched (zero-TP-loss)."
  artifacts:
    - path: "src/verify/orchestrator.py"
      provides: "verify_candidates(faults, corpus, manifest, ...) -> (verified_faults, CoverageReport)"
      contains: "def verify_candidates"
    - path: "src/verify/coverage.py"
      provides: "CoverageReport model (reviewed_keep / reviewed_downgrade / could_not_locate)"
      contains: "class CoverageReport"
    - path: "src/verify/assemble.py"
      provides: "assemble_scored_report(verified_faults, coverage) -> FaultReport — KEEP-tier only in .faults; DOWNGRADEd retained in CoverageReport (Gap 1 report-assembly filter)"
      contains: "def assemble_scored_report"
  key_links:
    - from: "src/verify/orchestrator.py"
      to: "Fault.dedup_key"
      via: "consolidate/fan-out key"
      pattern: "dedup_key"
    - from: "src/verify/orchestrator.py"
      to: "src/verify/consensus.consensus"
      via: "apply affirmative-grounded-majority; DOWNGRADE lowers confidence, never removes"
      pattern: "consensus"
    - from: "src/verify/orchestrator.py"
      to: "src/tools/get_section.get_section"
      via: "re-open FULL source for each candidate before verifying"
      pattern: "get_section|read_guideline"
    - from: "src/verify/assemble.py"
      to: "src/evals/metrics._end_to_end"
      via: "the FaultReport.faults this builds is what the FROZEN F1 path scores; DOWNGRADEd faults excluded from .faults so a downgrade actually lowers FP count"
      pattern: "confidence_tier|FaultReport"
---

<objective>
Wave 2: assemble the ATOMS from Plan 02 into the orchestrator — consolidate/dedup Phase-5 Faults by
dedup_key, re-open full source+rule per candidate, fan out isolated decorrelated verifier calls,
apply the consensus invariant (DOWNGRADE lowers confidence, never drops), and emit an honest
coverage report. Then add the report-assembly filter (Gap 1) that makes the frozen F1 path actually
see the verifier's precision effect.

Purpose: VERIFY-02 (fan-out on docId:sectionId:ruleId, consolidate/dedup, coverage report) and the
completion of VERIFY-01's downgrade-never-drop invariant at the orchestration layer. This is the
load-bearing recall invariant realized as a code path with a test asserting no branch removes a
candidate. The Gap-1 assembly helper closes the proxy-gate: `_end_to_end` scores `report.faults`
as-is, so a DOWNGRADEd-but-present FP would still count as a full FP and F1 would be identical
before/after verification. The helper surfaces ONLY KEEP-tier faults in the scored report and moves
DOWNGRADEd ones to the CoverageReport (nothing silently dropped).

Output: src/verify/{coverage,orchestrator,assemble}.py + four Wave-0 tests flipped to GREEN + a new
Gap-1 report-assembly filter test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-multi-agent-verification-interpretive-tail/07-RESEARCH.md
@./CLAUDE.md
@.planning/phases/07-multi-agent-verification-interpretive-tail/07-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 (this phase) + existing codebase. -->

From src/verify/ (built in Plan 02):
```python
# panel.py
def panel_for(producer_family: str | None) -> tuple[str, ...]
# verifier.py
def verify_once(candidate, source_text: str, rule_text: str, model: str, completion=...) -> VERDICT | "KEEP"
# consensus.py
def consensus(candidate, verdicts, panel_size, nt, ...) -> tuple["KEEP"|"DOWNGRADE", list[str]]
# grounding.py
def grounding_reresolves(candidate, grounding_span_text, nt) -> bool
```

From src/schemas/faults.py:
```python
class Fault: confidence; confidence_tier: Literal["full","low"]|None;   # DOWNGRADE sets "low" — the active/downgraded discriminator
             submission_span_id; absence_anchor; leg_tag;
             dedup_key;   # "{doc_id}:{section_id}:{rule_id_or_null}"  — the fan-out key
             source       # producer tag; None/deterministic legs → producer_family None
class FaultReport(BaseModel):
    job_id: str; faults: list[Fault]; faults_found: bool; domains_checked: list[str]; ...
```
Consolidation pattern (RESEARCH): group by `f.dedup_key or f"{f.submission_span_id.doc_id}:{f.submission_span_id.start}:null"`, MERGE never drop.

Producer-family mapping (VERIFY-03): deterministic legs (leg_tag in ABSENCE/STRUCTURAL/REFERENCE/
PRECEDENT) → producer_family = None (any panel). A candidate whose `source`/producer marker is a
model family (the interpretive tail, Plan 04) → that family, so the panel excludes it.
`family_of(candidate)` is the accessor other plans read — Plan 04's tail tags a candidate so this
returns "qwen"; define it here.

From src/evals/metrics.py — FROZEN (do NOT edit). This is WHY the Gap-1 assembly filter exists:
```python
def _end_to_end(report, eval_set, doc_id) -> {precision,recall,tp,fp,fn}
# faults = [f.model_dump() for f in report.faults]; score(faults, ...)  — scores EVERY fault in
# report.faults, regardless of confidence_tier/tier. A DOWNGRADEd FP left in report.faults still
# counts as a full FP → no-op verifier passes. The assembly filter must exclude DOWNGRADEd faults
# from report.faults BEFORE the report reaches this path.
```

From src/tools/get_section.py / read_guideline.py — re-open the FULL context (never a ≤500-char excerpt):
```python
def get_section(corpus, doc_id, ledger, start=None, end=None, heading=None, handle=None, max_chars=8000) -> str | ToolRejected
def read_guideline(manifest, ledger, citation=None, ...) -> list[dict] | str | ToolRejected
```
A ToolRejected/None on either half → the candidate is "could_not_locate" in the coverage report
(reviewed but not gradeable) — confidence NOT raised, candidate NOT dropped, and it is NOT filtered
out of the scored report (could_not_locate is not a DOWNGRADE).

CoverageReport shape (RESEARCH — VERIFY-02):
```python
class CoverageReport(BaseModel):
    reviewed_keep: list[str]         # dedup_keys → KEEP
    reviewed_downgrade: list[dict]   # {dedup_key, agreeing_verifiers: [model...]}  — the DOWNGRADEd faults live HERE
    could_not_locate: list[dict]     # {dedup_key, half: "source"|"rule", reason}
    # NO "compliant" field.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: coverage.py CoverageReport model + consolidation helper</name>
  <read_first>
    - src/schemas/faults.py (Fault.dedup_key + ComplianceVerdict — why "compliant" is unrepresentable, lines 40, 188-190)
    - .planning/phases/07-multi-agent-verification-interpretive-tail/07-RESEARCH.md (Coverage report shape + Consolidation by dedup key, lines 331-351)
    - tests/verify/test_coverage_report.py, test_orchestrator_consolidate.py (stubs to turn green)
  </read_first>
  <behavior>
    - CoverageReport has reviewed_keep / reviewed_downgrade / could_not_locate; getattr(report,
      "compliant", MISSING) is MISSING and "compliant" not in report.model_dump().
    - consolidate([f1, f2]) with equal dedup_key → one group of two; distinct keys → two groups;
      a Fault with dedup_key None → falls back to a doc:start:null key (never crashes, never drops).
  </behavior>
  <action>
    Create src/verify/coverage.py:
      - `class CoverageReport(BaseModel)` exactly as the RESEARCH shape (reviewed_keep: list[str];
        reviewed_downgrade: list[dict]; could_not_locate: list[dict]). Add a docstring stating a
        "no deficiencies found" result is expressed ONLY as (reviewed_keep == [] AND
        could_not_locate names exactly what was searched) — there is deliberately no "compliant"
        field (ComplianceVerdict discipline). reviewed_downgrade is ALSO the audit trail for the
        Gap-1 assembly filter: every dedup_key here was DOWNGRADEd and is excluded from the scored
        FaultReport.faults but retained in coverage (nothing silently dropped).
      - `def consolidate(faults: list[Fault]) -> dict[str, list[Fault]]` per RESEARCH: group by
        `f.dedup_key or f"{f.submission_span_id.doc_id}:{f.submission_span_id.start}:null"` (guard the
        None-submission_span_id / absence case with an absence-anchor-derived fallback key). MERGE,
        never drop; assert in a code comment that the summed group lengths == len(faults).
    Turn tests/verify/test_coverage_report.py and the consolidation half of
    test_orchestrator_consolidate.py from xfail/skip to live assertions.
  </action>
  <acceptance_criteria>
    - `grep -q "class CoverageReport" src/verify/coverage.py && grep -q "reviewed_downgrade" src/verify/coverage.py && grep -q "could_not_locate" src/verify/coverage.py`
    - `grep -v '^#' src/verify/coverage.py | grep -c "compliant" | grep -qx 0` (no compliant field)
    - `grep -q "def consolidate" src/verify/coverage.py && grep -q "dedup_key" src/verify/coverage.py`
    - `python3 -c "from verify.coverage import CoverageReport; r=CoverageReport(reviewed_keep=[],reviewed_downgrade=[],could_not_locate=[]); assert 'compliant' not in r.model_dump()"`
    - `python3 -m pytest tests/verify/test_coverage_report.py tests/verify/test_orchestrator_consolidate.py -x -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/verify/test_coverage_report.py tests/verify/test_orchestrator_consolidate.py -x -q</automated>
  </verify>
  <done>CoverageReport (no "compliant" field) + consolidate (merge-never-drop) exist; two Wave-0 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: orchestrator.py — full re-open + decorrelated fan-out + consensus + coverage, with the no-drop invariant test</name>
  <read_first>
    - src/verify/panel.py, verifier.py, consensus.py, grounding.py (Plan 02 atoms — call signatures)
    - src/verify/coverage.py (Task 1 — CoverageReport + consolidate)
    - src/tools/get_section.py (get_section signature + ToolRejected, lines 51-75)
    - src/tools/read_guideline.py (read_guideline citation-fetch mode, lines 33-60)
    - src/tools/ledger.py (RetrievalLedger — needed by the re-open tools)
    - src/schemas/faults.py (confidence / confidence_tier — the fields a DOWNGRADE lowers)
    - .planning/phases/07-multi-agent-verification-interpretive-tail/07-RESEARCH.md (System Architecture Diagram + Pitfall 4 no-drop, lines 117-175, 299-301)
    - tests/verify/test_invariant_no_drop.py (the stub to turn green)
  </read_first>
  <behavior>
    - verify_candidates returns (verified_faults, CoverageReport) where len(verified_faults) ==
      number of unique dedup_key groups; NO input candidate disappears.
    - When every panel member GROUNDED-DOWNGRADEs a candidate, that Fault appears in output with
      LOWERED confidence (and confidence_tier "low") — the same object, not removed.
    - When source or rule cannot be re-opened (ToolRejected/None), the candidate lands in
      coverage.could_not_locate and stays in verified_faults with confidence unchanged (not dropped),
      confidence_tier NOT set to "low" (could_not_locate is not a DOWNGRADE — it must remain ACTIVE
      in the scored report).
    - Producer family for a deterministic leg is None → full-fleet panel; a tail candidate's family
      excludes its own family (wiring point for Plan 04). family_of(candidate) is defined here.
    - No code path in src/verify/orchestrator.py calls .pop/.remove/del on a fault list or returns a
      filtered subset (the recall invariant, proven by a source-scan test).
  </behavior>
  <action>
    Create src/verify/orchestrator.py with `def family_of(candidate) -> str | None` (None for
    deterministic legs; the producer family marker for tail-produced candidates — the accessor Plan
    04 tags for) and
      `def verify_candidates(faults: list[Fault], corpus, manifest, ledger,
                             completion=chat_completion_tools, nt_for=<accessor>) ->
                             tuple[list[Fault], CoverageReport]`:
      1. groups = consolidate(faults). For each group, pick a representative candidate (merge
         confidence/anchors as already done by Phase-5 emit; do NOT re-detect).
      2. producer_family = family_of(candidate). panel = panel_for(producer_family).
      3. Re-open FULL source via get_section (using the candidate's span/section — full text, paginate
         by handle if oversized, NEVER truncate) and FULL rule via read_guideline(citation=<rule id>).
         If either returns ToolRejected/None → record {dedup_key, half, reason} in could_not_locate,
         KEEP the candidate as-is (confidence unchanged, confidence_tier unchanged), continue.
      4. verdicts = [verify_once(candidate, source_text, rule_text, model, completion=completion)
         for model in panel]  (isolated calls; no shared state; verifier sees only claim+source+rule).
      5. decision, agreeing = consensus(candidate, verdicts, len(panel), nt=nt_for(candidate)).
      6. If decision == "DOWNGRADE": LOWER candidate.confidence (e.g. * a fixed general factor or set
         to a documented lower floor) and set candidate.confidence_tier = "low"; append
         {dedup_key, agreeing_verifiers: agreeing} to reviewed_downgrade. NEVER remove it.
         Else: append dedup_key to reviewed_keep (leave confidence_tier as "full").
      7. Append the (possibly-mutated) candidate to verified_faults for EVERY group.
      Return (verified_faults, CoverageReport(...)). Add a module-level comment tying the no-drop
      guarantee to the β law (code gate). The confidence-lowering factor must be a general constant
      (documented, not corpus-derived) — no submission-specific value.
    Turn tests/verify/test_invariant_no_drop.py from xfail/skip to a live assertion that (a) output
    length == unique-candidate count under an all-DOWNGRADE scripted panel, (b) the DOWNGRADEd fault
    is present with lowered confidence + confidence_tier "low", and (c) a source scan of
    src/verify/orchestrator.py finds no `.pop(`/`.remove(`/`del ` on a fault collection.
  </action>
  <acceptance_criteria>
    - `grep -q "def verify_candidates" src/verify/orchestrator.py`
    - `grep -q "def family_of" src/verify/orchestrator.py`
    - `grep -q "consensus(" src/verify/orchestrator.py && grep -q "panel_for(" src/verify/orchestrator.py`
    - `grep -q "get_section" src/verify/orchestrator.py && grep -q "read_guideline" src/verify/orchestrator.py` (full re-open, both halves)
    - `grep -q "could_not_locate" src/verify/orchestrator.py && grep -q "confidence_tier" src/verify/orchestrator.py`
    - `grep -v '^#' src/verify/orchestrator.py | grep -c "\.pop(\|\.remove(\|\bdel \b" | grep -qx 0` (no fault removal path)
    - `python3 -m pytest tests/verify/test_invariant_no_drop.py -x -q` passes
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/verify/test_invariant_no_drop.py -x -q</automated>
  </verify>
  <done>verify_candidates fans out on dedup_key with full re-opened context + decorrelated panel + consensus, lowers confidence + sets confidence_tier "low" on DOWNGRADE without ever removing a candidate, and emits a compliant-free CoverageReport; family_of defined; no-drop invariant test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: assemble.py — the Gap-1 report-assembly filter (KEEP-tier only in the scored FaultReport; DOWNGRADEd retained in CoverageReport)</name>
  <files>src/verify/assemble.py, tests/verify/test_scored_report_filter.py</files>
  <read_first>
    - src/verify/orchestrator.py (verify_candidates output — the (list[Fault], CoverageReport) this assembles from)
    - src/verify/coverage.py (CoverageReport.reviewed_downgrade — where DOWNGRADEd faults are already recorded)
    - src/schemas/faults.py (FaultReport shape lines 210-221; Fault.confidence_tier "full"|"low"|None lines 191-193)
    - src/evals/metrics.py (_end_to_end lines 77-80 — scores report.faults AS-IS; this is the frozen path the filter must feed KEEP-only faults into; DO NOT edit it)
    - tests/verify/conftest.py (labeled_fp_candidate / labeled_tp_candidate builders from Plan 01)
  </read_first>
  <behavior>
    - assemble_scored_report(verified_faults, coverage) -> FaultReport where FaultReport.faults
      contains ONLY faults whose confidence_tier != "low" (KEEP-tier / active). DOWNGRADEd faults
      (confidence_tier == "low") are NOT in report.faults — they remain recorded in
      coverage.reviewed_downgrade (nothing silently dropped: assert len(report.faults) +
      len(coverage.reviewed_downgrade) == len(verified_faults) modulo could_not_locate accounting).
    - Gap-1 both halves, on the labeled fixture:
      (a) a DOWNGRADEd FP (confidence_tier set to "low" by the panel) is NOT present in
          report.faults → `_end_to_end(report,...)` does NOT count it as an FP (precision rises).
      (b) a DOWNGRADEd TP is STILL retained: it is recorded in coverage AND — because the matcher
          keys on presence — the F1 gate's zero-TP-loss matched-set check (which reads coverage +
          report together, see Plan 04) still counts it as matched. The assemble helper NEVER
          removes a fault from the world; it only decides which surface (active vs downgraded) it
          appears on.
    - A could_not_locate candidate (confidence_tier still "full") STAYS in report.faults (active) —
      it was not downgraded.
  </behavior>
  <action>
    Create src/verify/assemble.py with
      `def assemble_scored_report(verified_faults: list[Fault], coverage: CoverageReport,
                                  *, job_id: str = "") -> FaultReport`:
      - active = [f for f in verified_faults if f.confidence_tier != "low"]  (KEEP-tier + could-not-
        locate; the ACTIVE findings the frozen F1 path scores).
      - Build FaultReport(job_id=job_id, faults=active, faults_found=bool(active), ...). DOWNGRADEd
        faults are NOT added to .faults; they are already in coverage.reviewed_downgrade (audit
        trail). Add a module docstring: this is the Gap-1 seam — `_end_to_end` scores report.faults
        as-is, so surfacing only KEEP-tier faults is what makes a DOWNGRADE actually lower the FP
        count; the DOWNGRADEd fault is retained in coverage so recall accounting (zero-TP-loss) can
        still see it. This helper NEVER deletes a Fault (no .pop/.remove/del on a fault list — the
        β no-drop law: a downgrade changes SURFACE, not existence).
      - Provide a `downgraded_dedup_keys(coverage) -> set[str]` (or reuse reviewed_downgrade) helper
        the Plan-04 zero-TP-loss check reads so a downgraded-but-present TP is counted as retained.
    Create tests/verify/test_scored_report_filter.py (# Gap-1). Using the Plan-01 labeled fixtures +
    ScriptedFleetClient, drive verify_candidates so ONE labeled FP and ONE labeled TP are DOWNGRADEd,
    then assemble_scored_report, then score with evals.metrics._end_to_end against the fixture's GT,
    and assert BOTH halves:
      (a) the DOWNGRADEd FP is NOT counted as an FP (fp count is strictly lower than scoring the
          pre-filter report where every fault is present);
      (b) the DOWNGRADEd TP is STILL retained — assert its dedup_key is in
          downgraded_dedup_keys(coverage) AND the zero-TP-loss accounting (matched via presence in
          report.faults + coverage.reviewed_downgrade) still credits it as matched, so no baseline TP
          is lost by the filter.
    This is the load-bearing anti-proxy-gate test: a NO-OP verifier (never DOWNGRADEs) leaves the FP
    in report.faults and FAILS half (a); a DROP-EVERYTHING verifier loses the TP and FAILS half (b).
  </action>
  <acceptance_criteria>
    - `grep -q "def assemble_scored_report" src/verify/assemble.py`
    - `grep -q 'confidence_tier != "low"' src/verify/assemble.py` (KEEP-tier-only filter present, grep-verifiable)
    - `grep -v '^#' src/verify/assemble.py | grep -c "\.pop(\|\.remove(\|\bdel \b" | grep -qx 0` (assembly never deletes a fault — β no-drop)
    - `grep -q "downgraded_dedup_keys\|reviewed_downgrade" src/verify/assemble.py` (zero-TP-loss accessor for Plan 04)
    - `test -f tests/verify/test_scored_report_filter.py && grep -q "Gap-1\|Gap 1" tests/verify/test_scored_report_filter.py`
    - `grep -q "_end_to_end\|assemble_scored_report" tests/verify/test_scored_report_filter.py` (test scores the assembled report through the frozen path)
    - `python3 -m pytest tests/verify/test_scored_report_filter.py -x -q` passes (BOTH halves: FP-not-counted + TP-retained)
    - `python3 -m pytest tests/verify/ -q` fully green (all Wave-0 stubs except tail now live)
  </acceptance_criteria>
  <verify>
    <automated>python3 -m pytest tests/verify/test_scored_report_filter.py tests/verify/ -q</automated>
  </verify>
  <done>assemble_scored_report surfaces ONLY KEEP-tier faults in FaultReport.faults (DOWNGRADEd retained in CoverageReport), the Gap-1 test proves a DOWNGRADEd FP is not scored as an FP AND a DOWNGRADEd TP is still retained/matched, and the assembly never deletes a fault.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-5 Fault list → orchestrator | recall-critical candidates cross into the precision filter |
| re-opened source/rule text → verifier | untrusted submission text is the verifier's only evidence |
| verified faults → scored FaultReport | the assembly filter decides which faults the frozen F1 path scores (Gap 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Denial of service (recall loss) | a silent filter/del removing a candidate | mitigate | source-scan test asserts no pop/remove/del on a fault list; output length == unique-candidate count; assemble.py retains DOWNGRADEd faults in coverage |
| T-07-08 | Tampering | consolidation collision dropping a distinct candidate | mitigate | consolidate merges (summed group lengths == input length); distinct dedup_keys stay separate |
| T-07-09 | Repudiation | unqualified "compliant" hiding what was not checked | mitigate | CoverageReport has no compliant field; could_not_locate names each un-located half |
| T-07-15 | Tampering (proxy-gate) | a DOWNGRADEd FP left in report.faults so F1 is unchanged (no-op verifier passes) | mitigate | assemble_scored_report excludes confidence_tier=="low" from report.faults; Gap-1 test proves FP-not-counted AND TP-retained |
</threat_model>

<verification>
- `python3 -m pytest tests/verify/ -q` green (only the tail stub remains xfail until Plan 04).
- `python3 -m pytest -q` full no-endpoint suite green.
- Static: no fault-removal path in orchestrator.py OR assemble.py; no "compliant" field in coverage.py.
- Gap-1: `python3 -m pytest tests/verify/test_scored_report_filter.py -x -q` proves the assembled scored report excludes DOWNGRADEd FPs (precision rises) and retains DOWNGRADEd TPs (zero-TP-loss).
</verification>

<success_criteria>
VERIFY-02 (fan-out on dedup_key, consolidate/dedup, honest coverage report) and VERIFY-01's
downgrade-never-drop invariant are realized as code paths with tests. The orchestrator re-opens FULL
source+rule (not the P6 ≤500-char excerpt), fans out a decorrelated panel, applies consensus, and
lowers confidence on DOWNGRADE without ever removing a candidate. The Gap-1 assembly filter closes
the proxy-gate: only KEEP-tier faults reach the frozen F1 path, DOWNGRADEd faults are retained in
coverage, and a test enforces both FP-not-counted and TP-retained.
</success_criteria>

<output>
After completion, create `.planning/phases/07-multi-agent-verification-interpretive-tail/07-03-SUMMARY.md`
</output>
</output>
