---
phase: 01-ingestion-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/ingest/__init__.py
  - tests/ingest/conftest.py
  - tests/ingest/fixtures/__init__.py
  - tests/ingest/fixtures/build_merged_docx.py
  - tests/ingest/fixtures/merged_cells.docx
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-04, INGEST-05]
must_haves:
  truths:
    - "Every tests/ingest/* module can import shared fixtures from conftest without error"
    - "A committed merged-cell DOCX fixture exists with one horizontal (gridSpan) and one vertical (vMerge) merge"
    - "A synthetic document-dict factory produces the exact dict shape extract_pdf emits (converged model)"
    - "A corpus-tree builder writes an arbitrary nested tmp tree of PDF + DOCX for the rename-folders test"
  artifacts:
    - path: "tests/ingest/conftest.py"
      provides: "offline OCR monkeypatch, _block/_grid builders, doc-dict factory (incl. merged tables), tmp corpus-tree builder"
      min_lines: 60
    - path: "tests/ingest/fixtures/merged_cells.docx"
      provides: "committed DOCX with gridSpan + vMerge merges for SC2 merged-cell fidelity"
    - path: "tests/ingest/fixtures/build_merged_docx.py"
      provides: "deterministic, idempotent generator for merged_cells.docx"
  key_links:
    - from: "tests/ingest/conftest.py"
      to: "parse.ocr.get_settings"
      via: "monkeypatch to empty Databricks creds"
      pattern: "monkeypatch.setattr.*get_settings"
---

<objective>
Create the shared test scaffolding and committed fixtures every later Phase-1 plan's tests import. This is the Wave-0 foundation: without it, downstream plans cannot express their `<automated>` acceptance commands.

Purpose: The Nyquist validation contract (01-VALIDATION.md) requires every substrate/DOCX/table/store test to run offline, endpoint-free, against deterministic fixtures. This plan builds those fixtures once so all nine test files reuse them.
Output: `tests/ingest/` package with `conftest.py` (offline fixture, dict builders, doc-dict factory, corpus-tree builder) and a committed `merged_cells.docx` (the only merged-cell DOCX in the repo — `mini_spec.docx` has no merges).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ingestion-foundation/01-VALIDATION.md
@.planning/phases/01-ingestion-foundation/01-PATTERNS.md
@.planning/phases/01-ingestion-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared conftest — offline fixture, dict builders, doc-dict factory, corpus-tree builder</name>
  <files>tests/ingest/__init__.py, tests/ingest/conftest.py</files>
  <read_first>
    - tests/unit/test_section_splitter.py:20-38 (the `offline` monkeypatch + `_block`/`_grid` builders to copy)
    - src/parse/pdf.py:197-258 (extract_pdf return shape the doc-dict factory MUST match byte-for-byte)
    - src/parse/section_splitter.py:196-251 (`split_document` — the consumer whose dict keys the factory must satisfy)
    - .planning/phases/01-ingestion-foundation/01-PATTERNS.md:324-336 (conftest pattern assignment)
  </read_first>
  <action>
Create `tests/ingest/__init__.py` (empty, so tests/ingest is a regular package like tests/unit). Create `tests/ingest/conftest.py` with:

1. `offline` fixture — copy verbatim from test_section_splitter.py:20-26:
   ```python
   @pytest.fixture
   def offline(monkeypatch):
       from config import Settings
       import parse.ocr as ocrmod
       monkeypatch.setattr(ocrmod, "get_settings",
                           lambda: Settings(databricks_host="", databricks_token=""))
   ```
   Also monkeypatch `llm.client`'s settings the same way if the classifier tests need a no-creds LLM path (add a `no_llm` fixture doing the same for `parse.ocr` AND any `get_settings` the classifier imports).

2. `_block(text, page=1, order=0)` and `_grid(page, y0, y1, headers, rows, title="")` builders — copy from test_section_splitter.py:29-38.

3. `make_doc_dict(blocks, tables, filename="doc.pdf", page_count=1, toc=None)` factory returning the EXACT extract_pdf dict:
   `{"filename":..., "page_count":..., "toc":[...], "pages":[{"page_number":1,"page_label":"","width":612.0,"height":792.0,"rotation":0,"source":"pymupdf","is_scanned":False,"blocks":[...],"tables":[...],"figures":[]}]}`.
   Provide a `merged_table(headers, rows, merged_origins)` helper that builds an ExtractedTable dict (kind="grid") including the new `table_id` and `merged_origins` fields so serialize/tables tests can feed a merged table WITHOUT depending on the DOCX parser. `merged_origins` maps `"r,c"` (a covered non-origin coord) -> `[origin_r, origin_c]`.

4. `write_corpus_tree(root, spec)` builder: given a dict mapping relative paths (e.g. `"m3/quality/spec.pdf"`, `"Renamed Folder/AAA.docx"`) to file-content keys, writes a nested tmp tree of real tiny PDF/DOCX files. DOCX via `docx.Document()` (mirror build_merged_docx.py); PDF via fitz: `doc=fitz.open(); page=doc.new_page(); page.insert_text((72,72), content_text); doc.save(path)` — the PDFs MUST be real parseable PDFs with the given text content, NOT empty/invalid stubs, because the SC1 rename-folders test proves *classification* invariance and a parse_failed stub would never reach classification. For byte-identity across layouts, generate each unique content key ONCE to bytes, then write the same bytes at every path that references it (fitz output embeds no timestamp by default; single-generation sidesteps any nondeterminism). Used by the SC1 rename-folders test to build layout A and renamed-layout B with identical file *bytes*.
  </action>
  <acceptance_criteria>
    - `python -c "import ast,sys; ast.parse(open('tests/ingest/conftest.py').read())"` exits 0 (valid syntax).
    - `grep -q "def offline" tests/ingest/conftest.py && grep -q "def make_doc_dict" tests/ingest/conftest.py && grep -q "def write_corpus_tree" tests/ingest/conftest.py && grep -q "merged_origins" tests/ingest/conftest.py` all pass.
    - `pytest tests/ingest/ -q` collects with 0 errors (no tests yet is OK; collection must not error).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Committed merged-cell DOCX fixture (gridSpan + vMerge)</name>
  <files>tests/ingest/fixtures/__init__.py, tests/ingest/fixtures/build_merged_docx.py, tests/ingest/fixtures/merged_cells.docx</files>
  <read_first>
    - src/evals/make_docx_fixture.py:61-85 (the python-docx write-side API to mirror: `Document()`, `add_table`, `table.rows[r].cells`, `cell.text`, `cell.merge`)
    - .planning/phases/01-ingestion-foundation/01-RESEARCH.md:269-289 (Pattern 3 — `_tc` identity; a 3x3 with (0,0)-(0,1) horizontal + (1,2)-(2,2) vertical merge is the exact fixture shape)
  </read_first>
  <action>
Create `tests/ingest/fixtures/__init__.py` (empty) and `tests/ingest/fixtures/build_merged_docx.py`: a deterministic, idempotent generator (NO random/time values) mirroring make_docx_fixture.py. Build a Document with a heading, a short paragraph, and ONE 3x3 table where:
  - cells (0,0) and (0,1) are horizontally merged (`table.cell(0,0).merge(table.cell(0,1))`), origin (0,0), text "Merged Header AB";
  - cells (1,2) and (2,2) are vertically merged (`table.cell(1,2).merge(table.cell(2,2))`), origin (1,2), text "Spanning Limit";
  - remaining cells carry distinct texts ("R0C2","R1C0","R1C1","R2C0","R2C1") so every coordinate is distinguishable.
Set `table.style = "Table Grid"`. Expose `build_document()` + `main()` that writes `tests/ingest/fixtures/merged_cells.docx` (path via `Path(__file__).parent`). Run `python tests/ingest/fixtures/build_merged_docx.py` and COMMIT the resulting `merged_cells.docx`.
  </action>
  <acceptance_criteria>
    - `python tests/ingest/fixtures/build_merged_docx.py` prints the written path and exits 0.
    - `test -f tests/ingest/fixtures/merged_cells.docx` (committed asset present).
    - `python -c "import docx; d=docx.Document('tests/ingest/fixtures/merged_cells.docx'); t=d.tables[0]; a=t.cell(0,0)._tc; b=t.cell(0,1)._tc; assert a is b, 'horizontal merge not healed'; c=t.cell(1,2)._tc; e=t.cell(2,2)._tc; assert c is e, 'vertical merge not healed'; print('OK')"` prints OK (proves `_tc` identity dedup is exercisable). NOTE: both `_tc` references MUST be bound to variables and compared with `is` — lxml element proxies are only identity-stable while a reference is alive, so `id(...)==id(...)` without held refs can false-PASS on an unmerged table (first proxy freed, second reuses its address).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → filesystem (tmp) | Fixtures write only under pytest `tmp_path` / the committed `tests/ingest/fixtures/` dir; no untrusted input is parsed here. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | committed merged_cells.docx | accept | Fixture is generated deterministically and committed; regenerable via `build_merged_docx.py`. No runtime trust placed in it beyond tests. |
</threat_model>

<verification>
- `pytest tests/ingest/ -q` collects cleanly (no collection errors).
- `merged_cells.docx` present and its two merges heal to single `_tc` ids (Task 2 acceptance).
</verification>

<success_criteria>
- Shared conftest importable; `make_doc_dict`, `merged_table`, `write_corpus_tree`, `offline` available to all downstream tests.
- Committed merged-cell DOCX fixture exists and exercises both merge types.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-foundation/01-01-SUMMARY.md`.
</output>
