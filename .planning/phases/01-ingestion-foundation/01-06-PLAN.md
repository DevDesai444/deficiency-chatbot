---
phase: 01-ingestion-foundation
plan: 06
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - src/ingest/anchors.py
  - src/ingest/tables.py
  - tests/ingest/test_anchors.py
  - tests/ingest/test_tables.py
autonomous: true
requirements: [INGEST-04, INGEST-05]
must_haves:
  truths:
    - "Given a span-ID, the re-open primitive returns BOTH the raw source substring AND the canonical substring, or FAILS on hash mismatch — never one, never a silent wrong substring"
    - "Re-opening a span reproduces the text byte-for-byte; a tampered stream or wrong normalizer version raises HashMismatch"
    - "Every table cell is addressable through a (table_id,row,col) index that resolves to an ordinary canonical span-ID"
    - "Every coordinate a merged cell spans resolves to the SAME span-ID (the origin's)"
    - "Serializing/indexing the same document twice yields identical (table_id,row,col)->span-ID maps and identical offsets (deterministic, version-stamped)"
    - "Decisions honored: D-18 (section IDENTITY is the content-addressed span-ID; heading text is a human-readable label only — both stored)"
  artifacts:
    - path: "src/ingest/anchors.py"
      provides: "SpanID mint + open_span(raw,canonical)|FAIL + short_hash + HashMismatch (the D-21 re-open/verify primitive)"
      exports: ["mint_span", "open_span", "short_hash", "HashMismatch"]
    - path: "src/ingest/tables.py"
      provides: "build_table_index((table_id,row,col)->SpanID); merged coords resolve to origin span; PDF best-effort caveat"
      exports: ["build_table_index"]
  key_links:
    - from: "src/ingest/tables.py"
      to: "normalize.raw_range_to_canon + anchors.mint_span"
      via: "convert serializer raw cell range -> canonical range -> span-ID"
      pattern: "raw_range_to_canon"
    - from: "src/ingest/anchors.py"
      to: "normalize.canon_range_to_raw"
      via: "open_span renders the raw citation from a canonical span"
      pattern: "canon_range_to_raw"
---

<objective>
Build the two primitives Phase 2's navigation tools rest on: the **re-open/verify primitive** (`open_span`, D-21) that returns both raw (the verbatim citation, D-22) and canonical substrings or fails on hash mismatch, and the **table cell addressing** index (`(table_id,row,col) → span-ID`, D-31) where merged coordinates resolve identically.

Purpose: This is the anchor half of the grounding contract — byte-exact, unique, tamper-evident, identical machinery for PDF and DOCX (D-19). Phase 5 SC1's "code recomputation over two verbatim cells" and Phase 4's X1/X2 cell-level comparisons have no substrate without the table index created here at ingest.
Output: `ingest/anchors.py`, `ingest/tables.py`.

Prerequisite: Plan 03's `test_offset_roundtrip` is GREEN (RISK-1 cleared) — these are the first consumers of the offset map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ingestion-foundation/01-RESEARCH.md
@.planning/phases/01-ingestion-foundation/01-CONTEXT.md
@.planning/phases/01-ingestion-foundation/01-PATTERNS.md
@src/schemas/documents.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: anchors.py — span-ID mint + re-open/verify primitive (D-21 contract)</name>
  <files>src/ingest/anchors.py, tests/ingest/test_anchors.py</files>
  <behavior>
    - mint_span(canonical, start, end, doc_id, normalizer_version) returns a SpanID whose hash = short_hash(canonical[start:end], normalizer_version).
    - open_span(span, normalized_text, doc_id) returns (raw, canonical) where canonical == normalized_text.canonical[span.start:span.end] and raw == the raw source substring via canon_range_to_raw — byte-for-byte.
    - A canonical range spanning a dropped hyphen returns the hyphen in the raw side (from the offset map).
    - Mutating the stored canonical text OR passing a SpanID minted under a different normalizer_version raises HashMismatch (never returns a wrong substring).
  </behavior>
  <read_first>
    - .planning/phases/01-ingestion-foundation/01-RESEARCH.md:453-471 (the SpanID + open_span design to implement verbatim — the D-21 contract)
    - .planning/phases/01-ingestion-foundation/01-PATTERNS.md:77-95 (anchors pattern assignment; hash over CANONICAL substring + normalizer_version; geometry must NEVER enter SpanID)
    - src/schemas/llm.py:6-15 (ParseFailed ethos — a typed HashMismatch raise, never a silent wrong-substring return)
    - src/ingest/normalize.py (canon_range_to_raw from Plan 03; NormalizedText fields)
    - src/schemas/documents.py (SpanID type from Plan 02)
  </read_first>
  <action>
Create `src/ingest/anchors.py`:
- `def short_hash(canonical_substr: str, normalizer_version: str) -> str`: `hashlib.blake2b((normalizer_version + "\x00" + canonical_substr).encode("utf-8"), digest_size=8).hexdigest()`. Hash over the CANONICAL substring + version (RESEARCH Open Q1 — clean normalizer-drift detection; low-risk planner call under D-15). NO geometry, ever (D-19 boundary law).
- `class HashMismatch(Exception)`: carry the offending SpanID + expected/actual hash (typed, self-correcting for Phase 2's emit gate).
- `def mint_span(canonical: str, start: int, end: int, doc_id: str, normalizer_version: str) -> SpanID`: return `SpanID(doc_id, start, end, hash=short_hash(canonical[start:end], normalizer_version))`.
- `def open_span(span: SpanID, nt: NormalizedText, doc_id: str) -> tuple[str,str]` implementing the LOCKED D-21 contract (RESEARCH:461-467):
  ```
  canonical = nt.canonical[span.start:span.end]
  if short_hash(canonical, nt.normalizer_version) != span.hash: raise HashMismatch(...)
  raw_s, raw_e = canon_range_to_raw(nt.offset_map, span.start, span.end)
  return nt.raw_serialized[raw_s:raw_e], canonical   # BOTH, never one
  ```
  Arbitrary ranges are valid span-IDs; the section outline and table index just provide NAMED spans on top.

Write `tests/ingest/test_anchors.py::test_reopen_and_hash_mismatch`: mint over a normalized fixture (build via `ingest.normalize.normalize` on a raw string incl. a wrapped hyphen), assert byte-exact raw+canonical round-trip, assert the hyphen appears in the raw side, and assert tamper (mutate canonical) → HashMismatch and wrong-version SpanID → HashMismatch.
  </action>
  <acceptance_criteria>
    - `pytest tests/ingest/test_anchors.py::test_reopen_and_hash_mismatch -x` passes (INGEST-04/D-21).
    - `grep -v '^#' src/ingest/anchors.py | grep -Ec 'bbox|page|rotation'` returns 0 (no geometry in the identity module).
    - `grep -q 'class HashMismatch' src/ingest/anchors.py` and `grep -q 'def open_span' src/ingest/anchors.py` pass.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tables.py — (table_id,row,col) index with merged-coord identical resolution</name>
  <files>src/ingest/tables.py, tests/ingest/test_tables.py</files>
  <behavior>
    - build_table_index over a document whose serializer produced cell_ranges + a table with merged_origins returns a dict "table_id,r,c" -> SpanID for EVERY coordinate (including covered merged coords).
    - Every (r,c) a merge spans returns the SAME SpanID object/value as its origin (D-31).
    - Building the index twice over the same NormalizedText + tables yields identical maps and identical SpanID offsets (deterministic, version-stamped).
    - Cell span-IDs re-open (via anchors.open_span) to the exact cell text.
  </behavior>
  <read_first>
    - .planning/phases/01-ingestion-foundation/01-RESEARCH.md:269-289 (Pattern 3 — merged coord→origin) and :370-376 (Pitfall 4 — PDF find_tables loses merge metadata → best-effort)
    - .planning/phases/01-ingestion-foundation/01-PATTERNS.md:124-141 (tables pattern assignment + PDF-path caveat)
    - .planning/phases/01-ingestion-foundation/01-CONTEXT.md D-31 (dual addressing over one grounding contract)
    - src/ingest/serialize.py (cell_ranges keys "ti,r,c" for ORIGIN cells — from Plan 02)
    - src/ingest/normalize.py (raw_range_to_canon — from Plan 03) and src/ingest/anchors.py (mint_span — Task 1)
  </read_first>
  <action>
Create `src/ingest/tables.py`:
- `def build_table_index(nt: NormalizedText, tables: list[dict], cell_ranges: dict[str,list[int]], doc_id: str) -> dict[str, SpanID]`:
  For each table `ti` with `table_id`:
    1. For each ORIGIN cell key `"ti,r,c"` in `cell_ranges`: convert its RAW range → canonical via `raw_range_to_canon(nt.offset_map, rs, re)`, then `mint_span(nt.canonical, cs, ce, doc_id, nt.normalizer_version)`. Store under `f"{table_id},{r},{c}"`.
    2. For every COVERED coordinate in that table's `merged_origins` (`"r,c" -> [orow, ocol]`): map `f"{table_id},{r},{c}"` to the SAME SpanID as `f"{table_id},{orow},{ocol}"` (D-31 — many coords → one span).
  Return the full `(table_id,row,col) → SpanID` index.
- **PDF best-effort caveat (Pitfall 4):** accept an optional `table_reliability: dict[table_id,bool]`; when a PDF table is non-rectangular / has interior empties adjacent to filled cells, the caller (Plan 09) marks that table's tier `unavailable`/best-effort in the manifest (D-30). Do NOT claim merged-cell addressability the PDF grid can't back — this module only builds the index it is given; it does not fabricate merge info for PDF.
- Serialization/index order is deterministic and inherits `SERIALIZER_VERSION`/`NORMALIZER_VERSION` (version-stamped, D-31).

Write `tests/ingest/test_tables.py::test_merged_resolves_identically` and `::test_serialization_deterministic` using the conftest `make_doc_dict`+`merged_table` factory → `serialize_document` → `normalize` → `build_table_index`. Assert every merged coord shares one SpanID and two builds are identical; assert each cell span re-opens to its text.
  </action>
  <acceptance_criteria>
    - `pytest tests/ingest/test_tables.py::test_merged_resolves_identically -x` passes (INGEST-05/D-31).
    - `pytest tests/ingest/test_tables.py::test_serialization_deterministic -x` passes.
    - `grep -q 'raw_range_to_canon' src/ingest/tables.py` and `grep -q 'merged_origins' src/ingest/tables.py` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| stored canonical text → re-opened span | On re-open, the stored stream is treated as potentially drifted/tampered; the content-hash is the integrity check that fails loudly rather than returning a wrong citation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | content-hash tamper / span integrity | mitigate | `open_span` re-verifies `short_hash(canonical, normalizer_version)` and raises `HashMismatch` on any drift (D-21). blake2b is a checksum for drift detection, not an auth boundary (V6 partial). |
| T-01-14 | Tampering | PDF merged cell mis-addressed silently corrupting X1 | mitigate | PDF merged-cell resolution declared best-effort in the manifest (D-30); addressability is only claimed where the grid backs it (DOCX `_tc`). No fabricated merge info. |
</threat_model>

<verification>
- `pytest tests/ingest/test_anchors.py tests/ingest/test_tables.py -x -q` green.
- No geometry symbols in anchors.py; merged coords resolve to one span; deterministic across two builds.
</verification>

<success_criteria>
- open_span returns both raw+canonical or raises HashMismatch; byte-exact round-trip incl. dropped-hyphen case.
- (table_id,row,col) index resolves every coord; merged coords share the origin span; deterministic + version-stamped.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-foundation/01-06-SUMMARY.md`.
</output>
