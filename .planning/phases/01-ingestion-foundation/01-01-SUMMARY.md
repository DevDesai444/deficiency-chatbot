---
phase: 01-ingestion-foundation
plan: 01
subsystem: testing
tags: [pytest, conftest, fixtures, python-docx, pymupdf, docx-merge]

requires:
  - phase: 00-eval-harness
    provides: offline OCR monkeypatch pattern (tests/unit/test_section_splitter.py) reused here
provides:
  - tests/ingest/ package with a shared conftest (offline/no_llm fixtures, dict + doc-dict builders, real-file corpus-tree builder)
  - a committed merged-cell DOCX fixture (gridSpan + vMerge) — the only merged-cell DOCX in the repo
affects: [01-02, 01-03, 01-04, 01-05, 01-06, 01-07, 01-08, 01-09]

tech-stack:
  added: []
  patterns:
    - "Wave-0 shared conftest: all tests/ingest tests import fixtures/builders from one conftest so the suite runs offline"
    - "Real-file corpus builder: write_corpus_tree emits parseable fitz PDFs + python-docx DOCX, byte-identical across paths sharing a content key"

key-files:
  created:
    - tests/ingest/conftest.py
    - tests/ingest/fixtures/build_merged_docx.py
    - tests/ingest/fixtures/merged_cells.docx
  modified: []

key-decisions:
  - "make_doc_dict/merged_table build PLAIN dicts (not pydantic models), so Wave-0 scaffolding has no dependency on Plan 02's schema fields (table_id/merged_origins) — the DOCX/serialize tests can feed a merged table before the schema or parser exist."
  - "write_corpus_tree generates each (content-key, ext) ONCE to bytes and byte-copies to every referencing path, guaranteeing renamed layouts are byte-identical — the SC1 rename-folders invariant proves classification (not parse) invariance, so the PDFs must be REAL and parseable, not stubs."
  - "merged_cells.docx is generated by merging cells FIRST then assigning origin text, so python-docx does not concatenate pre-existing cell texts into the merged cell."

patterns-established:
  - "offline / no_llm fixtures: monkeypatch parse.ocr.get_settings (and llm.client.get_settings) to empty Databricks creds so the whole ingest suite is endpoint-free."

requirements-completed: []  # Wave-0 scaffolding: SUPPORTS INGEST-01/02/04/05 test expression but COMPLETES none — those requirements are delivered across later waves.

duration: ~20min
completed: 2026-07-30
---

# Phase 1 · Plan 01: Test Scaffolding & Merged-Cell Fixture — Summary

**Built the Wave-0 `tests/ingest/` foundation — a shared offline conftest (fixtures, dict/doc-dict builders, real-file corpus-tree builder) plus the repo's only merged-cell DOCX fixture — so every later Phase-1 plan can express its endpoint-free `<acceptance_criteria>`.**

## Performance
- **Duration:** ~20 min
- **Tasks:** 2 completed
- **Files created:** 5 (2 package inits, conftest, generator, committed .docx)

## Accomplishments
- `tests/ingest/conftest.py`: `offline` + `no_llm` fixtures, `_block`/`_grid`/`merged_table` builders, `make_doc_dict` (exact `extract_pdf` dict shape), and `write_corpus_tree` (nested real PDF/DOCX tree, byte-identical across renamed paths).
- `tests/ingest/fixtures/merged_cells.docx` + deterministic `build_merged_docx.py`: a 3×3 table with a horizontal (gridSpan) merge at (0,0)-(0,1) and a vertical (vMerge) merge at (1,2)-(2,2), both healing to a single `_tc` — the fixture SC2's merged-cell fidelity test gates on.

## Task Commits
1. **Task 1: shared conftest + package init** — `873b51b` (test)
2. **Task 2: merged-cell DOCX fixture + generator** — `e26b3c2` (test)

## Verification (every acceptance command actually run, via `.venv/bin/python`)
- `ast.parse(conftest.py)` → SYNTAX OK.
- greps for `def offline` / `def make_doc_dict` / `def write_corpus_tree` / `merged_origins` → all pass.
- `pytest tests/ingest/ -q` → collects with **0 errors** (exit 5 "no tests yet", allowed by the criterion; warnings are a harmless PyMuPDF SWIG deprecation).
- `build_merged_docx.py` → wrote the fixture, exit 0; `merged_cells.docx` present.
- `_tc` identity check (held refs + `is`, per the rewritten criterion) → both merges healed → **OK**.
- Extra sanity: `write_corpus_tree` produces byte-identical PDF/DOCX across renamed paths, and the PDFs are real + parseable with their content reaching the block layer.

## Files Created/Modified
- `tests/ingest/__init__.py`, `tests/ingest/fixtures/__init__.py` — package markers.
- `tests/ingest/conftest.py` — shared fixtures + builders.
- `tests/ingest/fixtures/build_merged_docx.py` — deterministic fixture generator.
- `tests/ingest/fixtures/merged_cells.docx` — committed binary fixture.

## Issues Encountered
- None. (`python` is not on PATH; used the project `.venv/bin/python` — Python 3.12.13 with fitz/docx/pytest and `pythonpath=src` resolved via pyproject.)

## Notes for the reviewer (wave gate)
- No product code yet — this is test infrastructure only. None of the 13 staged redesign files were touched; commits staged only the plan's exact `files_modified`.
