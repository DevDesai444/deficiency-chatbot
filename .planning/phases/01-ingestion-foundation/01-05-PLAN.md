---
phase: 01-ingestion-foundation
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/ingest/registry/__init__.py
  - src/ingest/registry/ctd_families.yaml
  - src/ingest/registry/body_lexicon.yaml
  - src/agents/detection/ctd.py
  - src/agents/detection/checklists.py
  - tests/ingest/test_registry.py
autonomous: true
requirements: [INGEST-01]
must_haves:
  truths:
    - "The CTD-family vocabulary lives in a data-driven registry (id, label, applicability trigger) — adding a family is a data edit, not a code change"
    - "The body keyword lexicon lives in the same registry, keyed per family"
    - "Registry ids equal the existing CTDSection StrEnum values so checklists/pipeline consumers keep matching (no renumber)"
    - "detect_ctd_section still returns a CTDSection for existing callers; a doc-level entry is added beside it"
    - "The registry entry shape matches Phase 2's RULES-05 requirement-index shape (id + one-line trigger)"
  artifacts:
    - path: "src/ingest/registry/__init__.py"
      provides: "load_families()/load_lexicon()/family_ids() loader + D-05 CTDSection compat accessor"
      exports: ["load_families", "load_lexicon", "family_ids"]
    - path: "src/ingest/registry/ctd_families.yaml"
      provides: "{id,label,applicability_trigger} per CTD family, ids == CTDSection values"
    - path: "src/ingest/registry/body_lexicon.yaml"
      provides: "per-family keyword lexicon (D-06)"
  key_links:
    - from: "src/agents/detection/checklists.py"
      to: "registry ids == CTDSection values"
      via: "_VALIDATION_SECTIONS membership still resolves"
      pattern: "_VALIDATION_SECTIONS"
---

<objective>
Migrate the CTD-family vocabulary out of the `CTDSection` Python enum into a data-driven registry (D-05) with a per-family body lexicon (D-06), keeping every existing consumer working via a compat shim (ids == StrEnum values). The registry entry shape deliberately mirrors Phase 2's RULES-05 requirement index — build it once, reuse.

Purpose: Adding Modules 1/2/4/5 or biologics families (v2) becomes a data edit. This is the classification vocabulary `classify.py` (Plan 08) reads.
Output: `src/ingest/registry/` package (loader + two YAML data files); minimal read-compat touches to `ctd.py`, `checklists.py`, `pipeline.py`.

**Naming decision (resolved):** the registry is a PACKAGE `src/ingest/registry/` whose `__init__.py` is the thin loader (with the D-05 compat shim) plus two YAML data files — NOT a `registry.py` module next to a `registry/` dir (that name would collide). `from ingest.registry import load_families` is the entry point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ingestion-foundation/01-CONTEXT.md
@.planning/phases/01-ingestion-foundation/01-PATTERNS.md
@src/agents/detection/catalog.py
@src/schemas/documents.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry package — loader + YAML families + body lexicon</name>
  <files>src/ingest/registry/__init__.py, src/ingest/registry/ctd_families.yaml, src/ingest/registry/body_lexicon.yaml, tests/ingest/test_registry.py</files>
  <behavior>
    - load_families() returns a list/dict of entries each with keys {id, label, applicability_trigger}; every id equals an existing CTDSection value string (e.g. "3.2.S.4.1").
    - load_lexicon() returns a dict keyed by the same family ids → list[str] of body keywords.
    - family_ids() == set of all registry ids; it is a superset of the CTDSection values currently used by checklists (_VALIDATION_SECTIONS).
    - Loading is cached (lru_cache) and reads the YAML via Path(__file__).parent.
  </behavior>
  <read_first>
    - src/agents/detection/catalog.py:11-63 (CANONICAL_DOMAINS dict + domain_catalog_text() — the EXACT registry shape + "feed the catalog text to the LLM" renderer to mirror)
    - src/schemas/documents.py:8-35 (CTDSection StrEnum — its VALUES become the registry ids; do NOT renumber)
    - src/agents/detection/checklists.py:19-36 (_VALIDATION_REQUIRED lexicon shape + _VALIDATION_SECTIONS the ids must satisfy)
    - .planning/phases/01-ingestion-foundation/01-PATTERNS.md:197-210 (registry pattern assignment + migration-safety convention)
    - .planning/phases/01-ingestion-foundation/01-CONTEXT.md D-05,D-06 (locked registry contract + RULES-05 alignment)
  </read_first>
  <action>
Create the package `src/ingest/registry/`:

1. `ctd_families.yaml` — one entry per CTDSection value (S_1..R_REGIONAL; skip UNKNOWN), each:
   ```yaml
   - id: "3.2.S.4.1"
     label: "Drug Substance Specification"
     applicability_trigger: "A drug-substance specification / acceptance-criteria table (CoA, limits)."
   ```
   Ids MUST be the exact StrEnum values from documents.py:8-35. Derive the one-line `applicability_trigger` from the section meaning + the closest `CANONICAL_DOMAINS` description in catalog.py (RULES-05 shape). Include a top-of-file comment: `# ids == CTDSection values (D-05 migration-safety); a new family is a data edit, not code.`

2. `body_lexicon.yaml` — per-family keyword lists (D-06), keyed by the same ids, seeded from `checklists.py:19-29` `_VALIDATION_REQUIRED` and `catalog.py` domain vocabulary:
   ```yaml
   "3.2.S.4.1": ["specification", "acceptance criteria", "limit", "assay", "impurity"]
   "3.2.S.4.3": ["validation", "specificity", "linearity", "accuracy", "robustness"]
   ```

3. `__init__.py` — thin loader (PyYAML if available; else fall back to a stdlib parse — but prefer `import yaml`; add `pyyaml` note in SUMMARY if it must be added to pyproject). Provide:
   - `@lru_cache load_families() -> list[dict]`, `@lru_cache load_lexicon() -> dict[str, list[str]]`, `family_ids() -> set[str]`.
   - `families_catalog_text() -> str` mirroring `catalog.py domain_catalog_text()` (`"- {id} ({label}): {trigger}"`) for the LLM prompt (Plan 08).
   - A D-05 compat accessor `as_ctd_section(family_id: str) -> CTDSection` that maps a registry id back to the `CTDSection` enum member (so consumers that still type-annotate `CTDSection` interoperate).

Write `tests/ingest/test_registry.py` asserting: every registry id is a valid CTDSection value; `family_ids()` ⊇ the four `_VALIDATION_SECTIONS` values; lexicon keys ⊆ family_ids.
  </action>
  <acceptance_criteria>
    - `pytest tests/ingest/test_registry.py -x -q` passes.
    - `python -c "from ingest.registry import load_families, family_ids; from schemas.documents import CTDSection; vals={s.value for s in CTDSection}; assert family_ids() <= vals, family_ids()-vals; print('ids OK', len(load_families()))"` prints ids OK.
    - `grep -q 'applicability_trigger' src/ingest/registry/ctd_families.yaml` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: D-05 consumer migration — ctd.py doc-level entry + checklists/pipeline read-compat</name>
  <files>src/agents/detection/ctd.py, src/agents/detection/checklists.py</files>
  <behavior>
    - detect_ctd_section(text) still returns a CTDSection (unchanged signature) for existing callers.
    - A new doc-level helper detect_family(text) -> str returns a registry id (or "" / "unknown") using the same regex seam, generalized to read registry ids.
    - checklists.run_checklists still resolves its validation sections via ids that match registry entries (no renumber); existing detection unit tests still pass.
    - pipeline.py is READ-ONLY in this plan: its detect_ctd_section/describe_document calls keep working because ctd.py preserves both signatures — verify by reading, make NO edit, and do NOT `git add` pipeline.py (it carries a staged, uncommitted redesign delta that must not be swept into a registry commit). If verification reveals an edit IS unavoidable, STOP and flag rather than committing the file.
  </behavior>
  <read_first>
    - src/agents/detection/ctd.py (whole file — the regex seam to extend to doc level; keep detect_ctd_section as the shim)
    - src/agents/detection/checklists.py:31-36 and its run_checklists usage of _VALIDATION_SECTIONS
    - src/agents/detection/pipeline.py:16,42-44 (imports + detect_ctd_section/describe_document call site — READ the CURRENT uncommitted version; make a minimal additive edit only)
    - .planning/phases/01-ingestion-foundation/01-PATTERNS.md:309-314 (migration-touch consumers — read-compat, do not renumber)
    - src/ingest/registry/__init__.py (from Task 1)
  </read_first>
  <action>
Additive, minimal edits — this is a code edit, not a data migration (no stored enum values on disk yet; do NOT renumber — RESEARCH:350):

- `ctd.py`: keep `detect_ctd_section(text) -> CTDSection` exactly. Add a doc-level `detect_family(text) -> str` that reuses the `_CTD_PATTERNS` first-match seam but returns a registry id string (via `ingest.registry`), returning `""` when no literal CTD number matches (the low-confidence case Plan 08 escalates). Keep `describe_document` working.
- `checklists.py`: leave `_VALIDATION_SECTIONS` as CTDSection members (their values already equal registry ids, so membership keeps matching). If any hardcoded family list is duplicated, add a comment pointing to `ingest.registry` as the single source of truth; do not rip out working code.
- `pipeline.py`: no behavioral change required — verify `detect_ctd_section(_leading_text(doc))` still resolves. If a doc-level family is wanted, it is added in Plan 08's classifier, NOT here. **Do not revert or overwrite the uncommitted redesign lines in this file.**
  </action>
  <acceptance_criteria>
    - `python -c "from agents.detection.ctd import detect_ctd_section, detect_family; from schemas.documents import CTDSection; assert isinstance(detect_ctd_section('see 3.2.S.4.1'), CTDSection); print(detect_family('3.2.S.4.1 Specification'))"` runs and prints a registry id (e.g. 3.2.S.4.1).
    - `pytest tests/unit/test_detection.py -x -q` still passes (no detection regression).
    - `git diff --stat src/agents/detection/pipeline.py` shows a SMALL additive diff (no large deletions of redesign code).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| registry YAML (repo-controlled) → classifier | The registry is trusted, version-controlled config, not untrusted input. Path/folder names never enter classification (D-09) — the registry keys on content vocabulary only. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering | malformed registry YAML | accept | Repo-controlled config; `test_registry.py` validates ids ⊆ CTDSection at load. A bad edit fails the test, not production. |
| T-01-04 | Tampering | filename/path as classification signal | mitigate | Registry keys on content vocabulary only; path exclusion (D-09) enforced by construction in the corpus (Plan 09) and proven by the SC1 rename-folders test. |
</threat_model>

<verification>
- `pytest tests/ingest/test_registry.py tests/unit/test_detection.py -x -q` green.
- Registry ids ⊆ CTDSection values; `_VALIDATION_SECTIONS` still resolves.
</verification>

<success_criteria>
- Data-driven registry (families + lexicon) loads; ids equal CTDSection values (no renumber).
- Existing detection consumers unchanged in behavior; pipeline redesign untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-foundation/01-05-SUMMARY.md`. Note whether `pyyaml` had to be added to pyproject.toml.
</output>
