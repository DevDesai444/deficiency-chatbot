---
phase: 01-ingestion-foundation
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/parse/docx.py
  - src/parse/section_splitter.py
  - tests/ingest/test_docx_parse.py
autonomous: true
requirements: [INGEST-02, INGEST-05]
must_haves:
  truths:
    - "A DOCX parses into the IDENTICAL document dict shape extract_pdf emits (same keys), with geometry nulled"
    - "The committed mini_spec.docx reconstructs its 5-row/3-col impurities table so its 3 planted deficiencies are findable"
    - "A merged cell (gridSpan + vMerge) reports one origin per spanned coordinate via ExtractedTable.merged_origins"
    - "A borderless table enumerates; a nested table is reachable; a complex/unreadable merge emits a typed ParseFailed and marks parsed_partial reason — never a crash, never a corrupt table"
    - "section_splitter.split_document consumes the DOCX dict unchanged (sorts by reading_order when page is None)"
  artifacts:
    - path: "src/parse/docx.py"
      provides: "extract_docx(path)->dict; iter_block_items; _tc-identity merged_origins; typed ParseFailed on complex merge"
      exports: ["extract_docx"]
    - path: "src/parse/section_splitter.py"
      provides: "sort-key guard tolerating page=None (DOCX)"
  key_links:
    - from: "src/parse/docx.py"
      to: "parse.pdf extract_pdf dict shape"
      via: "returns {filename,page_count,toc,pages:[{...,blocks,tables,figures}]}"
      pattern: "\"pages\""
    - from: "src/parse/section_splitter.py"
      to: "block page/reading_order sort"
      via: "guard for page is None"
      pattern: "reading_order"
---

<objective>
Add the DOCX parse path that converges byte-for-byte on `extract_pdf`'s document dict (INGEST-02), including merged-cell `_tc`-identity origin mapping (INGEST-05, D-31) and a one-line `section_splitter` guard so the DOCX dict (geometry nulled per D-20) flows through the existing splitter unchanged.

Purpose: SC2 — "a DOCX parses into the IDENTICAL structured document model used for PDFs, and the same parse-fidelity suite passes on both paths, emitting a typed ParseFailed rather than a corrupt table." This fills the `evals/run.py:174` DOCX seam (wired live in Plan 09).
Output: `src/parse/docx.py` (sibling to `src/parse/pdf.py` — chosen for symmetry with the exact analog it converges on), a guarded `section_splitter.py`.

**Naming decision (resolved):** the DOCX parser lives at `src/parse/docx.py`, NOT `src/ingest/docx_parse.py`. Rationale: it is a format parser whose sole job is to emit the same dict as its sibling `src/parse/pdf.py`; co-locating format parsers keeps the analog adjacent. The `src/ingest/` package orchestrates and dispatches to both `parse.pdf.extract_pdf` and `parse.docx.extract_docx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ingestion-foundation/01-RESEARCH.md
@.planning/phases/01-ingestion-foundation/01-PATTERNS.md
@src/parse/pdf.py
@src/schemas/documents.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DOCX parser converging on extract_pdf's dict + _tc merged-origin mapping</name>
  <files>src/parse/docx.py, tests/ingest/test_docx_parse.py</files>
  <behavior>
    - extract_docx("tests/ingest/fixtures/merged_cells.docx") returns a dict with the SAME top-level keys as extract_pdf: {filename, page_count, toc, pages}. page_count is None; each page's page_number/blocks[].bbox/blocks[].page are None (D-20 — never synthetic).
    - The merged_cells.docx table yields an ExtractedTable whose merged_origins maps every covered coord to its origin: "0,1"->[0,0] (horizontal) and "2,2"->[1,2] (vertical); origin coords are NOT keys.
    - extract_docx on the committed src/evals/dataset/docs/mini_spec.docx reconstructs the 5-row/3-col impurities table (headers ("Impurity","Result (%)","Limit"); rows include "Total impurities","Maximum") so its 3 planted deficiencies are present in the model.
    - A borderless table (no Table Grid style) still enumerates rows; a nested table (cell.tables) is reachable; a synthetic complex-merge that raises inside row.cells is caught → the table dict is replaced by a typed ParseFailed marker (not a crash).
  </behavior>
  <read_first>
    - src/parse/pdf.py:197-258 (`extract_pdf` — the EXACT return dict + per-page keys to match)
    - src/evals/make_docx_fixture.py:38-78 (mini_spec.docx content: header row + 5 data rows; the python-docx read/write API)
    - .planning/phases/01-ingestion-foundation/01-RESEARCH.md:249-289 (Pattern 2 iter_block_items body.iterchildren; Pattern 3 `_tc`-origin `cell_spans`)
    - .planning/phases/01-ingestion-foundation/01-RESEARCH.md:427-451 (verified extract_docx skeleton, geometry nulled)
    - .planning/phases/01-ingestion-foundation/01-RESEARCH.md:378-381 (Pitfall 5 — complex-merge IndexError → typed ParseFailed)
    - src/schemas/documents.py (ExtractedTable with new table_id/merged_origins; LayoutBlock geometry now optional — from Plan 02)
  </read_first>
  <action>
Create `src/parse/docx.py` using `python-docx` (`from docx import Document`, `from docx.oxml.ns import qn`). Mirror `extract_pdf`'s structure:

- `iter_block_items(document)` — walk `document.element.body.iterchildren()`, yielding `("paragraph", el)` for `w:p` and `("table", el)` for `w:tbl` in true reading order (RESEARCH:254-266). Do NOT use `.paragraphs`/`.tables` (loses interleaving — Pitfall / RESEARCH:318).
- For each paragraph with non-empty text → a `LayoutBlock(role="paragraph", text=..., bbox=None, page=None, reading_order=order, lines=[])` dict (`.model_dump()`); increment order.
- For each table → `_docx_table_to_extracted(tbl_element, order)`:
  - Wrap the python-docx `Table` object; build `coord_to_origin` via `_tc` identity (RESEARCH:278-289): `k=id(cell._tc)`; first appearance in row-major = origin. Populate `merged_origins` with ONLY covered (non-origin) coords → `[origin_r, origin_c]`.
  - `headers` = origin cells of row 0 (dedup covered), `rows` = origin cells of rows 1..n. Set `table_id=f"docx-t{order}"`, `kind="grid"`, `page=None`, `bbox=None`, `n_cols`/`n_rows`.
  - Wrap the whole reconstruction in try/except: on any exception (complex-merge IndexError, RESEARCH:378) append `{"_parse_failed": ParseFailed(layer="parse.docx", reason=f"table {order}: complex merge unreadable: {exc}", raw_output="").model_dump()}` instead of a corrupt table, so Plan 09 marks the doc `parsed_partial` (D-17). NEVER let it crash.
- `toc` = `_docx_toc(document)` (heading-styled paragraphs → `[{"level","title","page":None}]`; may be empty — flat docs are normal, D-28/D-30).
- Return `{"filename": Path(path).name, "page_count": None, "toc": toc, "pages": [{"page_number": None, "page_label": "", "width": None, "height": None, "rotation": 0, "source": "python-docx", "is_scanned": False, "blocks": [...], "tables": [...], "figures": []}]}`.
- **XXE hardening (T-01-05):** add a module comment + guard confirming python-docx/lxml does not resolve external entities for untrusted parts; if constructing any lxml parser directly, set `resolve_entities=False`/`no_network=True`. `limits.docx_zip_guard` (Plan 02) is invoked by the corpus BEFORE this parser (Plan 09).

Write `tests/ingest/test_docx_parse.py` with `test_merged_cells_resolve_to_origin`, `test_table_edge_cases` (borderless/nested/complex-merge→ParseFailed), and a mini_spec reconstruction assertion. Use the COMMITTED fixtures (endpoint-free), NOT skip_if_no_samples.
  </action>
  <acceptance_criteria>
    - `pytest tests/ingest/test_docx_parse.py -x -q` passes.
    - `pytest tests/ingest/test_docx_parse.py::test_merged_cells_resolve_to_origin -x` passes (INGEST-02/SC2).
    - `pytest tests/ingest/test_docx_parse.py::test_table_edge_cases -x` passes (borderless/nested/ParseFailed).
    - `python -c "from parse.docx import extract_docx; d=extract_docx('src/evals/dataset/docs/mini_spec.docx'); ks=set(d); assert ks=={'filename','page_count','toc','pages'}, ks; print('dict-shape OK')"` prints dict-shape OK.
    - `grep -q 'bbox=None\|\"bbox\": None\|bbox.*None' src/parse/docx.py` and `grep -q 'merged_origins' src/parse/docx.py` pass (geometry nulled; merge mapping present).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: section_splitter sort-key guard for page=None (DOCX)</name>
  <files>src/parse/section_splitter.py</files>
  <behavior>
    - split_document(extract_docx(mini_spec)) returns ≥1 section without raising (previously TypeError on None comparison in the sort key).
    - The existing PDF behavior is unchanged (page ints still sort first by page then reading_order).
  </behavior>
  <read_first>
    - src/parse/section_splitter.py:196-251 (`split_document` — the sort at :206 `key=lambda b: (b["page"], b["reading_order"])` breaks on page=None)
    - .planning/phases/01-ingestion-foundation/01-RESEARCH.md:448-451 (the one-line guard: with page=None sort by reading_order alone)
    - src/parse/docx.py (from Task 1 — the dict whose page is None)
  </read_first>
  <action>
In `src/parse/section_splitter.py`, change the sort key at line ~206 to tolerate `page is None`:
`body_blocks.sort(key=lambda b: (b["page"] if b["page"] is not None else -1, b["reading_order"]))`
Apply the same None-safe treatment anywhere else in this function that compares `b["page"]` / `page_number` as an int for ordering (check `_position` and `_kept_pages` usage). Add a one-line comment: `# DOCX blocks have page=None (D-20) — order by reading_order alone`. Make the MINIMAL edit; do not restructure the splitter.
  </action>
  <acceptance_criteria>
    - `python -c "from parse.docx import extract_docx; from parse.section_splitter import split_document; s=split_document(extract_docx('src/evals/dataset/docs/mini_spec.docx')); assert len(s)>=1; print('splitter OK', len(s))"` prints splitter OK.
    - `pytest tests/unit/test_section_splitter.py -x -q` still passes (no PDF regression).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| untrusted DOCX (a ZIP of XML) → python-docx/lxml | DOCX is a zip archive of XML; both the archive (bomb/zip-slip) and the XML (XXE) are attack surfaces. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | XXE / external entity in DOCX XML | mitigate | Confirm python-docx/lxml does not resolve external entities for untrusted parts; do not enable entity/network resolution. |
| T-01-01 | DoS | DOCX decompression bomb | transfer | Guarded upstream by `limits.docx_zip_guard` (Plan 02), invoked by the corpus before `extract_docx` (Plan 09). |
| T-01-12 | DoS / Tampering | complex/malformed merge crashing the parse | mitigate | try/except around table reconstruction → typed `ParseFailed` + `parsed_partial`, never a crash or a corrupt table (Pitfall 5, D-17). |
</threat_model>

<verification>
- `pytest tests/ingest/test_docx_parse.py tests/unit/test_section_splitter.py -x -q` green.
- DOCX dict has exactly the four extract_pdf top-level keys; geometry null; merged origins mapped.
</verification>

<success_criteria>
- DOCX → identical document dict; mini_spec table reconstructs; merged cells map to one origin; complex merge → typed ParseFailed.
- split_document consumes the DOCX dict unchanged with no PDF regression.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-foundation/01-04-SUMMARY.md`.
</output>
