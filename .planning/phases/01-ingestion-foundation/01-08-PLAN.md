---
phase: 01-ingestion-foundation
plan: 08
type: execute
wave: 5
depends_on: [02, 05, 06]
files_modified:
  - src/ingest/classify.py
  - tests/ingest/test_classify.py
autonomous: true
requirements: [INGEST-01]
must_haves:
  truths:
    - "Each document is classified by CONTENT into {free-form label, optional CTD-family guess, raw continuous confidence} — never by folder/filename"
    - "A cheap deterministic first pass (regex → lexicon → first-N-lines title block) classifies most docs LLM-free; only low-confidence / non-CTD docs escalate to a cheap-model LLM"
    - "Classification records its resolving TIER (regex/lexicon/llm) and the span-ID of the text that triggered it — not just a score"
    - "No hard confidence threshold is applied; the raw score is recorded and routed on, cutoff deferred to the eval harness"
    - "The per-tier escalation rate is measured and reported for the run; the LLM tier degrades to deterministic-only when no Databricks creds are configured"
    - "Decisions honored: D-01 (emit free-form label + optional CTD guess + raw confidence), D-02 (non-CTD/low-confidence docs first-class, always reviewed; CTD family is a routing hint not a gate), D-03 (no hard confidence threshold in Phase 1), D-07 (deterministic-first, LLM escalation only on low confidence/non-CTD), D-08 (signals = heading/TOC text + data-driven body keyword lexicon), D-10 (no separate letterhead signal), D-27 (per-tier escalation rate measured/reported), D-28 (first-N-lines title block as a deterministic signal, no TOC/heading assumed), D-29 (record resolving tier + triggering span-ID, not just a score)"
  artifacts:
    - path: "src/ingest/classify.py"
      provides: "classify_document(...)->DocClassification; deterministic tiers + structured LLM escalation; escalation-rate accumulator"
      exports: ["classify_document", "EscalationStats"]
  key_links:
    - from: "src/ingest/classify.py"
      to: "llm.structured.structured_call"
      via: "LLM escalation returns instance XOR ParseFailed"
      pattern: "structured_call"
    - from: "src/ingest/classify.py"
      to: "ingest.registry.families_catalog_text"
      via: "registry vocabulary feeds both deterministic lexicon and the LLM prompt"
      pattern: "registry"
---

<objective>
Build the content classifier (INGEST-01, D-01/02/03/04/07/08/09/10/27/28/29): deterministic-first (regex → body lexicon → first-N-lines title block) with cheap-model LLM escalation via the hardened `structured.py`, emitting `{label, family_guess, confidence, tier, triggering_span}` and a measured per-tier escalation rate.

Purpose: SC1 — every document is classified by content, never folder name (D-09 excludes path by construction). Recall-first (D-02): non-CTD / low-confidence docs are first-class and always reviewed; CTD family is a routing hint, never a gate. D-27's measured escalation rate is the guard that keeps the deterministic layer from silently degrading into per-document LLM classification at full cost.
Output: `src/ingest/classify.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ingestion-foundation/01-CONTEXT.md
@.planning/phases/01-ingestion-foundation/01-PATTERNS.md
@src/llm/structured.py
@src/schemas/documents.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic tiers — regex + body lexicon + first-N-lines title block</name>
  <files>src/ingest/classify.py, tests/ingest/test_classify.py</files>
  <behavior>
    - A doc whose canonical text contains a literal CTD number ("3.2.S.4.1") resolves at tier="regex", family_guess="3.2.S.4.1", high confidence, triggering_span pointing at the matched text.
    - A doc with no literal CTD number but strong lexicon hits (e.g. "specification","acceptance criteria","limit") resolves at tier="lexicon" with the best-matching family and a similarity-style confidence.
    - A flat doc whose only signal is its first-N-lines title block ("Drug Substance Specification, Product X, SPEC-001") is classified from that leading text (D-28), independent of heading markup; triggering_span points into the title block.
    - The classifier NEVER receives the filename/path (D-09) — its signature takes canonical text + outline + span index only.
    - No threshold is applied: a low score is recorded and returned, not dropped (D-03).
  </behavior>
  <read_first>
    - src/agents/detection/ctd.py:12-31 (the regex first-match seam extended to document level, now reading registry ids)
    - src/agents/detection/pipeline.py:29-34 (`_leading_text(doc, pages=3)` — the first-N-lines signal to mirror for D-28)
    - src/agents/detection/catalog.py:11-27,62-63 (CANONICAL_DOMAINS + domain_catalog_text — lexicon + prompt-feed pattern)
    - .planning/phases/01-ingestion-foundation/01-PATTERNS.md:170-193 (classify pattern assignment — signals + tier/trigger + escalation-rate)
    - .planning/phases/01-ingestion-foundation/01-CONTEXT.md D-01,D-02,D-03,D-04,D-07,D-08,D-09,D-28,D-29 (locked classification contract; D-04 = classify at the DOCUMENT level, do NOT per-classify sections)
    - src/ingest/registry/__init__.py (load_lexicon/family_ids/families_catalog_text — Plan 05); src/ingest/anchors.py (mint_span for triggering_span — Plan 06)
  </read_first>
  <action>
Create `src/ingest/classify.py`. Deterministic-first, three signals (D-07/D-08/D-28), NO path/filename input (D-09):
- `def _regex_tier(canonical, span_index) -> DocClassification | None`: reuse `ctd.detect_family` (Plan 05) over the canonical text; a literal-CTD match → `DocClassification(label=<registry label>, family_guess=<id>, confidence≈0.99, tier="regex", triggering_span=<span over the match>)`. Mint the triggering span via `anchors.mint_span` at the match offsets. Regex hit is effectively confidence 1.0 (D-29 — tiers emit confidence on incomparable scales; provenance makes that explicit).
- `def _lexicon_tier(canonical, outline) -> DocClassification | None`: score each family by body-lexicon keyword hits (from `registry.load_lexicon()`); best family with ≥1 hit → `tier="lexicon"`, `confidence` = a normalized hit ratio, `triggering_span` at the strongest keyword occurrence.
- `def _title_block_tier(canonical, span_index) -> DocClassification | None`: take the first ~15 non-empty lines OR ~1000 chars of canonical (D-28); run the regex + lexicon over just that leading block; this is the primary signal for ordinary CMC docs lacking heading markup. triggering_span points into the title block.
- `def classify_document(nt, outline, doc_id, model=None) -> DocClassification`: run regex → title-block → lexicon; take the first confident hit. If none is confident OR the doc is non-CTD, mark it for escalation (Task 2). Record the resolving `tier` and `triggering_span` on the result (D-29). A non-CTD doc still gets a free-form `label` and is returned (D-01/D-02) — never dropped, no threshold (D-03).

Write `tests/ingest/test_classify.py` covering the regex/lexicon/title-block behaviors above using conftest `make_doc_dict` → `serialize`→`normalize` (build a NormalizedText), asserting tier + family_guess + a non-null triggering_span, and asserting the function signature has NO path/filename parameter.
  </action>
  <acceptance_criteria>
    - `pytest tests/ingest/test_classify.py -x -q` passes (deterministic tiers).
    - `python -c "import inspect; from ingest.classify import classify_document; ps=set(inspect.signature(classify_document).parameters); assert not (ps & {'path','filename','folder'}), ps; print('no-path OK')"` prints no-path OK (D-09 by construction).
    - `grep -q 'tier' src/ingest/classify.py` and `grep -q 'triggering_span' src/ingest/classify.py` pass (D-29).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LLM escalation via structured.py + measured per-tier escalation rate</name>
  <files>src/ingest/classify.py, tests/ingest/test_classify.py</files>
  <behavior>
    - When deterministic tiers are not confident (or the doc is non-CTD), classify_document escalates via structured_call with a ClassifyResult model, returning tier="llm" and the model's label/family/self-assessed confidence.
    - structured_call returns instance XOR ParseFailed; on ParseFailed the classifier falls back to the best deterministic guess (never a raw text leak, never a crash).
    - With no Databricks creds configured, the LLM tier is skipped and the doc stays at its deterministic tier (mirror ocr.py:78 no-creds skip) — the run still completes.
    - An EscalationStats accumulator records counts per tier and computes escalation_rate = fraction resolved by each tier (D-27); it is exposed for the run summary.
  </behavior>
  <read_first>
    - src/llm/structured.py:282-316 (`structured_call(messages, model_cls, model=...) -> (instance, ParseFailed)` — reuse VERBATIM, do NOT reinvent)
    - src/parse/ocr.py:71-79 (no-creds skip pattern to mirror for the LLM tier)
    - src/config.py:6-16,54 (Settings + is_databricks — how to detect no-creds)
    - .planning/phases/01-ingestion-foundation/01-CONTEXT.md D-07,D-10,D-27,D-29 (escalation trigger + measured rate + tier provenance)
  </read_first>
  <action>
Extend `src/ingest/classify.py`:
- `class ClassifyResult(BaseModel)`: `label: str`, `family: str`, `confidence: float` — the LLM's structured output contract.
- `def _llm_tier(canonical_head, families_catalog_text, model=None) -> DocClassification | None`: build messages feeding `registry.families_catalog_text()` + the doc's leading text; call `structured_call(messages, ClassifyResult, model=model)`. On `instance` → `DocClassification(label, family_guess=family, confidence, tier="llm", triggering_span=<span over the head>)`. On `ParseFailed` → return None (caller keeps the deterministic guess). Guard with a no-creds check (Settings.is_databricks / empty host+token) → skip the call entirely and return None (ocr.py:78 pattern). The LLM self-assessed confidence is on its own scale — tier="llm" provenance makes that explicit (D-29).
- `class EscalationStats`: increment per resolving tier; `escalation_rate() -> dict[str,float]` (regex/lexicon/llm fractions). `classify_document` accepts an optional stats accumulator and records the resolving tier (D-27). Log the rate via `structlog` at run end (Plan 09 wires the run summary).

Extend `tests/ingest/test_classify.py`: with the `offline` fixture (no creds), a non-CTD doc does NOT call the LLM and stays deterministic; with a monkeypatched `structured_call` returning a ClassifyResult, escalation yields tier="llm"; with a monkeypatched `structured_call` returning ParseFailed, the classifier falls back without raising; EscalationStats fractions sum to ~1.0 over a small batch.
  </action>
  <acceptance_criteria>
    - `pytest tests/ingest/test_classify.py -x -q` passes (deterministic + escalation + no-creds + ParseFailed fallback).
    - A test asserts that under the `offline` fixture the LLM path is NOT invoked (monkeypatch structured_call to raise if called; assert not called).
    - `grep -q 'structured_call' src/ingest/classify.py` and `grep -q 'escalation_rate' src/ingest/classify.py` pass (D-07/D-27).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| document content → classifier | Only extracted CONTENT (canonical text + outline) crosses; the filename/path never does (D-09), so a hostile folder name cannot flip a document's class. |
| classifier → cheap LLM endpoint | Escalation sends leading text to a cheap model; the hardened `structured.py` bounds malformed output; no creds → no call. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | filename/path as classification attack vector | mitigate | `classify_document` takes no path/filename parameter (D-09, enforced by signature + proven by the SC1 rename-folders test in Plan 09). |
| T-01-15 | DoS / cost | deterministic layer silently degrading → every doc hits the LLM | mitigate | D-27 measured per-tier escalation rate surfaced in the run summary distinguishes "10% escalating" from "80% escalating" (per-doc LLM through the back door). |
| T-01-16 | Tampering | malformed LLM tool-args on weak local models | mitigate | `structured_call` L1–L6 (strict decode → repair → json_repair → moderator → typed ParseFailed); classifier falls back to deterministic guess, never a raw leak. |
</threat_model>

<verification>
- `pytest tests/ingest/test_classify.py -x -q` green.
- classify_document takes no path/filename; escalation rate measured; no-creds degrades cleanly.
</verification>

<success_criteria>
- Content classification emits {label, family_guess, confidence, tier, triggering_span}; deterministic-first, LLM-escalated; no threshold; path excluded.
- Per-tier escalation rate measured and reportable; LLM tier degrades to deterministic-only without creds.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ingestion-foundation/01-08-SUMMARY.md`.
</output>
