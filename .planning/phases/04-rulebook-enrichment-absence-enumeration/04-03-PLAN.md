---
phase: 04-rulebook-enrichment-absence-enumeration
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/rulebook/absence.py
  - src/evals/run.py
  - src/evals/baseline/absence_threshold.json
  - tests/rulebook/test_absence.py
  - tests/evals/test_generality_guard.py
autonomous: true
requirements: [RECALL-01]

must_haves:
  truths:
    - "A deterministic pre-loop pure pass (NOT a drive-loop tool) walks enumerate_requirements(manifest), queries the ephemeral submission index per applicable requirement, and emits an absence candidate when the top-hit score is below the recorded threshold (D-ABS1, D-ABS3)."
    - "It over-emits — every plausibly-unaddressed applicable requirement becomes a candidate; no precision cutoff is baked into the recall layer (D-ABS2)."
    - "A required family with zero classified documents fires whole-section absence, profile-gated so a non-required family never fires (D-SEC1, D-SEC2)."
    - "Every candidate emits through emit_absence_finding, so the rule half is byte-exact and the CoverageAbsenceAnchor is re-derivable (D-GATE1, D-GATE2)."
    - "The retrieval threshold is tuned on mvr1381, recorded, ratcheted — never a baked constant — and the frozen mvr1381 value recovers heldout32s41's absences unchanged (D-THR, D-GEN2(4))."
    - "absence_of_evidence is recovered above the 0.000 floor on the Phase-0 eval set (SC2, the #1 recall gap)."
    - "A CI guard test FAILS the build if the absence module embeds a corpus constant or a folder rename changes applicability (D-GEN1, D-GEN2, D-GEN3)."
  artifacts:
    - path: "src/rulebook/absence.py"
      provides: "enumerate_requirements ∘ search_corpus ∘ emit_absence_finding deterministic pass"
      contains: "def enumerate_absences"
      min_lines: 60
    - path: "src/evals/baseline/absence_threshold.json"
      provides: "mvr1381-tuned recorded threshold + per-document recall"
    - path: "src/evals/run.py"
      provides: "absence-gate subcommand (recover absence>0.000, ratchet threshold)"
      contains: "absence-gate"
    - path: "tests/rulebook/test_absence.py"
      provides: "off-eval whole-section composition tests (omit-required fires, omit-non-required silent)"
    - path: "tests/evals/test_generality_guard.py"
      provides: "D-GEN2 invariants: no-constant, rename-invariance, same-logic transfer, threshold transfer"
  key_links:
    - from: "src/rulebook/absence.py::enumerate_absences"
      to: "rulebook.requirement_index.enumerate_requirements"
      via: "applicability (never reinvented)"
      pattern: "enumerate_requirements"
    - from: "src/rulebook/absence.py::enumerate_absences"
      to: "tools.search_corpus.search_corpus"
      via: "retrieval-threshold query per requirement trigger"
      pattern: "search_corpus"
    - from: "src/rulebook/absence.py::enumerate_absences"
      to: "tools.emit_finding.emit_absence_finding"
      via: "grounded candidate emission"
      pattern: "emit_absence_finding"
    - from: "src/evals/run.py absence-gate"
      to: "src/evals/baseline/absence_threshold.json"
      via: "measure -> record -> ratchet; recover absence>0.000"
      pattern: "absence_threshold"
---

<objective>
Build src/rulebook/absence.py — the deterministic pre-loop pure pass that is the structural
answer to three consecutive Phase-3 drive-loop NO-GOs (recall is no longer the loop's job,
D-ABS3). It composes three verified analogs: enumerate_requirements (applicability) ∘
search_corpus (retrieval-threshold query) ∘ emit_absence_finding (grounding). Then recover
absence_of_evidence above the 0.000 floor (SC2), record the mvr1381-tuned threshold, and lock a
CI generality guard (SC3). This is the heart of RECALL-01.

Purpose: Close the #1 recall gap — absence_of_evidence = 0.000. Every eval absence is
requirement-level "claim/requirement present, data absent" (mvr1381 ×11, MS-03), so a
retrieval-threshold over-emit per applicable requirement is the mechanism (D-ABS1). Over-emit;
Phase 7's verifier prunes (D-ABS2).

Output: absence.py; the tuned+recorded absence_threshold baseline; an absence-gate CLI
subcommand; off-eval whole-section composition tests; and the D-GEN3 anti-overfitting CI guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-rulebook-enrichment-absence-enumeration/04-CONTEXT.md
@.planning/phases/04-rulebook-enrichment-absence-enumeration/04-PATTERNS.md
@.planning/phases/04-rulebook-enrichment-absence-enumeration/04-01-SUMMARY.md
@.planning/phases/04-rulebook-enrichment-absence-enumeration/04-02-SUMMARY.md

<interfaces>
<!-- Verified from disk. absence.py = enumerate_requirements ∘ search_corpus ∘ emit_absence_finding. -->

From src/rulebook/requirement_index.py (applicability — CALL verbatim, do NOT reinvent):
```python
def enumerate_requirements(manifest: CoverageManifest, family: str | None = None) -> list[RequirementEntry] | ToolRejected
def submission_profile(manifest: CoverageManifest) -> set[str]   # content-derived (never folder-driven)
class RequirementEntry(BaseModel):
    id: str; family: str; citation: str
    trigger: str                 # THE retrieval query text (rulebook text, never a corpus literal)
    provenance_span_id: SpanID   # THE rule half of grounding — loader-gate-guaranteed to re-open
# A ToolRejected return means family_not_in_registry — propagate, never raise.
```

From src/tools/search_corpus.py (retrieval — CALL verbatim):
```python
def search_corpus(corpus: CorpusIndex, query: str, ledger: RetrievalLedger, top_k: int = 10) -> list[dict]
# returns [{"doc_id","span_id": <span.model_dump()>,"score": <float>,"snippet"}, ...] sorted best-first;
# records every returned span in the ledger (so emit_absence_finding's was_issued passes on the claim span).
```

From src/tools/emit_finding.py (Plan 02 — the grounding gate to emit through):
```python
def emit_absence_finding(corpus, rule_span_id, absence_anchor: CoverageAbsenceAnchor, ledger,
                         requirement_id="", rule_citation="", title="", detail="",
                         rulebook_cache_dir=DEFAULT_RULEBOOK_CACHE_DIR) -> Fault | ToolRejected
```

From src/schemas/faults.py (Plan 02):
```python
class RetrievalHit(BaseModel): span_id: SpanID; score: float
class CoverageAbsenceAnchor(BaseModel):
    profile: list[str]; family: str; requirement_id: str; threshold: float
    sub_threshold_hits: list[RetrievalHit]; manifest_span_ids: list[SpanID]; claim_span_id: SpanID | None
```

Module imports to mirror (search_corpus.py + requirement_index.py; src/ is import-rooted, NO `src.` prefix):
```python
from ingest.corpus import CorpusIndex
from ingest.manifest import CoverageManifest
from rulebook.requirement_index import enumerate_requirements, submission_profile, RequirementEntry, load_requirement_index
from rulebook import edges
from rulebook.store import DEFAULT_RULEBOOK_CACHE_DIR
from tools.search_corpus import search_corpus
from tools.emit_finding import emit_absence_finding
from tools.ledger import RetrievalLedger
from schemas.faults import CoverageAbsenceAnchor, RetrievalHit, Fault
from schemas.documents import SpanID
```
<!-- W2: `Fault` (enumerate_absences return type list[Fault]) confirmed in schemas.faults (faults.py:56);
     `DEFAULT_RULEBOOK_CACHE_DIR` (default arg) confirmed in rulebook.store (store.py:15). Verified on disk. -->

From src/evals/run.py::cmd_retrieval_gate (measure->record->ratchet analog, lines 192-240;
subcommand registration in build_parser lines 433-484; lazy-import discipline).

Datasets (verified): mvr1381 = TUNING (11 absence, held_out=False); heldout32s41 = GENERALITY WITNESS
(2 absence, held_out=True — NEVER tune on this); minispec/MS-03 (1 absence, docx, held_out=False). rulebook rule for
"rule half re-open" is that RequirementEntry.provenance_span_id is loader-gate-guaranteed to resolve in
the rulebook store, but the absence pass must still record it in the ledger before emit (see Task 1).

Whole-section (D-SEC1): a family in enumerate_requirements' applicable set (because a
profile_requires_family edge unions it in) that has ZERO classified documents in the manifest fires
"entire section absent"; profile-gate it (D-SEC2) — only for a family a content-exhibited profile requires.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build src/rulebook/absence.py — the deterministic absence pass</name>
  <files>src/rulebook/absence.py</files>
  <read_first>
    - src/rulebook/requirement_index.py (enumerate_requirements, submission_profile, RequirementEntry, load_requirement_index, the profile_requires_family edge union lines 180-200)
    - src/tools/search_corpus.py (search_corpus signature + return dict shape + ledger.record_span)
    - src/tools/emit_finding.py (emit_absence_finding from Plan 02)
    - src/schemas/faults.py (CoverageAbsenceAnchor, RetrievalHit, Fault from Plan 02 — `Fault` at faults.py:56 is the enumerate_absences return element type)
    - src/rulebook/store.py (DEFAULT_RULEBOOK_CACHE_DIR at store.py:15 — the default rulebook_cache_dir arg)
    - src/rulebook/edges.py (get_edges — for the whole-section zero-doc check)
    - src/ingest/manifest.py (CoverageManifest, DocEntry — classification.family_guess, doc span-IDs)
    - src/tools/ledger.py (RetrievalLedger.record_span / was_issued)
    - 04-CONTEXT.md D-ABS1/2/3/4, D-SEC1/2, D-GATE1/2, D-GEN2 no-constant
    - 04-PATTERNS.md (the absence.py section — analogs A/B/C, the composition)
  </read_first>
  <behavior>
    - enumerate_absences over a manifest whose submission never addresses an applicable requirement emits ≥1 absence Fault with verdict==GAP and a re-derivable CoverageAbsenceAnchor.
    - A requirement whose trigger retrieves a strong (above-threshold) hit is NOT emitted (the search-and-found path).
    - A required family with zero classified documents fires whole-section absence; a non-required family never fires (D-SEC1/D-SEC2).
    - The module references no corpus/doc/submission-ID literal (D-GEN2 no-constant).
  </behavior>
  <action>
    Create src/rulebook/absence.py as a PURE FUNCTION over (coverage_manifest, corpus_index, threshold) —
    NOT a drive-loop tool, NOT gated on the loop (D-ABS3). Signature:
    ```python
    def enumerate_absences(corpus: CorpusIndex, manifest: CoverageManifest, ledger: RetrievalLedger,
                           threshold: float, rulebook_cache_dir: str = DEFAULT_RULEBOOK_CACHE_DIR,
                           top_k: int = 10) -> list[Fault]:
    ```
    Logic:
    1. `applicable = enumerate_requirements(manifest)`; if it returns ToolRejected, return [] (or propagate —
       do not raise). `profiles = sorted(submission_profile(manifest))`.
    2. Compute `families_present = {d.classification.family_guess for d in manifest.documents if d.classification and d.classification.family_guess}`.
    3. REQUIREMENT-LEVEL (D-ABS1): for each RequirementEntry `e` in `applicable`:
       - `hits = search_corpus(corpus, e.trigger, ledger, top_k=top_k)` — the query is `e.trigger` (rulebook
         text), NEVER a corpus literal (D-GEN2).
       - top-hit score = `hits[0]["score"]` if hits else 0.0. If top score < threshold → OVER-EMIT a candidate
         (D-ABS2, no precision cutoff):
         - Build `CoverageAbsenceAnchor(profile=profiles, family=e.family, requirement_id=e.id,
           threshold=threshold, sub_threshold_hits=[RetrievalHit(span_id=SpanID(**h["span_id"]), score=h["score"]) for h in hits],
           manifest_span_ids=[...doc span-IDs proving the search space...], claim_span_id=None)`.
         - (D-ABS4) If a near-miss hit looks like an unsupported narrative claim, you MAY set claim_span_id to
           its ledger-issued SpanID (already recorded by search_corpus); the emit gate re-opens it byte-exact.
         - Record `e.provenance_span_id` in the ledger (`ledger.record_span(e.provenance_span_id)`) so the emit
           gate's was_issued passes on the rule half (the loader gate already guarantees it re-opens byte-exact).
         - `fault = emit_absence_finding(corpus, e.provenance_span_id, anchor, ledger, requirement_id=e.id,
           rule_citation=e.citation, title=f"Required item not addressed: {e.citation}", rulebook_cache_dir=...)`.
         - Append `fault` when it is a Fault (skip a ToolRejected — telemetry it, never raise).
    4. WHOLE-SECTION (D-SEC1/D-SEC2): for each profile in `profiles`, for each `(_, dst_family, _, _)` in
       `edges.get_edges(src_id=profile, edge_type="profile_requires_family")`: if `dst_family not in
       families_present` (zero classified documents) → emit a whole-section absence candidate for each
       requirement in that family (reuse the requirement-level emit with an anchor whose family=dst_family,
       manifest_span_ids=[] and a note that the family has zero documents). Fire ONLY for a family a
       content-exhibited profile requires (profile-gate) — the loop over `profiles` already enforces this.
    Absolutely NO submission/corpus/doc-ID string literal anywhere in the module (D-GEN2 no-constant): the
    query text is always `e.trigger`, applicability always flows from the manifest.
  </action>
  <verify>
    <automated>pytest tests/rulebook/test_absence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def enumerate_absences" src/rulebook/absence.py` returns 1.
    - `grep -c "enumerate_requirements\|search_corpus\|emit_absence_finding" src/rulebook/absence.py` returns ≥3 (composes all three analogs).
    - The return-type + default-arg imports resolve: `grep -c "from schemas.faults import.*Fault" src/rulebook/absence.py` returns ≥1 AND `grep -c "from rulebook.store import DEFAULT_RULEBOOK_CACHE_DIR" src/rulebook/absence.py` returns 1 (W2 — the enumerate_absences signature's `list[Fault]` return and `rulebook_cache_dir=DEFAULT_RULEBOOK_CACHE_DIR` default are both imported).
    - No corpus constant: `grep -iE "mvr1381|spec32s41|heldout32s41|minispec" src/rulebook/absence.py` returns nothing (D-GEN2).
    - No drive-loop coupling: `grep -iE "\bloop\b|drive_loop|run_review|chat_completion" src/rulebook/absence.py` returns nothing (D-ABS3 pure pass).
    - `pytest tests/rulebook/test_absence.py -x -q` exits 0 (behaviors proven in Task 2's tests).
  </acceptance_criteria>
  <done>enumerate_absences is a pure composition of enumerate_requirements ∘ search_corpus ∘ emit_absence_finding, over-emits sub-threshold requirements + zero-doc families, emits through the byte-exact gate, and embeds no corpus constant.</done>
</task>

<task type="auto">
  <name>Task 2: Tune + record the threshold, wire absence-gate CLI (recover absence>0.000), write composition + generality tests</name>
  <files>src/evals/run.py, src/evals/baseline/absence_threshold.json, tests/rulebook/test_absence.py, tests/evals/test_generality_guard.py</files>
  <read_first>
    - src/evals/run.py (cmd_retrieval_gate lines 192-240; build_parser lines 433-484 — the `subparsers.add_parser("<name>", ...)` + `.set_defaults(func=cmd_...)` registration idiom, and the EXISTING coverage-gate registration Plan 04-01 appended here; lazy-import lines 24-29/203-204)
    - src/evals/baseline/retrieval_recall.json (baseline JSON shape)
    - src/evals/schema.py (ABSENCE_OF_EVIDENCE line 28, tp_required, load_eval_set line 89, held_out field line 71)
    - src/rulebook/absence.py (Task 1)
    - tests/rulebook/test_requirement_index_integration.py (the self-contained-store real-ingest-then-assert fixture pattern, lines 20-70)
    - src/tools/emit_finding.py docstring + build.py (cache_dir/db_path offline-isolation params, D-RB6)
    - 04-CONTEXT.md D-THR, D-GEN1 (heldout32s41 witness), D-GEN2 (4 invariants incl. threshold transfer), D-GEN3 (CI test that FAILS the build)
  </read_first>
  <action>
    Three deliverables:

    (A) THRESHOLD BASELINE (D-THR) + ABSENCE-GATE. Tune the retrieval threshold on **mvr1381 only** (the
    tuning corpus): sweep candidate thresholds, pick the recall-biased value that recovers mvr1381's 11
    absence_of_evidence items via `enumerate_absences` while over-emitting (favor recall — D-GEN2(4) argues
    for a generous bar, not one finely fitted to mvr1381). Write `src/evals/baseline/absence_threshold.json`
    copying retrieval_recall.json's shape: `{"generated_from":"live:<date>","threshold":<value>,
    "per_document":{"mvr1381":{"absence_recall":<val>,"emitted":<n>},"minispec":{"absence_recall":<val>,"emitted":<n>},
    "non_held_out_aggregate":{"absence_recall":<val>}}}`. NEVER bake the threshold as a code constant — it is
    read from this JSON.

    (B) absence-gate CLI. Add `def cmd_absence_gate(args)` to run.py (lazy-import absence + ingest inside the
    body). Iterate the eval set and process ONLY non-held-out documents — mirror the existing
    `for doc in eval_set: if doc.held_out: continue` idiom (run.py line 211/263), so it processes the
    non-held-out ABSENCE sources **mvr1381 (11) + minispec (1)** and SKIPS held_out=True (heldout32s41/spec32s41)
    — heldout32s41 is the D-GEN1 generality witness, NEVER a recall floor. For each non-held-out eval document:
    ingest, run `enumerate_absences(corpus, manifest, ledger, threshold=<from baseline>)`, score
    absence_of_evidence recall against the eval set. GATE:
    (i) HARD (SC2, W3): the measured absence_of_evidence recall on the **non-held-out EVAL-SET AGGREGATE
    (mvr1381 + minispec, i.e. every doc with held_out=False)** must be **> 0.000** — accumulate matched vs
    required absence items across all non-held-out docs and assert the aggregate recall exceeds 0.000. Print
    "ABSENCE-GATE FAILED: non-held-out absence recall 0.000 (mvr1381+minispec), the #1 gap not recovered" +
    return 1 if the aggregate is 0.000. This matches ROADMAP SC2's "above the 0.000 floor on the Phase-0 eval
    set" (the eval set = the non-held-out scored split, NOT mvr1381 alone). Keep heldout32s41 excluded from this
    floor (D-GEN1 anti-circularity — it is the generality witness, proven separately in test C's THRESHOLD-TRANSFER
    invariant, never the recall floor);
    (ii) no-regress floor vs the committed baseline threshold's recorded `non_held_out_aggregate.absence_recall`.
    Register `absence-gate` in build_parser using the same `subparsers.add_parser("absence-gate", ...)` +
    `.set_defaults(func=cmd_absence_gate)` idiom, APPENDING it after the existing `retrieval-gate` and
    `coverage-gate` registrations — do NOT rewrite, reorder, or remove any existing `add_parser(...)` block
    (Plan 04-01 registered `coverage-gate` in this same build_parser; it MUST remain). Add
    `--baseline` default `src/evals/baseline/absence_threshold.json`. LLM-free, offline (D-RB6).

    (C) TESTS.
    - `tests/rulebook/test_absence.py` (behaviors for Task 1 + D-SEC1 off-eval composition, modeled on
      test_requirement_index_integration.py's self-contained-store fixture with tmp_path-scoped cache_dir/db_path):
      * a submission that addresses a requirement (strong retrieval hit) → NOT emitted;
      * a submission that never addresses an applicable requirement → emitted (verdict GAP, re-derivable anchor);
      * D-SEC1 whole-section: a constructed corpus OMITTING a **required** family → whole-section absence FIRES;
        one omitting a **non-required** family → nothing fires (the class the main eval can't measure).
    - `tests/evals/test_generality_guard.py` — the D-GEN3 CI guard (this test FAILS the build on violation):
      * NO-CONSTANT: assert `src/rulebook/absence.py` source contains no dataset literal (read the file,
        assert none of mvr1381/spec32s41/heldout32s41/minispec appears);
      * RENAME-INVARIANCE: ingest heldout32s41, capture applicable-requirement set + absence-candidate IDs;
        copy its corpus dir to a renamed/reorganized path, re-ingest, assert the IDENTICAL applicable set +
        identical absence-candidate IDs (content-derived, not folder-derived);
      * SAME-LOGIC TRANSFER: assert heldout32s41's absences arise from the SAME index entries firing (compare
        the requirement_ids on the anchors), not corpus-specific rules;
      * THRESHOLD TRANSFER (D-GEN2(4)): with the threshold FROZEN at its mvr1381-tuned value from
        absence_threshold.json, assert heldout32s41's absence candidates are recovered unchanged (proving the
        threshold is a general recall bar, not an mvr1381-specific constant).
    Isolate every rulebook store per test with tmp_path-scoped cache_dir/db_path (D-RB6); never touch
    Databricks or the shared data/ store.
  </action>
  <verify>
    <automated>python -m evals.run coverage-gate && python -m evals.run absence-gate && pytest tests/rulebook/test_absence.py tests/evals/test_generality_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/evals/baseline/absence_threshold.json` exists, is valid JSON, contains a numeric `threshold` and a `per_document.mvr1381` block plus a `per_document.minispec` block (`python -c "import json;d=json.load(open('src/evals/baseline/absence_threshold.json'));assert isinstance(d['threshold'],(int,float));assert 'mvr1381' in d['per_document'] and 'minispec' in d['per_document'];print('OK')"`).
    - `grep -c "absence-gate\|cmd_absence_gate" src/evals/run.py` returns ≥2.
    - W1 (both subcommands survive 04-03's build_parser edit): `python -c "import re,sys;s=open('src/evals/run.py').read();assert len(re.findall(r'add_parser\(\s*\"coverage-gate\"',s))>=1,'coverage-gate registration dropped';assert len(re.findall(r'add_parser\(\s*\"absence-gate\"',s))>=1,'absence-gate not registered';print('OK both registered')"` prints `OK both registered` — proving 04-03's edit APPENDED absence-gate without clobbering 04-01's coverage-gate registration. Equivalently `grep -c 'add_parser(.*"coverage-gate"' src/evals/run.py` ≥1 AND `grep -c 'add_parser(.*"absence-gate"' src/evals/run.py` ≥1.
    - W1 (both run): `python -m evals.run coverage-gate` exits 0 AND `python -m evals.run absence-gate` exits 0 — 04-01's coverage-gate still resolves its `func` after 04-03's edit.
    - W3 (SC2 aggregate floor): `python -m evals.run absence-gate` exits 0 AND its output shows the **non-held-out aggregate (mvr1381 + minispec)** absence recall strictly > 0.000 — the #1 gap recovered on the Phase-0 eval set (ROADMAP SC2), heldout32s41 excluded (D-GEN1).
    - `pytest tests/rulebook/test_absence.py tests/evals/test_generality_guard.py -x -q` exits 0 — the off-eval whole-section composition tests AND all four D-GEN2 invariants (no-constant, rename-invariance, same-logic transfer, threshold transfer) pass.
    - The threshold is NOT a code constant: `grep -nE "threshold\s*=\s*0\.[0-9]" src/rulebook/absence.py` returns nothing (it is read from the JSON baseline, D-THR).
  </acceptance_criteria>
  <done>The threshold is tuned on mvr1381, recorded + ratcheted; `python -m evals.run absence-gate` proves absence_of_evidence > 0.000 on the non-held-out aggregate (mvr1381+minispec, SC2) while 04-01's coverage-gate registration survives (W1); off-eval whole-section composition tests + the D-GEN3 four-invariant CI guard all pass (SC3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement trigger → retrieval query | the query text is rulebook-sourced; a corpus literal here would silently overfit |
| tuned threshold → committed baseline | a threshold fitted to mvr1381 would inflate the scored recall number and fail on held-out corpora |
| absence candidate → emit gate | every candidate must ground through emit_absence_finding (rule half byte-exact, anchor re-derivable) |
| 04-01 coverage-gate ↔ 04-03 absence-gate in one build_parser | two plans edit the same build_parser; a 04-03 edit that clobbers 04-01's coverage-gate registration is the project's signature boundary-crossing bug |

Offline, deterministic, in-process pure pass. No network endpoint, no untrusted runtime input, no auth
surface, no drive-loop coupling (D-ABS3) — web-app threats do not apply.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | absence module embedding a corpus constant (overfitting) | mitigate | D-GEN3 CI guard test (tests/evals/test_generality_guard.py) FAILS the build if absence.py contains any dataset literal or a folder rename changes applicability — enforced every run, never a one-time audit |
| T-04-09 | Tampering | threshold fitted to the scored corpus | mitigate | D-THR: threshold tuned on mvr1381, recorded in absence_threshold.json (not a code constant), and the THRESHOLD-TRANSFER invariant asserts the frozen value recovers heldout32s41 unchanged (D-GEN2(4)) |
| T-04-10 | Spoofing | ungrounded absence candidate | mitigate | every candidate emits through emit_absence_finding — rule half byte-exact + re-derivable CoverageAbsenceAnchor (D-GATE1/2); a ToolRejected is skipped, never emitted as a Fault |
| T-04-11 | Elevation of privilege | re-coupling recall to the drive loop | accept→prevent | D-ABS3 pure pass; the no-loop-coupling grep gate (Task 1 acceptance) prevents accidental loop dependency — low risk, structurally enforced |
| T-04-12 | Tampering | 04-03's build_parser edit silently drops 04-01's coverage-gate registration | mitigate | W1: Task 2 read_first names the exact build_parser range (run.py 433-484) both plans touch; the acceptance criterion greps that BOTH `add_parser("coverage-gate")` AND `add_parser("absence-gate")` are present (each ≥1) AND runs both subcommands to exit 0 — the criterion FAILS if 04-03's edit removes 04-01's registration |
</threat_model>

<verification>
- `python -m evals.run coverage-gate` exits 0 — 04-01's coverage-gate registration survives 04-03's build_parser edit (W1).
- `python -m evals.run absence-gate` exits 0 with the non-held-out aggregate (mvr1381+minispec) absence recall > 0.000 — the #1 recall gap recovered on the Phase-0 eval set (SC2, W3).
- `pytest tests/rulebook/test_absence.py tests/evals/test_generality_guard.py -x -q` exits 0 — whole-section composition + four generality invariants (SC3).
- `grep -iE "mvr1381|spec32s41|heldout32s41|minispec" src/rulebook/absence.py` returns nothing — no corpus constant (D-GEN2).
- `grep -iE "\bloop\b|run_review|chat_completion" src/rulebook/absence.py` returns nothing — pure pre-loop pass (D-ABS3).
</verification>

<success_criteria>
- enumerate_absences is a deterministic pre-loop pure pass composing enumerate_requirements ∘ search_corpus ∘ emit_absence_finding (D-ABS1/D-ABS3).
- Over-emits sub-threshold requirements + zero-doc required families (D-ABS2, D-SEC1/2).
- absence_of_evidence recovered > 0.000 on the non-held-out Phase-0 eval set (mvr1381 + minispec aggregate, SC2), heldout32s41 excluded (D-GEN1).
- Threshold tuned on mvr1381, recorded, ratcheted; frozen value transfers to heldout32s41 (D-THR, D-GEN2(4)).
- D-GEN3 CI guard fails the build on a corpus constant or a rename-sensitivity (SC3).
- Every candidate grounded + re-openable; whole-section composition tests pass (SC4).
- Both coverage-gate (04-01) and absence-gate (04-03) remain registered + runnable in the shared build_parser (W1).
</success_criteria>

<output>
After completion, create `.planning/phases/04-rulebook-enrichment-absence-enumeration/04-03-SUMMARY.md`
</output>
</output>
