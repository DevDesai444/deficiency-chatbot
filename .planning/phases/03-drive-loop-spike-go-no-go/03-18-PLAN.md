---
phase: 03-drive-loop-spike-go-no-go
plan: 18
type: execute
wave: 10
depends_on: ["03-17"]
files_modified:
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-1.json
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-2.json
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-3.json
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-1.jsonl
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-2.jsonl
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-3.jsonl
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-1-summary.json
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-2-summary.json
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-3-summary.json
autonomous: false
requirements: [AGENT-01, AGENT-03, AGENT-04, GROUND-01, GROUND-03, DETECT-03, DETECT-04]

must_haves:
  truths:
    - "The three scored runs execute the EXACT frozen command in §10 of the committed v3.3 pre-registration — no value is chosen by the executor (D-GO2(ii), D-GO3(i))"
    - "Each run commits its FaultReport, per-turn JSONL and provenanced summary, so the verdict is re-derivable by someone who did not watch the runs (D-TEL1(ii))"
    - "Every run summary carries prereg_commit_sha == 2f29413 (the governing v3.3 commit), proving it was scored under the frozen criteria (D-TEL1(i))"
    - "A failed or errored run is recorded as a FAILING run, not re-rolled (D-GO2(i))"
    - "No task here declares GO or NO-GO — the executor reports numbers only (D-GO5)"
  artifacts:
    - path: ".planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-1.json"
      provides: "Committed FaultReport, re-scorable with no LLM"
    - path: ".planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-1-summary.json"
      provides: "Provenanced RunSummary carrying prereg_commit_sha == 2f29413"
  key_links:
    - from: "each run summary"
      to: "03-GO-NOGO-PREREGISTRATION-V3.md commit SHA (2f29413)"
      via: "capture_provenance(--prereg)"
      pattern: "prereg_commit_sha"
---

<objective>
Execute the measurement — the v3.3 scored run set. **The measurement is the deliverable**; *"A NO-GO diagnosed by telemetry is a successful Phase 3. An undiagnosed number is not."*

**This plan reports. It does not judge.** D-GO5: the GO/NO-GO call is the senior reviewer's, made against the committed pre-registration; the executor reports numbers and telemetry and does not declare the verdict.

**Single source of truth.** The frozen run protocol lives in **§10 of `03-GO-NOGO-PREREGISTRATION-V3.md`** (governing commit `2f29413`). This plan does not restate or re-derive any value — the executor runs §10 verbatim. If anything here appears to conflict with §10, **§10 wins**.

**Scope note (reviewer, on the record).** v3.3 is a wiring re-roll (U1/U2/U3 oracle-engagement tracking). It re-runs only the **3 scored recall runs**. The v3 smoke-run assumptions (A1/A4), the Qwen tool-fidelity probe (D-GO3(ii)), and the real-model low-ceiling confirmation (D-BUD6) were established under the v3 line and **stand** — the U1/U2/U3 change touches oracle-lead tracking, not the tool-message protocol, tool-call fidelity, or the budget gate. If the reviewer later wants any of those re-confirmed, that is a separate directive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-drive-loop-spike-go-no-go/03-GO-NOGO-PREREGISTRATION-V3.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md
@src/evals/run.py
@src/agents/review/telemetry.py
---
</context>

<interfaces>
**Authoritative command — §10.3 of the committed v3.3 pre-registration.** The executor passes it verbatim, changing only `--run-index` across the three runs:

```
.venv/bin/python -m evals.run agent-run \
  --model databricks-meta-llama-3-3-70b-instruct \
  --run-index <1|2|3> \
  --run-prefix agent-run-v3- \
  --max-tokens 3200000 --max-wall-clock 1200 --max-turns 120 \
  --document-split scored \
  --out-dir .planning/phases/03-drive-loop-spike-go-no-go/runs \
  --prereg .planning/phases/03-drive-loop-spike-go-no-go/03-GO-NOGO-PREREGISTRATION-V3.md
```

**Artifact naming.** With `--run-prefix agent-run-v3-`, the three scored runs write `agent-run-v3-{1,2,3}.json` (FaultReport), `agent-run-v3-{1,2,3}.jsonl` (per-turn stream), and `agent-run-v3-{1,2,3}-summary.json` (provenanced RunSummary). This prefix is the v3 scored-set name; v1 (`agent-run*`) and v2 (`agent-run-v2-*`) artifacts are immutable prior evidence and must not be touched. The v3.2 working-tree artifacts under this same `agent-run-v3-` prefix are already preserved in commit `fe4c408`, so re-running overwrites only reproducible working files.

**Re-scoring** (`evals/capture.py`): `.venv/bin/python -m evals.run score --captured <path>` reproduces a run's numbers with no LLM call.

**D-GO2(i):** a failed/errored run is a **FAILING run, not a re-roll**. Sole exception: an infrastructure fault wholly outside the loop (endpoint 5xx / auth expiry with **zero tool calls made**, checked in the JSONL); the re-run and its reason are recorded.

**D-GO2(ii):** all 3 runs identical in configuration before the first executes; **any change voids the set and all 3 re-run.** Amending the pre-registration after run 1 begins voids the set (D-GO5).
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: §10 pre-run preconditions — reviewer authorizes before run 1</name>
  <files>(none — reviewer checkpoint)</files>
  <read_first>
    - .planning/phases/03-drive-loop-spike-go-no-go/03-GO-NOGO-PREREGISTRATION-V3.md (§10 — the four pre-run preconditions, verbatim)
  </read_first>
  <action>
Run the §10 pre-run preconditions **once, immediately before run 1**, and report their results. Per §10:

1. **Live probes:** 15-entry dual-resolve **15/15** AND R1 composition **30/30** against the installed store.
2. **Clean SUT + provenance:** `git status --porcelain -- src/ tests/` is **empty**; record `code_head_sha` (`git rev-parse HEAD`) — it must be identical for all 3 runs.
3. **Oracle-lead pre-flight:** `run_oracles_tool` on the scored corpus returns leads naming **all three** of {C-01 `11477`, C-02 `0.15`, B-08 `Any Unspecified Impurity`}. Any target missing = STOP, no runs.
4. **v3.3 U1 assertion:** `.venv/bin/pytest tests/agents/review/test_oracle_wiring.py::test_u1_run_oracles_dict_return_is_tracked` passes (proves the oracle_leads_surfaced tracker fires).

If any precondition fails, STOP and report — do not run. Do not change any configuration; §10 is frozen.
  </action>
  <resume-signal>Reviewer replies "preconditions pass — execute the 3 runs", or lists what must change first.</resume-signal>
  <verify>
    <manual>All four §10 preconditions reported with results; reviewer has replied with the resume signal, recorded verbatim in the SUMMARY.</manual>
  </verify>
  <done>The §10 preconditions were run and reported, and execution proceeds only on the reviewer's recorded resume signal.</done>
  <acceptance_criteria>
    - Probe results (15/15, 30/30), oracle pre-flight (all three targets), and the U1 assertion result are stated
    - `git status --porcelain -- src/ tests/` was empty and `code_head_sha` is recorded
    - The reviewer replied with the resume signal, recorded verbatim
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Execute the 3 scored runs and commit their artifacts</name>
  <files>.planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-1.json, .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-2.json, .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-3.json</files>
  <read_first>
    - .planning/phases/03-drive-loop-spike-go-no-go/03-GO-NOGO-PREREGISTRATION-V3.md (§10 — every value on the command line comes from here, not from executor judgment)
    - src/evals/run.py (the `agent-run` subcommand and the three artifacts it writes)
    - src/agents/review/telemetry.py (what lands in the summary; `capture_provenance`'s `prereg_commit_sha`)
  </read_first>
  <action>
(a) Execute the §10 command (quoted in `<interfaces>`) three times, **byte-identical apart from `--run-index` (1, 2, 3)**. Change nothing between runs — not the prompt, a ceiling, a tool description, or the corpus. Record the exact command line and `git rev-parse HEAD` for each run.

(b) Commit the nine artifacts: `agent-run-v3-{1,2,3}.json`, `agent-run-v3-{1,2,3}.jsonl`, `agent-run-v3-{1,2,3}-summary.json`. Verify each FaultReport re-scores with no LLM:
```
.venv/bin/python -m evals.run score --captured .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-1.json
```

(c) **Verify provenance on every summary.** Each must carry `prereg_commit_sha == 2f29413`, plus harness/matcher versions, `matcher_content_sha256`, `baseline_sha256`, normalizer/serializer/parser versions, corpus content-hash, the model id, and `run_completed` with `abort_reason`. After run 1, confirm its summary's `prereg_commit_sha == 2f29413` and `code_head_sha` matches before starting runs 2 and 3 (§10 precondition 2/3).

(d) **Handle failure per D-GO2(i).** A run that errors, exhausts its budget, or crashes is a FAILING run counted against the ">=2 of 3", recorded with `run_completed=False` and its `abort_reason`. Do not re-roll. Sole exception: an infrastructure fault wholly outside the loop with **zero tool calls in the JSONL**; any such re-run and its reason are recorded.

(e) **Report, do not interpret.** For each run record the raw numbers — `recall_by_family` per family, overall recall, tp/fp/fn, precision, anchor_rate, `found_set`, stop reason, turn count, billed tokens, continuation count. Compute the per-family median across the 3 runs using `metrics.recall_by_family` and `statistics.median` only. **Do not state whether the gate passed** — that reading is the reviewer's at plan 03-19, against the committed pre-registration.
  </action>
  <verify>
    <automated>for i in 1 2 3; do test -f .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-$i.json || exit 1; test -f .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-$i.jsonl || exit 1; test -f .planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-$i-summary.json || exit 1; done; PYTHONPATH=src .venv/bin/python -c "
import json
sha=None
for i in (1,2,3):
    s=json.load(open(f'.planning/phases/03-drive-loop-spike-go-no-go/runs/agent-run-v3-{i}-summary.json'))
    assert s.get('prereg_commit_sha'), f'run{i} missing prereg_commit_sha'
    sha = sha or s['prereg_commit_sha']
    assert s['prereg_commit_sha']==sha, 'runs scored under different pre-registrations'
print('ok', sha)"</automated>
  </verify>
  <acceptance_criteria>
    - Nine artifacts exist under `runs/`: `agent-run-v3-{1,2,3}.json`, `.jsonl`, `-summary.json`
    - All three summaries carry the **same** `prereg_commit_sha`, equal to `2f29413` and to `git log -1 --format=%H -- .../03-GO-NOGO-PREREGISTRATION-V3.md`
    - Each `agent-run-v3-{N}.json` re-scores through `evals.run score --captured` with no LLM call
    - Every summary has every D-TEL1(i) provenance field populated, with `run_completed` / `abort_reason` set
    - The three command lines differ **only** in `--run-index` — quoted verbatim in the SUMMARY
    - Any failing run is recorded as FAILING with its `abort_reason`; any re-run cites the D-GO2(i) exception and shows zero tool calls in the JSONL
    - The SUMMARY contains no statement of GO, NO-GO, pass or fail
  </acceptance_criteria>
  <done>Three scored runs executed under one frozen §10 configuration, nine committed artifacts whose provenance (`prereg_commit_sha == 2f29413`) proves they were scored under the pre-registered criteria.</done>
</task>

</tasks>

<threat_model>
| Threat | Mitigation |
|--------|-----------|
| Configuration drift between runs (Repudiation) | All three command lines quoted verbatim, differing only in `--run-index`; every summary carries the same `prereg_commit_sha` (`2f29413`), asserted by the verify command. D-GO2(ii) voids the set on any change. |
| Quietly re-drawing a failing run (Repudiation) | A failed run is recorded `run_completed=False` with its `abort_reason` and counts against ">=2 of 3". The sole exception requires **zero tool calls in the JSONL** — objective and checkable. |
| Executor declaring the verdict (Elevation) | Acceptance criteria forbid any statement of GO/NO-GO/pass/fail. The plan is `autonomous: false`; the verdict belongs to plan 03-19's reviewer checkpoint. |
| Committed artifacts leaking source/secrets (Info disclosure) | Plan 03-08's writers exclude tool-result bodies and credentials (`test_no_secret_or_document_text_reaches_the_artifacts`). |
</threat_model>

<verification>
- Nine scored-run artifacts exist and all three summaries share `prereg_commit_sha == 2f29413`, matching the committed pre-registration
- Each `agent-run-v3-{N}.json` re-scores through `evals.run score --captured` with no LLM call
- No output of this plan states GO or NO-GO
</verification>

<success_criteria>
- The §10 preconditions were run and reported, and the reviewer froze/authorized the set before run 1
- Three scored runs executed with byte-identical §10 configuration apart from `--run-index`, under `--run-prefix agent-run-v3-`
- Nine artifacts committed, each summary fully provenanced with `prereg_commit_sha == 2f29413`
- Failing runs recorded as failing, never re-rolled outside the declared exception
- No verdict is declared anywhere in this plan's outputs
</success_criteria>

<output>
After completion, create `.planning/phases/03-drive-loop-spike-go-no-go/03-18-SUMMARY.md`
</output>
