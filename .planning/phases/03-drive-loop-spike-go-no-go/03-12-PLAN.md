---
phase: 03-drive-loop-spike-go-no-go
plan: 12
type: execute
wave: 5
depends_on: ["03-08", "03-10", "03-12-P0"]
files_modified:
  - .planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run1.json
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run2.json
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run3.json
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run1-summary.json
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run2-summary.json
  - .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run3-summary.json
autonomous: false
requirements: [AGENT-01]

must_haves:
  truths:
    - "The baseline arm is re-run 3x under the same procedure as the agent arm — N=3, temperature 0, frozen config — and the MEDIAN governs (D-LOOP2)"
    - "The baseline's own variance (min/median/max) is reported, because if the single-shot detector swings widely then 'above baseline' is a weaker claim than it looks"
    - "The re-measurement happens BEFORE any agent result exists, so re-baselining after seeing results is structurally impossible (D-LOOP2, rider iii)"
    - "The re-measured median is recorded with the model id the committed baseline never captured"
    - "If the absolute difference between the median and 0.071 exceeds 0.03, that is a reportable measurement-stability finding and the senior reviewer confirms the new reference before the agent arm runs"
    - "Both arms are MECHANICALLY comparable: each baseline run commits the same provenance block the agent runs will carry, so a reviewer can confirm identical harness / matcher / baseline by comparing files rather than by trusting prose (D-GO1 rider iii)"
  artifacts:
    - path: ".planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md"
      provides: "3 baseline runs, per-family min/median/max, the drift check, and the governing reference"
    - path: ".planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run1.json"
      provides: "Committed FaultReport, re-scorable without an LLM"
    - path: ".planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run1-summary.json"
      provides: "Provenance sidecar — harness/matcher/baseline identity, so the two arms are comparable without prose"
      contains: "matcher_content_sha256"
  key_links:
    - from: "committed baseline-run{1,2,3}.json"
      to: "evals.capture.load_captured then metrics.recall_by_family"
      via: "re-scoring with no new metric code"
      pattern: "load_captured"
    - from: "baseline-run{N}-summary.json"
      to: "agents.review.telemetry.capture_provenance"
      via: "the SAME provenance capture the agent arm uses — never a second implementation"
      pattern: "matcher_content_sha256"
---

<objective>
Close D-PRE1's **fourth** step: re-measure the baseline arm 3x and freeze its median as the governing reference.

**Reviewer checkpoint amendment (2026-08-03):** the first remeasurement produced `0.000`, but the senior reviewer ruled that neither `0.000` nor `0.071` governs yet. The `0.000` median measured a broken redesigned single-shot detector, not a valid baseline; `0.071` remains only the committed historical reference until the live arm can reproduce its protected set. Therefore `03-12` stays OPEN and now depends on `03-12-P0-PLAN.md`, which repairs the single-shot regression and proves `{C-01, C-02}` are re-found in at least 2 of 3 live runs before this plan reruns from scratch.

The committed reference is `recall_by_family.json` at overall recall **0.071** (tp 2, fp 25, fn 26, `found_set ["C-01","C-02"]`) — and it **records no model id**. D-LOOP2 closes that gap and imposes arm symmetry: *"Both arms identical: N=3, temperature 0, frozen config, median as headline. Arm asymmetry is what was rejected in union-vs-single scoring, and a single-draw control is the same error pointed the other way."*

The sequencing is the integrity guarantee, not a rule someone has to remember: the repaired baseline re-run happens **before the agent runs**, and its median is committed to the pre-registration as the governing reference. *"The sequence makes re-baselining after seeing results impossible."*

It runs **after P2, P1 and the boundary hunt** because P2 changed parse output and therefore the number being frozen — re-baselining before the fix would freeze a figure the fix immediately invalidates.

**Checkpoint:** D-LOOP2 pre-registers the line: absolute difference between the median and 0.071 greater than **0.03**. If it trips, that is a reportable measurement-stability finding, disclosed in the report, and **the senior reviewer confirms the new reference before the agent arm runs.** This plan stops there rather than deciding.

Purpose: the gate compares the agent arm against this number. If this number is a single draw, or drifted silently, the comparison is not sound.
Output: 3 committed `FaultReport` JSONs, per-family min/median/max, the drift check, and a reviewer-confirmed governing reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-drive-loop-spike-go-no-go/03-CONTEXT.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-RESEARCH.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-P2-BASELINE-SHIFT.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-BOUNDARY-CROSSING-AUDIT.md
@src/evals/run.py
@src/evals/metrics.py
@src/evals/capture.py
@src/evals/baseline/recall_by_family.json
</context>

<interfaces>
**The committed reference** (`src/evals/baseline/recall_by_family.json`): `generated_from "golden:mvr1381_run3"`, overall recall **0.071** (tp 2, fp 25, fn 26), precision 0.074, `anchor_rate` 0.581, `found_set ["C-01","C-02"]`, `recall_by_family {absence_of_evidence 0.0, derivation_plausibility 0.0, cross_reference_integrity 0.286, regulatory_framing 0.0}`. **No model id recorded.**

**The baseline arm is the EXISTING legacy path, untouched.** `python -m evals.run run` iterates `eval_set.documents`, skips `held_out` (`run.py:260-261`), parses and calls `run_detection` per document (`:263-273`), computes metrics (`:280`), and records a `parse_failure` rather than crashing on a bad document (`:274-276`). Nothing in Phase 3 modifies it — `src/agents/detection/` is the baseline arm and D-LOOP1 requires it stay runnable.

**Model:** `databricks-meta-llama-3-3-70b-instruct`, routed through `resolve_detector_model` (`config.py:100-105`), baseline-matched per D-GO3(i): *"Running the recall gate on a different model changes two variables at once. Baseline-matched is comparison validity, not convenience."*

**Re-scoring without an LLM** (`evals/capture.py:16-18`):

```python
def load_captured(path: str | Path) -> FaultReport:
    """Load and validate a captured `FaultReport` JSON file from `path`."""
    return FaultReport.model_validate_json(Path(path).read_text())
```

**The median computation uses existing functions only.** `capture.load_captured` then `metrics.recall_by_family(report, eval_set, "mvr1381")` then `statistics.median` per family across the 3 runs, plus min/max. **No new metric code** — `metrics.recall_by_family` (`metrics.py:230-234`) is the frozen implementation and a second one *is* a harness change under D-GO1(iii).

**Measurement ceilings that constrain how this number should be read** (computed from the committed dataset): five `mvr1381` GT anchors produce zero matchable tokens under `match.py:88-92` and are permanent false negatives for **any** system — `A-07` (`'0.5%'`), `B-03` (`'389'`), `B-06` (`'485'`), `C-03` (`'ND'`), `D-01` (`'45, 56'`). Maximum achievable overall recall is **23/28 = 0.821**.

**D-GO2(i) applies to the baseline arm too:** a failed or errored run is a FAILING run, not a re-roll. The sole exception is an infrastructure fault wholly outside the detector (endpoint 5xx / auth expiry with zero work done), and any re-run plus its reason is recorded.

**`capture_provenance` (plan 03-08, wave 3)** returns the full D-TEL1(i) block: `run_index`, `model_id`, `prereg_commit_sha`, `harness_version`, `matcher_version`, `matcher_content_sha256`, `baseline_path` + `baseline_sha256`, `normalizer_version`, `serializer_version`, `parser_version`, `corpus_content_hash`, `run_completed` + `abort_reason`. `matcher_content_sha256` is a sha256 over `src/evals/match.py`'s bytes and `baseline_sha256` over the committed baseline JSON — computed, not hand-maintained, which is why they are the fields that make the cross-arm claim checkable. A bare `FaultReport` carries none of them.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run the baseline arm 3x under frozen configuration and commit each FaultReport</name>
  <files>.planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run1.json, .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run2.json, .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run3.json</files>
  <read_first>
    - src/evals/run.py (lines 240-335 — `cmd_run`, `_write_metrics`, `build_parser`; the exact CLI you invoke)
    - src/config.py (lines 90-110 — `DETECTOR_MODELS`, `resolve_detector_model`; the allow-list that makes the baseline-matched claim enforceable in code)
    - src/evals/capture.py (lines 10-30 — `load_captured`, how the committed reports are re-scored)
    - .planning/phases/03-drive-loop-spike-go-no-go/03-P2-BASELINE-SHIFT.md (plan 03-01's disclosure — the parse fix whose effect this measurement now captures)
  </read_first>
  <action>
Run the **existing legacy detector arm** three times. Do not modify anything under `src/agents/detection/` or `src/evals/` — this arm's whole value is being byte-identical to what produced 0.071.

(a) Freeze the configuration before run 1 and do not change it between runs (D-GO2(ii), applied symmetrically to the baseline arm):
- model: `databricks-meta-llama-3-3-70b-instruct`
- temperature: 0
- corpus: the eval set's non-held-out documents (`mvr1381`, `minispec`); `spec32s41` stays held out
- harness / matcher / baseline: as committed, unmodified

Record the exact command line and `git rev-parse HEAD` at run time in the report document. Any configuration change between runs **voids the set and all 3 re-run**.

(b) Execute three times, capturing each run's `FaultReport` as serialized JSON:

```
.venv/bin/python -m evals.run run --model databricks-meta-llama-3-3-70b-instruct --out <path>
```

(c) Commit the three reports to `.planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run{1,2,3}.json` as serialized `FaultReport`s, so the median is re-derivable by someone who did not watch the runs. Verify each with:

```
.venv/bin/python -m evals.run score --captured .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run1.json
```

which must reproduce the run's numbers with no LLM call.

(c2) **Emit a provenance sidecar beside each report** — `runs/baseline-run{N}-summary.json` — so the two arms are comparable **mechanically** rather than only through prose. Every agent-run summary will carry `harness_version`, `matcher_version`, `matcher_content_sha256` and `baseline_sha256` (D-TEL1(i)); a bare `FaultReport` carries none of them, so without the sidecar a reviewer cannot confirm both arms were scored under one harness — which is exactly D-GO1 rider (iii)'s ask — and the only bridge would be a hand-written `git rev-parse HEAD` buried in this document.

Reuse `capture_provenance` from `src/agents/review/telemetry.py` (plan 03-08, wave 3). Do **not** write a second provenance capture, and do **not** edit `src/evals/run.py` to produce it: run it from a one-off scratch script (a `python -c` invocation or a throwaway file outside the repo tree) over each committed report, so the acceptance criterion `git diff --quiet -- src/agents/detection/ src/evals/` still holds and the baseline arm stays byte-identical to what produced 0.071.

`prereg_commit_sha` will be `""` on these sidecars, and that is **correct**: the pre-registration does not exist until plan 03-17 (wave 9). A baseline run carrying one would mean the baseline was measured *after* the criteria were frozen — the inversion D-LOOP2's sequencing exists to prevent. The four fields that must match across the arms are `harness_version`, `matcher_version`, `matcher_content_sha256` and `baseline_sha256`; plan 03-17 §6 is where that comparison is recorded.

(d) Handle failure honestly. If a run errors, it is a **FAILING run, not a re-roll** — record it as such with its error. The sole exception is an infrastructure fault wholly outside the detector (endpoint 5xx / auth expiry with zero documents processed); a re-run under that exception, and its reason, are recorded in the report document. Do not quietly draw again.

(e) If the Databricks endpoint is unavailable, stop and report it. Do not synthesize, estimate, or reuse the committed 0.071 as if it were a fresh measurement. The live runs cannot be faked, and a fabricated reference would corrupt the gate that decides three phases.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -c "
import json
from evals.capture import load_captured
base = None
for i in (1, 2, 3):
    load_captured(f'.planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run{i}.json')
    s = json.load(open(f'.planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run{i}-summary.json'))
    key = (s['harness_version'], s['matcher_version'], s['matcher_content_sha256'], s['baseline_sha256'])
    assert all(key), f'baseline-run{i}-summary.json is missing a provenance field'
    assert s.get('prereg_commit_sha', '') == '', f'baseline-run{i} carries a prereg SHA that cannot exist yet'
    base = base or key
    assert key == base, 'baseline runs were scored under different harness/matcher/baseline'
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist under `.planning/phases/03-drive-loop-spike-go-no-go/runs/` named `baseline-run1.json`, `baseline-run2.json`, `baseline-run3.json`
    - Each validates through `evals.capture.load_captured` without error
    - Three provenance sidecars exist — `baseline-run{1,2,3}-summary.json` — each with non-empty `harness_version`, `matcher_version`, `matcher_content_sha256`, `baseline_sha256` and `model_id`, and all three carrying **identical** values for those four harness/matcher/baseline fields
    - `prereg_commit_sha` is `""` on all three sidecars — asserted, not assumed; the pre-registration does not exist until wave 9 and a baseline run carrying one would invert D-LOOP2's sequencing
    - The sidecars were produced by `agents.review.telemetry.capture_provenance` from a one-off scratch script: `grep -rc "def capture_provenance" src/evals/` returns `0` (no second implementation) and `git diff --quiet -- src/agents/detection/ src/evals/` returns clean — the baseline arm and harness were not modified by this plan
    - The report document records the exact command line, the model id `databricks-meta-llama-3-3-70b-instruct`, and `git rev-parse HEAD` at run time
    - Any failed run is recorded as a FAILING run with its error, and any re-run cites the D-GO2(i) infrastructure exception explicitly
  </acceptance_criteria>
  <done>Three baseline runs exist as committed, re-scorable `FaultReport`s produced under one frozen configuration, each with a provenance sidecar that makes the cross-arm harness comparison mechanical.</done>
</task>

<task type="auto">
  <name>Task 2: Compute median and variance with existing harness functions only, and apply the pre-registered drift check</name>
  <files>.planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md</files>
  <read_first>
    - src/evals/metrics.py (lines 196-235 — `compute_metrics` and `recall_by_family`; the FROZEN implementations to call, never reimplement)
    - src/evals/match.py (lines 19-30 and 66-92 — the tokenization floors and the `if not tokens: return False` rule at :89-90, which is why five GT items are permanently unmatchable)
    - src/evals/gate.py (lines 21-64 — `baseline_found_ids` and `check_gate`; `{C-01, C-02}` is the protected set)
    - src/evals/baseline/recall_by_family.json (the committed 0.071 reference)
    - .planning/phases/03-drive-loop-spike-go-no-go/03-P2-BASELINE-SHIFT.md (for attributing any shift to the parse fix rather than to variance)
  </read_first>
  <action>
Write `.planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md` containing all of the following with real measured numbers.

(a) Per-family min / median / max across the 3 runs, computed by pure composition of existing functions — `capture.load_captured`, then `metrics.recall_by_family(report, eval_set, "mvr1381")`, then `statistics.median` / `min` / `max`. Do NOT write a spike-local scorer: `metrics.recall_by_family` is frozen by D-GO1(iii) and a second implementation *is* a harness change.

| Family | run1 | run2 | run3 | min | median | max | committed baseline |
|---|---|---|---|---|---|---|---|
| `absence_of_evidence` | | | | | | | 0.0 |
| `derivation_plausibility` | | | | | | | 0.0 |
| `cross_reference_integrity` | | | | | | | 0.286 |
| `regulatory_framing` | | | | | | | 0.0 |
| overall | | | | | | | 0.071 |

Also record per run: `tp`, `fp`, `fn`, `precision`, `anchor_rate`, and the `found_set`.

(b) The pre-registered drift line. Compute the absolute difference between the overall median and 0.071 and compare against **0.03**. State the result as the literal word `WITHIN` or `EXCEEDS` — no hedging, no "approximately".

(c) The protected set. D-GO1(b) requires `{C-01, C-02}` not be lost. Report whether each baseline run still finds both. If the baseline arm itself has stopped finding them, that is a material finding about the reference rather than about the agent, and it must be surfaced before the agent arm runs.

(d) The baseline's own variance as a first-class result. Include this sentence with the measured spread beside it: *"If the single-shot detector swings widely across its own 3 runs, 'above baseline' is a weaker claim than it looks, and the gate reading must say so."* This is a pre-registered reading, not an optional observation.

(e) Attribution. Cross-reference `03-P2-BASELINE-SHIFT.md`. If the median differs from 0.071, state how much of the difference is attributable to the P2 parse fix versus run-to-run variance, per D-PRE1(a). Do not fold a parse-driven shift into "variance".

(f) What the committed baseline could not record. The baseline JSON has no model id; this measurement does. State the model id as part of the new governing reference.

(g) A `## Governing reference proposed for the pre-registration` section stating the exact per-family median values and overall median that plan 03-17 will commit, plus this run set's `git rev-parse HEAD`, plus the four cross-arm provenance values read off the sidecars — `harness_version`, `matcher_version`, `matcher_content_sha256`, `baseline_sha256` — so plan 03-17 §6 can quote them without re-deriving anything. These are the values the agent-run summaries must match for D-GO1 rider (iii) to hold.

Do **not** edit `src/evals/baseline/recall_by_family.json`. The governing reference lives in the pre-registration; rewriting the committed baseline would be a harness change under D-GO1(iii).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md && grep -qE "WITHIN|EXCEEDS" .planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md && grep -q "databricks-meta-llama-3-3-70b-instruct" .planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md && git diff --quiet -- src/evals/baseline/ && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - The document contains the full per-family table with numeric values in every cell for all 4 families plus overall (no `TBD`, no blanks)
    - It contains exactly one of the literal words `WITHIN` or `EXCEEDS` for the drift check
    - It reports `tp`, `fp`, `fn`, `precision`, `anchor_rate` and `found_set` per run
    - It states whether `C-01` and `C-02` were found in each run
    - It contains the literal string `a weaker claim than it looks`
    - It contains a `## Governing reference proposed for the pre-registration` section with explicit median values and the four cross-arm provenance values (`harness_version`, `matcher_version`, `matcher_content_sha256`, `baseline_sha256`) copied from the sidecars
    - `git diff --quiet -- src/evals/baseline/` returns clean (the committed baseline JSON was not rewritten)
    - `grep -c "TBD\|TODO" .planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md` returns `0`
  </acceptance_criteria>
  <done>The governing reference is a 3-run median with its variance, computed only by frozen harness functions, with drift measured against the pre-registered line.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Senior-reviewer confirmation of the governing reference (D-LOOP2)</name>
  <files>(none — reviewer checkpoint)</files>
  <read_first>
    - .planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md (the measurement you are confirming)
    - .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run1.json, baseline-run2.json, baseline-run3.json (re-derive any number yourself)
    - .planning/phases/03-drive-loop-spike-go-no-go/03-P2-BASELINE-SHIFT.md (what P2 moved, so a parse shift is not read as variance)
    - src/evals/baseline/recall_by_family.json (the committed 0.071 reference)
  </read_first>
  <what-built>
Three baseline runs of the legacy single-shot detector on `databricks-meta-llama-3-3-70b-instruct`, committed as re-scorable `FaultReport` JSONs, plus `03-BASELINE-REMEASUREMENT.md` with per-family min/median/max, the protected-set check, and the drift result against the pre-registered 0.03 line.

Nothing was decided. D-LOOP2 states that if the median lands materially off the committed 0.071, *"the senior reviewer confirms the new reference before the agent arm runs."*
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md`.
2. Check the drift line result — `WITHIN` or `EXCEEDS` the pre-registered 0.03.
3. Check the baseline's own spread across its 3 runs. A wide spread weakens every later "above baseline" claim, and the gate reading must say so.
4. Check whether `{C-01, C-02}` survived in all three runs. If not, the protected set that D-GO1(b) depends on has moved in the reference itself.
5. Re-derive any number you want independently:
   `.venv/bin/python -m evals.run score --captured .planning/phases/03-drive-loop-spike-go-no-go/runs/baseline-run2.json`
6. Decide which median governs the gate. This value goes into `03-GO-NOGO-PREREGISTRATION.md` (plan 03-17) and **cannot be amended after any agent run begins** without voiding the run set (D-GO5).

Note the sequencing guarantee you are protecting: no agent result exists yet, so confirming this reference now makes re-baselining after seeing results structurally impossible.
  </how-to-verify>
  <resume-signal>Reply with the confirmed governing median (or "confirmed as measured"), plus any instruction about how the variance should be read at the gate. If EXCEEDS, state explicitly whether the new median or the committed 0.071 governs.</resume-signal>
  <action>
STOP HERE. This is a blocking reviewer gate. Present the `<what-built>` and `<how-to-verify>` content below to the senior reviewer verbatim, then WAIT for the `<resume-signal>` response. Do not proceed to any downstream plan, do not make the decision on the reviewer's behalf, and do not paraphrase or pre-empt their answer. Record their reply verbatim in the plan SUMMARY.
  </action>
  <verify>
    <manual>Reviewer has replied with the resume signal; the reply is recorded verbatim in the plan SUMMARY.</manual>
  </verify>
  <done>Reviewer replied with the resume signal, their answer is recorded verbatim, and execution proceeds only on the basis of that answer.</done>
  <acceptance_criteria>
    - The reviewer has replied with a governing median, or "confirmed as measured"
    - The reply is recorded verbatim in the plan SUMMARY, including any instruction about how variance should be read at the gate
    - If the drift check read `EXCEEDS`, the reply states explicitly whether the new median or the committed 0.071 governs
    - No downstream plan (03-17, 03-18) has begun before this reply exists
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live model output to the governing reference | The number the entire gate compares against originates from three non-deterministic runs |
| Measurement time to gate time | A reference re-measured after results exist would be unfalsifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-GATE | Repudiation | the governing reference | mitigate | The re-measurement is structurally ordered before any agent run (`depends_on` chain P2 to P1 to hunt to this plan to pre-registration to runs), the three `FaultReport`s are committed so anyone can re-derive the median with `evals.run score --captured`, and the median is computed only by the frozen `metrics.recall_by_family`. D-LOOP2's design intent: *"the sequence makes re-baselining after seeing results impossible"* |
| T-03-GATE-DRAW | Repudiation | single-draw control | mitigate | Arm symmetry: N=3 with the median governing, matching the agent arm's D-GO2 procedure exactly. A single-draw baseline against a 3-run agent arm is the same asymmetry error that union-vs-single scoring was rejected for, pointed the other way |
| T-03-PARSE | Tampering | attribution of a baseline shift | mitigate | Any difference from 0.071 is attributed against `03-P2-BASELINE-SHIFT.md` — parse-driven movement is named as such rather than folded into run-to-run variance, per D-PRE1(a) |
| T-03-BASELINE-EDIT | Tampering | `src/evals/baseline/recall_by_family.json` | mitigate | This plan is forbidden from editing it (acceptance criterion asserts `git diff --quiet -- src/evals/baseline/`). Rewriting the committed baseline is a harness change under D-GO1(iii) and would void the comparison. The new reference lives in the pre-registration instead |
| T-03-FABRICATE | Spoofing | unavailable endpoint | mitigate | Task 1(e) forbids synthesizing or reusing 0.071 as a fresh measurement; an unavailable endpoint stops the plan and is reported. A fabricated reference would corrupt the gate that decides Phases 4 to 6 |
| T-03-PROVENANCE | Repudiation | cross-arm comparability | mitigate | Each baseline run commits a `capture_provenance` sidecar carrying the same `harness_version` / `matcher_version` / `matcher_content_sha256` / `baseline_sha256` block the agent-run summaries carry, asserted identical across the three runs by the verify command. Without it the arms are bridged only by prose, and D-GO1 rider (iii)'s *"same harness, matcher version and committed baseline"* claim could not be confirmed mechanically. The sidecar reuses plan 03-08's helper rather than a second implementation, which would itself be a harness divergence |
</threat_model>

<verification>
- Three `baseline-run{1,2,3}.json` files exist and load through `evals.capture.load_captured`
- Three `baseline-run{1,2,3}-summary.json` provenance sidecars exist with identical harness/matcher/baseline fields and an empty `prereg_commit_sha`
- `03-BASELINE-REMEASUREMENT.md` has a complete numeric table, the drift verdict, and the governing-reference section
- `git diff --quiet -- src/agents/detection/ src/evals/` returns clean
- Reviewer has confirmed which median governs
</verification>

<success_criteria>
- The baseline arm ran 3x under one frozen configuration with the model id recorded
- Three re-scorable `FaultReport`s are committed to the phase directory, each with a `capture_provenance` sidecar so the cross-arm harness comparison is mechanical rather than prose
- Per-family min/median/max is computed only by frozen harness functions
- The 0.03 drift line is evaluated to `WITHIN` or `EXCEEDS`
- The protected set `{C-01, C-02}` is checked per run
- The baseline's own variance is reported as a first-class result with its pre-registered reading
- The senior reviewer has confirmed the governing reference before any agent run exists
</success_criteria>

<output>
After completion, create `.planning/phases/03-drive-loop-spike-go-no-go/03-12-SUMMARY.md`
</output>
