---
phase: 03-drive-loop-spike-go-no-go
plan: 10
type: execute
wave: 4
depends_on: ["03-04", "03-06"]
files_modified:
  - .planning/phases/03-drive-loop-spike-go-no-go/03-BOUNDARY-CROSSING-AUDIT.md
  - tests/integration/test_composition_chains.py
autonomous: true
requirements: [GROUND-01, GROUND-03]

must_haves:
  truths:
    - "A written, enumerated list exists of every enumerate-to-X / classify-to-Y / build-to-Z chain in this codebase that is unit-tested on each side but never composed on real data (D-PRE1(b))"
    - "Each chain on that list has a composition test driving the REAL committed artifacts all the way through, or a written reason it cannot"
    - "The audit cannot be satisfied by 'we looked and it seemed fine' — the deliverable is a list plus tests, and the list names its search method"
  artifacts:
    - path: ".planning/phases/03-drive-loop-spike-go-no-go/03-BOUNDARY-CROSSING-AUDIT.md"
      provides: "The enumerated chain list, the search method, and per-chain disposition"
    - path: "tests/integration/test_composition_chains.py"
      provides: "One composition test per chain found"
  key_links:
    - from: "tests/integration/test_composition_chains.py"
      to: "the real committed corpus / rulebook / ingest cache"
      via: "real primitives end to end, never a fixture that fakes the boundary"
      pattern: "build_corpus_index|ingest_corpus"
---

<objective>
Close D-PRE1's **third** precondition: the boundary-crossing hunt, scoped by the Phase-2 verification queue as *"not a style pass; hunt for more enumerate-to-X / build-to-Y / classify-to-Z chains unit-tested each side but never composed on real data (3 found this phase)."*

This project has a signature bug: **a green unit test on both sides of an untested boundary.** `tests/tools/test_enumerate_fetch_emit_e2e.py`'s own docstring names the failure class — *"green unit tests on each side… but nothing drove the REAL committed artifact all the way through in one composed test."* Verification-queue item 5 is what that class looks like in production: all 15 requirement-index citations failed to resolve against the rulebook store while both sides were individually green.

**D-PRE1(b) makes the deliverable concrete so this cannot pass as diligence:** a written list, plus a composition test for each chain found. Not a report that says the code looked fine.

RESEARCH.md seeds the hunt with three candidates so it starts from evidence rather than a blank page:
1. **The span-ID round-trip** (Pitfall 1) — render to parse to re-mint to `was_issued` to `open_span`. *Already discharged by plan 03-06's `test_spanref_roundtrip.py`* — record it on the list as CLOSED with the test as evidence.
2. **`run_oracles` to `get_section` to `emit_finding`** (Pitfall 9) — the oracle lead must become a real re-opened span before the gate accepts it.
3. **`cache_key` to parse output** (Runtime State Inventory) — *already discharged by plan 03-01's parser-version test.* Record as CLOSED.

Purpose: this runs after P2 and P1 because both change what "real data" means, and before the D-LOOP2 baseline because a boundary defect found afterwards would invalidate the frozen number.
Output: the audit document and one composition test per open chain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-drive-loop-spike-go-no-go/03-CONTEXT.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-RESEARCH.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-PATTERNS.md
@.planning/phases/02-retrieval-navigation-tools-rulebook/02-PHASE-VERIFICATION-QUEUE.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md
@tests/tools/test_enumerate_fetch_emit_e2e.py
</context>

<interfaces>
**The failure class, in the words of the test that exists to catch it** (`tests/tools/test_enumerate_fetch_emit_e2e.py` docstring): green unit tests on each side of a boundary, but nothing drove the real committed artifact all the way through in one composed test.

**What a chain looks like.** Three shapes recur in this codebase:
- `enumerate -> X` — something lists identifiers, another thing resolves them. (Item 5: requirement-index citations enumerated, `lookup_citation` resolves. Both green, neither composed, all 15 failed.)
- `classify -> Y` — something assigns a category, another thing keys behavior off it. (P1: `classify_document` assigns a family, the requirement index fires on it. The traceability test used a hardcoded manifest.)
- `build -> Z` — something constructs a key or an artifact, another thing consumes it. (`cache_key` builds a key from versions; `read_doc_cache` consumes it. The parser version was missing and stale entries were served.)

**Three seeds from RESEARCH.md**, with current status:

| # | Chain | Status entering this plan |
|---|---|---|
| 1 | rendered span-ID string -> `parse_span_ref` -> `was_issued` -> `open_span` | **CLOSED** by plan 03-06 `tests/agents/review/test_spanref_roundtrip.py` |
| 2 | `run_oracles` lead -> `get_section` -> `emit_finding` | **OPEN** — plan 03-09 asserts the gate rejects an un-re-opened lead; the *positive* path (lead -> re-open -> accepted finding) needs composing |
| 3 | `cache_key` -> parse output -> served cache entry | **CLOSED** by plan 03-01 `tests/ingest/test_store.py::test_parser_version_bump_invalidates_cache` |

**Method to use for the search** (state it in the document so the audit is reproducible):
- Grep for functions whose names or docstrings contain `enumerate`, `classify`, `build_`, `_key`, `lookup`, `resolve`, `index` and identify their consumers.
- For each pair, check whether any test constructs the producer's real output and feeds it to the real consumer, versus a test that hand-writes the intermediate value.
- **A hand-written intermediate is the tell.** A test containing a literal dict/manifest/key where production would have a computed one is a chain that was never composed.

**Exemplar to imitate:** `tests/tools/test_enumerate_fetch_emit_e2e.py`, including its `update_manifest=False` guard at `:38-41` — *"this fixture must NEVER rewrite the committed `rulebook/manifest.yaml`."*
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate every producer/consumer chain and classify each as composed or un-composed</name>
  <files>.planning/phases/03-drive-loop-spike-go-no-go/03-BOUNDARY-CROSSING-AUDIT.md</files>
  <read_first>
    - tests/tools/test_enumerate_fetch_emit_e2e.py (whole file — the exemplar and the failure-class definition)
    - .planning/phases/02-retrieval-navigation-tools-rulebook/02-PHASE-VERIFICATION-QUEUE.md (items 1, 2, 5 — the three chains already found, and what each looked like before it was caught)
    - src/ingest/classify.py, src/ingest/corpus.py, src/ingest/store.py (the classify and build chains)
    - src/rulebook/requirement_index.py, src/rulebook/store.py (the enumerate chain that produced item 5)
    - src/tools/read_guideline.py (lines 33-80 — enumerate mode vs fetch mode, the D-RI2 surface)
    - src/agents/review/spanref.py and tests/agents/review/test_spanref_roundtrip.py (from plan 03-06 — chain 1's closure evidence)
  </read_first>
  <action>
Run the search described in the interfaces block and produce an **enumerated list**. This is a code-reading task with a written deliverable; it is not satisfied by an impression.

Concrete procedure:

1. Grep `src/` for producer functions: names or docstrings matching `enumerate`, `classify`, `build_`, `_key`, `lookup`, `resolve`, `index`, `mint`, `serialize`. Record each with its `file:line`.
2. For each producer, find its consumers by grepping for its name across `src/`. Record each producer/consumer pair as a candidate chain.
3. For each candidate chain, search `tests/` for a test that:
   - constructs the producer's output by **calling the producer**, and
   - feeds that value to the **real consumer**, and
   - uses real committed artifacts (real ingest cache, real rulebook store, or a fixture built through the genuine pipeline like `build_corpus_index`) rather than a hand-written literal.
4. Classify each chain:
   - **COMPOSED** — such a test exists. Record its node ID.
   - **UN-COMPOSED** — both sides are unit-tested but the intermediate is hand-written somewhere, or no test spans the boundary. **This is a finding.**
   - **N/A** — the producer has exactly one consumer in the same module and no serialization boundary between them.

The tell to look for explicitly: a test containing a literal dict, manifest, cache key or citation string where production would compute one. Item 5 and P1 both hid behind exactly that.

Record the count. Phase 2 found 3; this hunt starting from 3 seeds should not plausibly return 0 new chains without an explanation of why the search space is exhausted — if it does return 0, say so and show the enumerated candidate list that supports it.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-drive-loop-spike-go-no-go/03-BOUNDARY-CROSSING-AUDIT.md && grep -qE "^\| *[0-9]+ *\|" .planning/phases/03-drive-loop-spike-go-no-go/03-BOUNDARY-CROSSING-AUDIT.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `03-BOUNDARY-CROSSING-AUDIT.md` exists and contains a numbered table with one row per candidate chain, each row carrying producer `file:line`, consumer `file:line`, and a classification of exactly `COMPOSED` / `UN-COMPOSED` / `N/A`
    - The document contains a `## Search Method` section naming the grep terms used and the "hand-written intermediate is the tell" heuristic, so the audit is reproducible
    - Every chain classified `COMPOSED` cites a pytest node ID as its evidence
    - The three RESEARCH.md seed chains all appear in the table with their status
    - `grep -c "we looked\|seemed fine\|appears correct\|no issues found" .planning/phases/03-drive-loop-spike-go-no-go/03-BOUNDARY-CROSSING-AUDIT.md` returns `0`
    - The document contains an explicit total of the form `N candidate chains examined, M UN-COMPOSED`
  </acceptance_criteria>
  <done>Every producer/consumer chain in the codebase is enumerated with a reproducible method and classified, with evidence for each closure.</done>
</task>

<task type="auto">
  <name>Task 2: Write a composition test for every UN-COMPOSED chain found</name>
  <files>tests/integration/test_composition_chains.py, .planning/phases/03-drive-loop-spike-go-no-go/03-BOUNDARY-CROSSING-AUDIT.md</files>
  <read_first>
    - tests/tools/test_enumerate_fetch_emit_e2e.py (whole file — the shape every test here copies, including the `update_manifest=False` guard at :38-41)
    - tests/agents/review/test_spanref_roundtrip.py (from plan 03-06 — the other exemplar, and chain 1's closure)
    - tests/agents/review/test_oracles_tool.py (from plan 03-09 — asserts the gate REJECTS an un-re-opened lead; chain 2's positive path is what remains)
    - tests/tools/conftest.py and tests/agents/review/conftest.py (the real corpus builders)
    - .planning/phases/03-drive-loop-spike-go-no-go/03-BOUNDARY-CROSSING-AUDIT.md (from Task 1 — the list you are discharging)
  </read_first>
  <action>
Create `tests/integration/test_composition_chains.py` with **one test per UN-COMPOSED chain** from Task 1's table. Each test must:
- call the **real** producer (never hand-write the intermediate value),
- feed its output to the **real** consumer,
- assert the composed behavior, not just that each side ran,
- use real committed artifacts or a fixture built through the genuine pipeline,
- carry a docstring naming the chain, its audit row number, and what would break if the boundary were wrong.

**Chain 2 is known open and must be included** — the positive `run_oracles -> get_section -> emit_finding` path:

```python
def test_oracle_lead_reopened_becomes_an_accepted_finding(tmp_path):
    """Boundary-crossing audit chain #2 (Pitfall 9). Plan 03-09 proves the gate REJECTS an
    un-re-opened oracle lead; this proves the POSITIVE path composes -- a lead's locating hint
    turns into a real get_section call, whose issued span-ID emit_finding accepts.

    If this boundary were wrong, absence_of_evidence would sit at 0.0 for a reason having nothing
    to do with the agent, and D-GO1(iii)'s named headline expectation would be untestable."""
    leads = run_oracles_tool(corpus, "d1", ledger)
    lead = leads["absence_leads"][0]
    rendered = get_section(corpus, "d1", ledger, heading=lead["heading_hint"])   # the hint, USED
    ref = SPAN_REF_RE.findall(rendered)[0]
    span = parse_span_ref(ref, corpus, RULEBOOK_CACHE, expect="submission")
    fault = emit_finding(corpus, span, rule_span, ledger, verdict="gap", title="...", detail="...")
    assert not isinstance(fault, ToolRejected), fault
    assert fault.submission_span_id is not None and fault.rule_span_id is not None
```

Note the load-bearing detail: the test must **use the lead's own `heading_hint`** to drive `get_section`. Passing a heading the test author knows is correct would compose two functions while skipping the very field whose correctness is in question.

For each additional UN-COMPOSED chain Task 1 found, write the analogous test. If a chain genuinely cannot be composed offline (e.g. it requires a live endpoint), do **not** write a fake — record it in the audit document under `## Chains that cannot be composed offline`, with the reason and what would be needed, and make sure it is visible to the reviewer at the plan 03-17 pre-registration gate.

Follow `test_enumerate_fetch_emit_e2e.py:38-41`'s discipline: `update_manifest=False` on every rulebook build. A test that rewrites the committed `rulebook/manifest.yaml` would make every later run unreproducible.

Finally, update `03-BOUNDARY-CROSSING-AUDIT.md` so every UN-COMPOSED row now cites the pytest node ID that closes it, and add a `## Disposition` section stating the final counts: chains examined, un-composed found, tests written, chains left open with reasons.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/integration/test_composition_chains.py -x -q && .venv/bin/pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/integration/test_composition_chains.py` exists and passes
    - The number of `def test_` functions in the file is at least the number of `UN-COMPOSED` rows in the audit table (or the shortfall is explained in `## Chains that cannot be composed offline`)
    - `grep -c "def test_oracle_lead_reopened_becomes_an_accepted_finding" tests/integration/test_composition_chains.py` returns `1`
    - `grep -c 'heading_hint' tests/integration/test_composition_chains.py` returns at least `1` (the lead's own field drives the call, not an author-known constant)
    - `grep -c "Mock\|MagicMock\|patch(" tests/integration/test_composition_chains.py` returns `0`
    - Every UN-COMPOSED row in the audit table now cites a pytest node ID
    - The audit document has a `## Disposition` section with four explicit counts
    - `git status --porcelain rulebook/manifest.yaml` shows no new modification
    - `.venv/bin/pytest -q` (full suite) passes
  </acceptance_criteria>
  <done>Every un-composed chain either has a composition test driving real artifacts end to end, or a written reason it cannot be composed offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Producer output to consumer input | Every serialization/identifier boundary where two independently-tested components meet |
| Test fixture to real committed artifact | A hand-written intermediate value in a test is precisely how an un-composed boundary hides |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-GATE | Repudiation | un-composed producer/consumer boundaries | mitigate | A written enumerated table with a stated, reproducible search method plus one composition test per open chain. Acceptance criteria forbid the phrases "we looked" / "seemed fine" / "no issues found" and require a numeric total, so the audit cannot be discharged by an impression. Verification-queue item 5 is the precedent: both sides green, all 15 failing in production |
| T-03-ORACLE | Elevation of Privilege | `run_oracles -> get_section -> emit_finding` positive path | mitigate | `test_oracle_lead_reopened_becomes_an_accepted_finding` drives the lead's **own** `heading_hint` into a real `get_section` call and asserts the resulting span is accepted by the real gate. Using an author-known heading would skip the field whose correctness is in question |
| T-03-PARSE | Tampering | committed `rulebook/manifest.yaml` | mitigate | Every rulebook build in the new tests uses `update_manifest=False` per `test_enumerate_fetch_emit_e2e.py:38-41`; an acceptance criterion asserts `git status --porcelain rulebook/manifest.yaml` gains no modification. A test that rewrote it would make every later run unreproducible |
| T-03-SPAN | Repudiation | chains that cannot be composed offline | mitigate | Recorded in a named audit section with the reason and what would be needed, and surfaced at the plan 03-17 pre-registration gate rather than silently omitted. A fake composition test would be worse than an acknowledged gap |
</threat_model>

<verification>
- `.venv/bin/pytest tests/integration/test_composition_chains.py -x -q` passes
- `.venv/bin/pytest -q` (full suite) passes
- `03-BOUNDARY-CROSSING-AUDIT.md` has an enumerated table, a search method, and a disposition with counts
- `git status --porcelain rulebook/manifest.yaml` unchanged
</verification>

<success_criteria>
- Every producer/consumer chain in the codebase is enumerated with `file:line` for both sides and classified
- The search method is written down and reproducible
- Each COMPOSED chain cites a pytest node ID; each UN-COMPOSED chain has a new composition test or a written reason it cannot be composed offline
- The `run_oracles -> get_section -> emit_finding` positive path is composed using the lead's own locating hint
- The audit contains explicit counts and none of the forbidden hand-wave phrases
</success_criteria>

<output>
After completion, create `.planning/phases/03-drive-loop-spike-go-no-go/03-10-SUMMARY.md`
</output>
