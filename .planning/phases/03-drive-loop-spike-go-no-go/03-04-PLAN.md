---
phase: 03-drive-loop-spike-go-no-go
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md
  - src/rulebook/requirement_index.py
  - tests/rulebook/test_requirement_index_integration.py
autonomous: true
requirements: [GROUND-03]

must_haves:
  truths:
    - "Real ingest_corpus on mvr1381 and spec32s41 classifies them into a family whose requirement-index entries actually fire in production, not only in a hardcoded unit fixture (P1, D-PRE1 step 2)"
    - "The enumerate-to-fetch-to-emit chain resolves on real committed data end to end: a citation the requirement index enumerates is a citation rulebook.store.lookup_citation can fetch (verification-queue item 5)"
    - "absence_of_evidence — the named headline expectation of D-GO1(iii) — has a proven live path from requirement enumeration to rule fetch, or its blocker is written down before any run"
  artifacts:
    - path: ".planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md"
      provides: "Measured real-ingestion classification result + item-5 closure evidence"
    - path: "tests/rulebook/test_requirement_index_integration.py"
      provides: "Integration test binding real classification to requirement-index firing"
  key_links:
    - from: "real ingest_corpus classification"
      to: "requirement-index entries CFR-211160B-SOUND-BASIS / CFR-211194-CALCULATIONS"
      via: "family match on the real classified profile"
      pattern: "3.2.S.5"
    - from: "requirement index citation strings"
      to: "rulebook.store.lookup_citation"
      via: "read_guideline fetch mode"
      pattern: "lookup_citation"
---

<objective>
Close D-PRE1's **second** precondition (P1), and close the one material item D-PRE1 does not name.

**P1** — the D-RI1(2) traceability test passes 14/14 against **hardcoded manifests** that assume `mvr1381` and `spec32s41` classify to family `3.2.S.5`. A green test there proves *"IF the profile includes 3.2.S.5, the CFR entries fire"* — it does not prove real ingestion tags these documents `3.2.S.5`. It runs **after** P2 (plan 03-01) because the classification signal may itself sit on the OCR-less scanned pages P2 recovers; proving classification against text `pdf.py` was discarding would prove the wrong thing.

**Verification-queue item 5** — recorded as MATERIAL: all 15 requirement-index `citation` strings failed to resolve via `rulebook.store.lookup_citation` because the index citations are subsection-level (`"21 CFR 211.160(b)"`) while the store keys are document-level (`ecfr-211.160`). The queue header marks it RESOLVED (`9c1f191`, 15/15 e2e) and `tests/tools/test_enumerate_fetch_emit_e2e.py` exists. RESEARCH.md rates that closure as **A6 — unverified**, and flags that `absence_of_evidence`, the named headline family under D-GO1(iii), is **entirely downstream of it**.

Purpose: if either of these is broken, the loop's `absence_of_evidence` recall is structurally zero for a reason that has nothing to do with the agent — and the gate would read that as an architecture failure.
Output: a measured, written classification proof; a re-verified enumerate-to-fetch chain; and an integration test that binds real classification to requirement-index firing so this can never silently regress.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-CONTEXT.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-RESEARCH.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-PATTERNS.md
@.planning/phases/02-retrieval-navigation-tools-rulebook/02-PHASE-VERIFICATION-QUEUE.md
@.planning/phases/02-retrieval-navigation-tools-rulebook/02-CONTEXT.md
@src/rulebook/requirement_index.py
@src/ingest/classify.py
</context>

<interfaces>
From the Phase-2 verification queue (read this session):

Item 2 / P1: *"run real `ingest_corpus` on `mvr1381` + `spec32s41` and confirm 3.2.S.5 is classified, so `CFR-211160B-SOUND-BASIS` / `CFR-211194-CALCULATIONS` fire in production, not only in the unit fixture. If real classification differs, re-tag the two entries from the manifest (entries are correct; only the linkage needs integration proof)."*

Item 5 (MATERIAL, marked RESOLVED at `9c1f191`): *"all 15 requirement-index `citation` strings fail to resolve via `rulebook.store.lookup_citation`. The index citations are subsection/glossary-level (`"21 CFR 211.160(b)"`, `"ICH Q6A -- 3.3(e) Water Content"`, `"ICH Q2(R2) -- Glossary: Specificity/Selectivity"`) but the store keys are document-level (`ecfr-211.160`, `ICH Q6A`). The loader gate still passes (it validates the provenance span's doc_id), but the D-RI2/D-EF1(5) enumerate-to-fetch-to-emit contract fails on real data."*

`read_guideline`'s two modes (`read_guideline.py:33-40`, the D-RI2 contract this depends on):
- `citation=None` → ENUMERATE: returns `[{"requirement_id","citation","rule_doc_id","trigger"}]`, records NO spans (`:45-57`)
- `citation=str` → FETCH via `_fetch_citation` (`:74`), annotated with span-IDs recorded

`REQUIREMENT_INDEX_VERSION = "3"` at `src/rulebook/requirement_index.py:42`.

`tests/tools/test_enumerate_fetch_emit_e2e.py` (8,888 bytes) is the exemplar composition test — its guard convention at `:38-41` uses `update_manifest=False` on every rulebook build: *"this fixture must NEVER rewrite the committed `rulebook/manifest.yaml`."* Honor that here.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify verification-queue item 5 is genuinely closed on real committed data (RESEARCH.md A6)</name>
  <files>.planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md</files>
  <read_first>
    - tests/tools/test_enumerate_fetch_emit_e2e.py (whole file — this is the test whose greenness is the claimed closure evidence; its docstring names the exact failure class)
    - src/rulebook/requirement_index.py (whole file — all 15 entries and their `citation` strings)
    - src/rulebook/store.py (`lookup_citation` and `all_chunks` — the keys the citations must resolve against)
    - src/tools/read_guideline.py (whole file — enumerate mode :45-57, fetch mode via `_fetch_citation` :74)
  </read_first>
  <action>
Do not take the queue header's RESOLVED marking on faith. Prove it on the committed data.

(a) Run the claimed closure evidence and record the exact output:

```
.venv/bin/pytest tests/tools/test_enumerate_fetch_emit_e2e.py -v
```

Record the pass count. The queue claims 15/15.

(b) Independently verify the chain **outside** that test, against the real committed rulebook store (`data/rulebook_cache/`, 607 entries) — a green test can pass against a tmp_path-scoped fixture store while production keys still mismatch, which is precisely the failure class item 5 named. Write a throwaway verification script (do not commit it; report its output) that:
  1. Loads every entry from `src/rulebook/requirement_index.py`
  2. For each entry's `citation` string, calls `rulebook.store.lookup_citation` against the **real** `data/rulebook_cache/` store
  3. Prints a per-entry `citation -> RESOLVED | NOT_FOUND` table and a total

(c) If **any** citation fails to resolve against the real store, this is item 5 re-opening. Do NOT attempt a fix in this plan and do NOT change `src/evals/match.py` or any baseline. Record the exact failing citations in the P1 proof document under a heading `## BLOCKER: verification-queue item 5 is NOT closed on real data`, and state plainly that `absence_of_evidence` — the named headline family of D-GO1(iii) — cannot fire until it is. This becomes a reviewer decision at the plan 03-17 pre-registration gate, not a silent workaround.

(d) If all resolve, record the count and note that A6 is now VERIFIED rather than ASSUMED.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/tools/test_enumerate_fetch_emit_e2e.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/tools/test_enumerate_fetch_emit_e2e.py -q` output (pass count) is quoted verbatim in the P1 proof document
    - The proof document contains a per-citation resolution table with one row per requirement-index entry and an explicit total of the form `N/15 resolved`
    - The proof document contains either the literal string `A6 VERIFIED` or a `## BLOCKER: verification-queue item 5 is NOT closed on real data` section — never neither, never both
    - `git status --porcelain rulebook/manifest.yaml` shows no new modification introduced by this task
  </acceptance_criteria>
  <done>Item 5's closure is confirmed against the real committed rulebook store, or its re-opening is documented as a named blocker before any spike run.</done>
</task>

<task type="auto">
  <name>Task 2: Prove real-ingestion classification drives requirement-index firing, and re-tag if it does not</name>
  <files>src/rulebook/requirement_index.py, tests/rulebook/test_requirement_index_integration.py</files>
  <read_first>
    - src/ingest/classify.py (whole file — `classify_document`, `EscalationStats`, and what the classification profile looks like)
    - src/ingest/corpus.py (the `ingest_corpus` entry point and where `classify_document` is called)
    - src/rulebook/requirement_index.py (the `CFR-211160B-SOUND-BASIS` and `CFR-211194-CALCULATIONS` entries and their family/trigger fields)
    - tests/rulebook/ (the existing D-RI1(2) traceability test with the hardcoded manifests — read it to see exactly what it assumes)
    - .planning/phases/03-drive-loop-spike-go-no-go/03-P2-BASELINE-SHIFT.md (produced by plan 03-01 — the parse fix whose recovered text may carry the classification signal)
  </read_first>
  <action>
(a) Run **real** `ingest_corpus` (post-P2, against the re-ingested cache from plan 03-01) on both documents:
  - `data/32s43-validation-related-compounds-method.pdf` (`mvr1381`)
  - `data/32s41-Specification.pdf` (`spec32s41`)

Record the **actual** classification each document receives — the family/CTD-section values `classify_document` produces, not what the fixture assumes.

(b) Determine whether the classification produced makes `CFR-211160B-SOUND-BASIS` and `CFR-211194-CALCULATIONS` fire. The concrete question: does `read_guideline(manifest, ledger, family=<the real classified family>)` enumerate those two requirement IDs?

(c) Two branches, both acceptable, neither silent:
  - **Classification matches the fixture's assumption.** Record it. P1 is proven.
  - **Classification differs.** The verification queue's own instruction applies: *"entries are correct; only the linkage needs integration proof"* — re-tag the two requirement-index entries so their family matches the **real** classified family, and bump `REQUIREMENT_INDEX_VERSION` from `"3"` to `"4"` in `src/rulebook/requirement_index.py:42`. Record the before/after family values in the proof document. Do NOT change the entries' citations, rule_doc_ids or triggers — only the family linkage.

(d) Add `tests/rulebook/test_requirement_index_integration.py` — an integration test that binds real classification to requirement-index firing, so this can never regress into a hardcoded-manifest assumption again. It must build its corpus through the real ingest path (never a hand-written manifest) and assert:

```python
def test_real_classification_makes_the_cfr_entries_enumerate():
    """P1 / D-PRE1 step 2. The D-RI1(2) traceability test passes 14/14 against HARDCODED
    manifests that assume mvr1381 classifies to 3.2.S.5 -- it proves "IF the profile includes
    3.2.S.5, the entries fire", not that real ingestion tags the document that way. This is the
    composition test that closes the gap. absence_of_evidence (D-GO1(iii)'s named headline
    expectation) is downstream of it."""
    # real ingest -> real classification -> read_guideline(family=<classified family>)
    assert "CFR-211160B-SOUND-BASIS" in enumerated_requirement_ids
    assert "CFR-211194-CALCULATIONS" in enumerated_requirement_ids
```

If the test needs real PDFs that may not exist in every environment, gate it on file existence with an explicit `pytest.skip` reason naming the missing path — but it must NOT skip in this repo, where both PDFs are present. Follow `tests/tools/test_enumerate_fetch_emit_e2e.py:38-41`'s `update_manifest=False` discipline: never rewrite the committed `rulebook/manifest.yaml`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/rulebook/test_requirement_index_integration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/rulebook/test_requirement_index_integration.py -x -q` reports `1 passed` (not `1 skipped`) in this repo
    - The test contains no hand-written manifest dict — grep the file for `CoverageManifest(` with literal document entries and confirm the corpus comes from the real ingest path instead
    - The proof document records the literal classified family string for both `mvr1381` and `spec32s41`
    - If re-tagging occurred: `grep -n "REQUIREMENT_INDEX_VERSION" src/rulebook/requirement_index.py` shows `"4"`, and the proof document has a before/after table for the two entries
    - If no re-tagging occurred: `git diff --quiet -- src/rulebook/requirement_index.py`
    - `git status --porcelain rulebook/manifest.yaml` shows no new modification
  </acceptance_criteria>
  <done>Real ingestion provably drives the two CFR requirement entries into enumeration, with a composition test that would fail if the linkage broke.</done>
</task>

<task type="auto">
  <name>Task 3: Write the P1 classification proof document</name>
  <files>.planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md</files>
  <read_first>
    - .planning/phases/02-retrieval-navigation-tools-rulebook/02-PHASE-VERIFICATION-QUEUE.md (items 1, 2, 5 and the disposition table — the exact obligations this document discharges)
    - .planning/phases/03-drive-loop-spike-go-no-go/03-P2-BASELINE-SHIFT.md (plan 03-01's output — this document sits immediately after it in the D-PRE1 chain)
  </read_first>
  <action>
Create `.planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md` containing, with real measured values and no placeholders:

1. **Header** — the D-PRE1 position: *"P2 (03-01) -> **P1 (this document)** -> boundary-crossing hunt (03-10) -> D-LOOP2 baseline (03-12) -> pre-registration (03-17) -> the 3 agent runs (03-18)."*

2. **Real classification results** — a table with one row per document:

| doc_id | source file | classified family / CTD section | matches the D-RI1(2) fixture assumption? |
|---|---|---|---|
| `mvr1381` | `data/32s43-validation-related-compounds-method.pdf` | *measured* | yes / no |
| `spec32s41` | `data/32s41-Specification.pdf` | *measured* | yes / no |

3. **Requirement-index firing** — did `CFR-211160B-SOUND-BASIS` and `CFR-211194-CALCULATIONS` enumerate under the real classified family? Yes/no per entry, plus the re-tagging record if any.

4. **Verification-queue item 5** — the per-citation resolution table from Task 1 with its `N/15 resolved` total, and either `A6 VERIFIED` or the named blocker section.

5. **What this means for the gate.** One paragraph, explicit: whether `absence_of_evidence` has a proven live path from requirement enumeration through rule fetch to `emit_finding`. If it does not, state that a `0.0` on that family in the spike would be attributable to this gap and not to the agent — the same discipline as D-PRE1(a)'s parse-shift attribution.

6. **Post-P2 note** — state whether the classification result depended on text recovered by plan 03-01's parse fix. This is the reason P1 runs after P2, and whether the ordering actually mattered is worth recording for Phase 4.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md && grep -qE "A6 VERIFIED|item 5 is NOT closed" .planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md && grep -q "CFR-211194-CALCULATIONS" .planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - The document exists and contains all six numbered sections
    - It contains a literal classified-family string for both `mvr1381` and `spec32s41` (not `TBD`, not `TODO`)
    - It contains both requirement IDs `CFR-211160B-SOUND-BASIS` and `CFR-211194-CALCULATIONS`
    - It contains exactly one of `A6 VERIFIED` or `item 5 is NOT closed`
    - It contains a sentence naming `absence_of_evidence` and whether its live path is proven
    - `grep -c "TBD\|TODO\|<fill in>" .planning/phases/03-drive-loop-spike-go-no-go/03-P1-CLASSIFICATION-PROOF.md` returns `0`
  </acceptance_criteria>
  <done>The P1 precondition is discharged in writing with measured values, and any residual blocker is named before it can be mistaken for an agent failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real document content to classification profile | Untrusted document text determines which regulatory requirements are considered applicable |
| Requirement-index citation to rulebook store key | A translation seam between two independently-versioned artifacts; a mismatch silently zeroes a whole failure family |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-GATE | Repudiation | classification-to-requirement linkage | mitigate | The hardcoded-manifest assumption is replaced by `tests/rulebook/test_requirement_index_integration.py`, which builds its corpus through the real ingest path. A future regression fails a test rather than silently zeroing `absence_of_evidence` and being read as an agent failure at the gate |
| T-03-PARSE | Tampering | committed `rulebook/manifest.yaml` | mitigate | The test follows `tests/tools/test_enumerate_fetch_emit_e2e.py:38-41`'s `update_manifest=False` discipline; an acceptance criterion asserts `git status --porcelain rulebook/manifest.yaml` gains no new modification. A test that rewrites the committed manifest would make every later run unreproducible |
| T-03-STORE | Elevation of Privilege | `rulebook.store.lookup_citation` resolution | mitigate | Task 1 resolves every index citation against the **real** `data/rulebook_cache/` store rather than a tmp_path fixture, because a fixture-scoped pass is exactly how item 5 hid the first time. A failure is recorded as a named blocker, never worked around |
</threat_model>

<verification>
- `.venv/bin/pytest tests/rulebook tests/tools -q` passes
- `.venv/bin/pytest -q` (full suite) passes
- `git status --porcelain rulebook/manifest.yaml` unchanged from before this plan
- `03-P1-CLASSIFICATION-PROOF.md` exists with no placeholder text
</verification>

<success_criteria>
- Real classification of `mvr1381` and `spec32s41` is measured and written down
- The two CFR requirement entries provably enumerate under the real classified family (after re-tagging if needed)
- `tests/rulebook/test_requirement_index_integration.py` passes without skipping and does not use a hand-written manifest
- Verification-queue item 5's closure is confirmed against the real store, or its re-opening is named as a blocker
- The proof document states explicitly whether `absence_of_evidence` has a proven live path
</success_criteria>

<output>
After completion, create `.planning/phases/03-drive-loop-spike-go-no-go/03-04-SUMMARY.md`
</output>
