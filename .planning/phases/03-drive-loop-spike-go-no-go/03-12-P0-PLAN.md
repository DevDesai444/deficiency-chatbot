---
phase: 03-drive-loop-spike-go-no-go
plan: 12-P0
type: execute
wave: 5
depends_on: ["03-11", "03-12"]
files_modified:
  - src/agents/detection/prompts.py
  - src/agents/detection/workers.py
  - tests/agents/detection/test_planner_redesign.py
  - tests/agents/detection/test_baseline_regression_guard.py
autonomous: true
requirements: [AGENT-01]

must_haves:
  truths:
    - "Neither `0.000` nor `0.071` governs until the single-shot detector regression is repaired and 03-12 is rerun from scratch"
    - "The redesigned planner/worker chain must be able to emit findings from the real golden document's TP-bearing Table 19/Table 20 sections before the baseline arm is measured again"
    - "The repair must not touch the matcher, harness, committed golden captures, or `src/evals/baseline/recall_by_family.json` (D-GO1(iii))"
    - "A live single-shot baseline run must re-find both protected historical TPs, `C-01` and `C-02`, in at least 2 of 3 runs before 03-12 can freeze any new reference"
  artifacts:
    - path: "tests/agents/detection/test_baseline_regression_guard.py"
      provides: "Composition guard: planner/worker path over real mvr1381 TP sections emits at least one candidate finding"
    - path: "src/agents/detection/prompts.py"
      provides: "Repair to the redesigned worker prompt so table contradictions are not deleted as compliance"
    - path: "src/agents/detection/workers.py"
      provides: "Worker conversion no longer silently drops untitled worker findings without trace"
  key_links:
    - from: "planner -> workers on real TP sections"
      to: "candidate finding emission"
      via: "real section/table sandwich, no hand-written intermediate"
      pattern: "run_workers"
    - from: "live single-shot detector"
      to: "C-01 and C-02 protected set"
      via: "evals.run score/gate with unchanged matcher and golden"
      pattern: "C-01"
---

<objective>
Repair the single-shot detector regression discovered by the 03-12 reviewer checkpoint before any baseline value is frozen.

The senior reviewer ruled that the `0.000` median is not a baseline. It is a measurement of a broken detector: the redesigned planner/worker chain committed unvalidated in `8760665` emits zero findings. Evidence recorded by the reviewer: `/tmp/census.json` shows 10 planner workers, 0 routed suspicions, 0 worker findings, 0 worker failures, 0 findings before verify, and 0 findings after verify/challenge; a direct specialist probe over the TP-bearing sections returned a valid parsed `findings=[]` with no failure. Freezing `0.000` would make D-GO1 a rubber stamp and void zero-TP-lost; freezing `0.071` while the live arm cannot reproduce it would violate D-LOOP2 symmetry.

Purpose: make the legacy single-shot baseline arm runnable and meaningful again before 03-12 reruns the three measurements.
Output: a targeted detector repair, a real-section composition test for the missing planner->worker boundary, and live-run evidence that both protected historical TPs (`C-01`, `C-02`) are found in at least 2 of 3 runs.
</objective>

<context>
@/tmp/census.json
@.planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md
@.planning/phases/03-drive-loop-spike-go-no-go/03-P2-BASELINE-SHIFT.md
@src/agents/detection/prompts.py
@src/agents/detection/planning.py
@src/agents/detection/workers.py
@src/agents/detection/sandwich.py
@src/agents/detection/pipeline.py
@src/evals/run.py
@src/evals/gate.py
@src/evals/baseline/recall_by_family.json
@tests/agents/detection/test_planner_redesign.py
@tests/evals/test_gate.py
@tests/evals/test_match.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the reviewer ruling as the blocking P0 gate</name>
  <files>.planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md</files>
  <action>
Add a `## Senior Reviewer Ruling` section to `03-BASELINE-REMEASUREMENT.md` recording, verbatim in substance:
- Neither number governs yet.
- `0.071` remains only the committed historical reference.
- `03-12` stays open.
- A blocking P0 repair precedes 03-12 completion.
- Do not proceed to Wave 6 before the repaired baseline is frozen.
- The amended D-PRE1 order is: P2 -> P1 -> boundary hunt -> P0 repair -> 03-12 rerun -> pre-registration -> agent runs.
  </action>
  <verify>
    <automated>grep -q "NEITHER number governs yet" .planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md && grep -q "P0 repair" .planning/phases/03-drive-loop-spike-go-no-go/03-BASELINE-REMEASUREMENT.md && echo ok</automated>
  </verify>
  <done>The checkpoint outcome is recorded before repair work begins.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the planner/worker path without touching harness, matcher, or golden</name>
  <files>src/agents/detection/prompts.py, src/agents/detection/workers.py</files>
  <action>
Fix the single-shot regression in the redesigned chain using the reviewer diagnosis:
- Compare the redesigned `WORKER_SPECIALIST` / planner prompts against the pre-redesign prompt surface; do not keep prompt constraints that teach the model to delete real table contradictions as compliant results.
- Verify the worker payload serializes section tables. If tables are present, do not invent a second table path; if a missing table serialization is found, fix that exact boundary.
- Address the dead routed-suspicion mechanism only inside the redesigned detection chain.
- `_to_faults` must not silently drop untitled worker findings. Assert titles are populated, or derive a safe title from evidence/detail and record the fallback as a parse/worker issue visible to tests.
- Do not edit `src/evals/match.py`, `src/evals/metrics.py`, `src/evals/run.py`, `src/evals/baseline/`, or `src/evals/dataset/golden/`.
  </action>
  <verify>
    <automated>git diff --quiet -- src/evals/match.py src/evals/metrics.py src/evals/run.py src/evals/baseline/ src/evals/dataset/golden/ && echo ok</automated>
  </verify>
  <done>The redesigned single-shot detector can emit candidate findings again without changing scoring machinery.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the missing composition guard and live protected-set acceptance evidence</name>
  <files>tests/agents/detection/test_baseline_regression_guard.py</files>
  <action>
Add the missing composition test:

`planner -> workers` on the real golden document's TP-bearing sections (Table 19 / Table 20) must emit `>=1` candidate finding. The test must use real section/table objects from `data/32s43-validation-related-compounds-method.pdf` or committed parse cache, route them through the real sandwich/worker conversion boundary, and must not hand-write the worker intermediate that production would compute.

Then run the pre-registered acceptance:
- A live single-shot run re-finds both `C-01` and `C-02` in at least 2 of 3 runs.
- The matcher, harness, and golden remain untouched.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/agents/detection/test_baseline_regression_guard.py -x -q && .venv/bin/pytest tests/agents/detection tests/evals -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/agents/detection/test_baseline_regression_guard.py` exists
    - It contains a test whose docstring names the `planner -> workers` boundary and the real TP-bearing Table 19/Table 20 sections
    - The composition guard emits at least one candidate finding
    - A live single-shot run re-finds both `C-01` and `C-02` in at least 2 of 3 runs
    - `git diff --quiet -- src/evals/match.py src/evals/metrics.py src/evals/run.py src/evals/baseline/ src/evals/dataset/golden/` returns clean
  </acceptance_criteria>
  <done>The tripwire that was missing at `8760665` exists, and the live baseline arm can reproduce its protected historical findings before 03-12 reruns.</done>
</task>

</tasks>

<verification>
- `/tmp/census.json` diagnosis is referenced in the ruling record
- `tests/agents/detection/test_baseline_regression_guard.py -x -q` passes
- Existing detection/eval regression tests pass
- Live single-shot protected-set acceptance passes: both `C-01` and `C-02` found in at least 2 of 3 runs
- Matcher, harness, committed baseline, and golden captures are unchanged
</verification>

<success_criteria>
- The reviewer ruling is recorded and `03-12` remains open
- The redesigned detector no longer emits zero findings on the real TP-bearing worker boundary
- The missing planner->workers composition guard is committed with the fix
- The protected set `{C-01, C-02}` is found in at least 2 of 3 live single-shot runs
- Only after this plan is complete may `03-12` be rerun from scratch and a new median frozen
</success_criteria>

<output>
After completion, create `.planning/phases/03-drive-loop-spike-go-no-go/03-12-P0-SUMMARY.md`
</output>
