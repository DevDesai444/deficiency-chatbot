---
phase: 05-deterministic-structural-cross-document-recall
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schemas/faults.py
  - src/rulebook/guard_vocab.py
  - src/parse/docx.py
  - src/parse/pdf.py
  - tests/rulebook/test_structural.py
  - tests/rulebook/test_references.py
  - tests/rulebook/test_precedent_search.py
  - tests/tools/test_emit_structural_finding.py
  - tests/tools/test_emit_reference_finding.py
  - src/evals/dataset/synthetic_fixture/doc_a.pdf
  - src/evals/dataset/synthetic_fixture/doc_b.docx
  - src/evals/dataset/synthetic_fixture/doc_c.pdf
  - src/evals/baseline/structural_threshold.json
  - src/evals/baseline/precedent_threshold.json
autonomous: true
requirements:
  - RECALL-02
  - RECALL-03
  - RECALL-04
  - RECALL-05

must_haves:
  truths:
    - "D-ENV1: Fault model in src/schemas/faults.py has StructuralAnchor, ReferenceAnchor, PrecedentAnchor as sibling models plus leg_tag + structural_anchor + reference_anchor + precedent_anchor fields, all optional, additive only (no existing field removed)"
    - "D-GRD3: src/rulebook/guard_vocab.py defines AGGREGATE_LEXICON and REFERENCE_CUE_WORDS as frozensets; test asserts no entry is a corpus-specific token"
    - "D-REF1 / D-GRD3: src/parse/docx.py emits a 'hyperlinks' key in the parsed dict (list of {rId, target, paragraph_index}); src/parse/pdf.py emits a 'links' key (list of {kind, uri, page})"
    - "D-GRD1/D-GRD4: synthetic fixture exists at src/evals/dataset/synthetic_fixture/ with doc_a (QOS analog), doc_b (Module analog), doc_c (Spec analog); fixture has planted structural + cross-ref + precedent-matchable deficiencies using realistic regulatory prose"
    - "Wave 0 test scaffolds exist and FAIL cleanly (no ImportError): tests/rulebook/test_structural.py, test_references.py, test_precedent_search.py, tests/tools/test_emit_structural_finding.py, test_emit_reference_finding.py"
  artifacts:
    - path: "src/schemas/faults.py"
      provides: "StructuralAnchor, ReferenceAnchor, PrecedentAnchor, Fault.leg_tag, Fault.structural_anchor, Fault.reference_anchor, Fault.precedent_anchor"
    - path: "src/rulebook/guard_vocab.py"
      provides: "AGGREGATE_LEXICON frozenset, REFERENCE_CUE_WORDS frozenset"
    - path: "src/parse/docx.py"
      provides: "hyperlinks list in parsed dict"
    - path: "src/parse/pdf.py"
      provides: "links list in parsed dict"
    - path: "src/evals/dataset/synthetic_fixture/"
      provides: "multi-doc committed guard fixture (D-GRD1/D-GRD4)"
    - path: "src/evals/baseline/structural_threshold.json"
      provides: "initial threshold placeholder (0.0)"
    - path: "src/evals/baseline/precedent_threshold.json"
      provides: "initial threshold placeholder (0.0)"
  key_links:
    - from: "src/rulebook/structural.py (Wave 2)"
      to: "src/rulebook/guard_vocab.AGGREGATE_LEXICON"
      via: "import"
      pattern: "from rulebook.guard_vocab import AGGREGATE_LEXICON"
    - from: "src/rulebook/references.py (Wave 2)"
      to: "src/rulebook/guard_vocab.REFERENCE_CUE_WORDS"
      via: "import"
      pattern: "from rulebook.guard_vocab import REFERENCE_CUE_WORDS"
    - from: "src/schemas/faults.py"
      to: "StructuralAnchor"
      via: "export"
      pattern: "class StructuralAnchor"
    - from: "src/parse/docx.py"
      to: "hyperlinks key in parsed output"
      via: "dict key"
      pattern: "\"hyperlinks\""
---

<objective>
Wave 0 foundation for Phase 5: establish the shared candidate envelope (D-ENV1), the guard vocab registry (D-GRD3), the parse-layer hyperlink/link extraction backfill (D-REF1), the committed synthetic guard fixture (D-GRD1/D-GRD4), threshold placeholder files, and all test scaffolds. Every subsequent plan depends on this foundation.

Purpose: Without the Fault schema extension and anchor types, no leg can emit candidates. Without guard_vocab.py, structural.py and references.py cannot import AGGREGATE_LEXICON / REFERENCE_CUE_WORDS. Without the parse-layer backfill, RECALL-03 has no hyperlink edges. Without the synthetic fixture, D-GRD2 invariants pytest.skip. Without test scaffolds, Wave-2 plans have no failing tests to implement against.

Output: Extended faults.py, new guard_vocab.py, docx.py + pdf.py with link extraction, synthetic fixture at src/evals/dataset/synthetic_fixture/, threshold JSON stubs, and Wave-0 test files that fail cleanly on missing implementations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deterministic-structural-cross-document-recall/05-CONTEXT.md
@.planning/phases/05-deterministic-structural-cross-document-recall/05-RESEARCH.md
@.planning/phases/05-deterministic-structural-cross-document-recall/05-PATTERNS.md

<!-- Prior phase summary needed for anchor/Fault extension pattern -->
@.planning/phases/04-rulebook-enrichment-absence-enumeration/04-03-SUMMARY.md

<interfaces>
<!-- Key existing code the executor needs. Extracted from 05-PATTERNS.md and verified repo code. -->

From src/schemas/faults.py (lines 68-116):
```python
# Existing anchor — the pattern to follow for all three new anchors:
class CoverageAbsenceAnchor(BaseModel):
    profile: list[str] = Field(default_factory=list, ...)
    family: str = Field(default="", ...)
    requirement_id: str = Field(default="", ...)
    threshold: float = Field(default=0.0, ...)
    sub_threshold_hits: list[RetrievalHit] = Field(default_factory=list, ...)
    manifest_span_ids: list[SpanID] = Field(default_factory=list, ...)
    claim_span_id: SpanID | None = Field(default=None, ...)

# Existing Fault additive field at ~line 101:
absence_anchor: CoverageAbsenceAnchor | None = Field(default=None, ...)
```

From src/parse/docx.py (line structure — current extract_docx returns dict):
- Currently returns dict with keys: "paragraphs", "tables", "sections"
- Does NOT extract hyperlinks (confirmed gap from RESEARCH.md)
- Add "hyperlinks": list[dict] with keys {rId, target, paragraph_index}
- Use zipfile + ET to read word/_rels/document.xml.rels

From src/parse/pdf.py (line structure):
- Currently returns dict with "blocks", "tables"
- Does NOT extract link annotations (confirmed gap)
- Add "links": list[dict] with keys {kind, uri, page, rect}
- Use fitz (PyMuPDF) page.get_links() for each page

From tests/rulebook/test_absence.py (pattern for new test scaffolds):
- Module-scoped rulebook fixture pattern at lines 29-51
- tmp_path usage for corpus isolation
- No network, no Databricks, no LLM calls
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend faults.py with StructuralAnchor, ReferenceAnchor, PrecedentAnchor + D-ENV1 Fault fields</name>
  <files>src/schemas/faults.py</files>
  <read_first>
    - src/schemas/faults.py (read in full — need the exact line where CoverageAbsenceAnchor ends and Fault begins, plus existing imports)
    - .planning/phases/05-deterministic-structural-cross-document-recall/05-PATTERNS.md § "src/schemas/faults.py" (the three anchor class definitions + additive Fault fields)
    - .planning/phases/05-deterministic-structural-cross-document-recall/05-RESEARCH.md § "StructuralAnchor design", "ReferenceAnchor design", "PrecedentAnchor design"
  </read_first>
  <behavior>
    - StructuralAnchor added with: claim_span_id, basis_span_ids, relation (Literal EQUALS/LEQ/GEQ/SUM/MAX/MIN/MEAN), expected_value, actual_value, comparison_store (Literal CORPUS/RULEBOOK default CORPUS), scoping_confidence (Literal full/low default full)
    - ReferenceAnchor added with: src_span_id, dst_span_id (nullable), edge_type (Literal hyperlink/textual_ref/value_crossref), anomaly (Literal UNRESOLVED_REF/ABSENT_TARGET/VALUE_CONTRADICTION), scoping_confidence (Literal full/low default full)
    - PrecedentAnchor added with: submission_span_id, precedent_doc_ids (list[str]), similarity_scores (list[float]), threshold (float), anda_excluded (list[str] default [])
    - Fault gains four additive optional fields: structural_anchor, reference_anchor, precedent_anchor (all [Anchor] | None = Field(default=None)), leg_tag (Literal ABSENCE/STRUCTURAL/REFERENCE/PRECEDENT | None = Field(default=None))
    - No existing Fault field is changed or removed
    - All new anchors placed BEFORE the Fault class, as siblings of CoverageAbsenceAnchor
  </behavior>
  <action>
Read src/schemas/faults.py in full. Find the line where CoverageAbsenceAnchor ends and Fault begins.

Insert the three new anchor classes between CoverageAbsenceAnchor and Fault (or after CoverageAbsenceAnchor before Fault). Follow the exact CoverageAbsenceAnchor shape (pydantic BaseModel, Field descriptions). Add `from __future__ import annotations` if not already present. Add `Literal` to the typing imports if not already there.

Add to Fault (after the existing `absence_anchor` field, line ~101):
```python
structural_anchor: StructuralAnchor | None = Field(
    default=None,
    description="D-STR3: re-derivable structural inconsistency anchor. Mutually exclusive with other anchors.")
reference_anchor: ReferenceAnchor | None = Field(
    default=None,
    description="D-REF2: cross-reference edge + anomaly. Mutually exclusive with other anchors.")
precedent_anchor: PrecedentAnchor | None = Field(
    default=None,
    description="D-PRC2: precedent similarity evidence. Mutually exclusive with other anchors.")
leg_tag: Literal["ABSENCE", "STRUCTURAL", "REFERENCE", "PRECEDENT"] | None = Field(
    default=None,
    description="D-ENV1: which recall leg produced this Fault.")
```

Anti-overfitting: no submission-specific constant (no inline float threshold values, no CTD section paths, no doc names, no ANDA numbers) in this file. anchor field defaults are None.

DO NOT change ComplianceVerdict, EvidenceClass, Tier, or any existing Fault field.
  </action>
  <verify>
    <automated>PYTHONPATH=src uv run python -c "from schemas.faults import StructuralAnchor, ReferenceAnchor, PrecedentAnchor, Fault; f = Fault(title='t', detail='d', tier='VERIFIED', evidence_class='CODE_VERIFIED', confidence=0.9, verdict='VIOLATION', submission_span_id=None, rule_span_id=None); assert f.structural_anchor is None; assert f.leg_tag is None; print('OK')"</automated>
  </verify>
  <done>
    StructuralAnchor, ReferenceAnchor, PrecedentAnchor importable from schemas.faults. Fault has optional structural_anchor, reference_anchor, precedent_anchor, leg_tag fields defaulting to None. No existing field changed. Python import succeeds with no error.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create guard_vocab.py + parse-layer hyperlink/link backfill + test scaffolds + synthetic fixture</name>
  <files>
    src/rulebook/guard_vocab.py
    src/parse/docx.py
    src/parse/pdf.py
    tests/rulebook/test_structural.py
    tests/rulebook/test_references.py
    tests/rulebook/test_precedent_search.py
    tests/tools/test_emit_structural_finding.py
    tests/tools/test_emit_reference_finding.py
    src/evals/dataset/synthetic_fixture/doc_a.pdf
    src/evals/dataset/synthetic_fixture/doc_b.docx
    src/evals/dataset/synthetic_fixture/doc_c.pdf
    src/evals/baseline/structural_threshold.json
    src/evals/baseline/precedent_threshold.json
  </files>
  <read_first>
    - src/rulebook/guard_vocab.py (does not exist; create from PATTERNS.md pattern)
    - src/parse/docx.py (read in full — find extract_docx return dict and where to add hyperlinks)
    - src/parse/pdf.py (read in full — find where to add link annotations)
    - tests/rulebook/test_absence.py (lines 1-60 — module-scoped fixture pattern, imports)
    - tests/tools/test_emit_absence_finding.py (lines 1-70 — gate test pattern, imports)
    - .planning/phases/05-deterministic-structural-cross-document-recall/05-PATTERNS.md § "guard_vocab.py", "test_structural.py", "test_emit_structural_finding.py"
    - .planning/phases/05-deterministic-structural-cross-document-recall/05-RESEARCH.md § "Research Domain 5: Anti-Overfitting Guard + Synthetic Fixture", "Pitfall 8"
  </read_first>
  <behavior>
    guard_vocab.py:
    - AGGREGATE_LEXICON = frozenset({"total", "sum", "maximum", "max", "minimum", "min", "average", "mean"})
    - REFERENCE_CUE_WORDS = frozenset({"see", "refer", "table", "section", "module", "figure", "appendix", "as described in", "as stated in", "per", "referenced in"})
    - Module docstring explaining D-GRD3 purpose; entries are general vocabulary, not corpus constants

    parse/docx.py:
    - extract_docx adds "hyperlinks": list[dict] to its returned dict
    - Each hyperlink dict: {rId: str, target: str, paragraph_index: int | None}
    - Extraction: open DOCX as zipfile, read "word/_rels/document.xml.rels", parse with ET, find Relationship[@Type ends with "/hyperlink"], extract rId + Target
    - If DOCX is not a zip (bad file), return "hyperlinks": [] and do not raise

    parse/pdf.py:
    - extract_pdf adds "links": list[dict] to its returned dict
    - Each link dict: {kind: int, uri: str | None, page: int}
    - Extraction: for each page in fitz doc, call page.get_links(), collect all links
    - If doc is corrupt or page has no links, continue silently

    Synthetic fixture (src/evals/dataset/synthetic_fixture/):
    - doc_a.pdf: "Quality Overall Summary 2.3 Analytical Procedure" analog. Contains a textual reference "See Table 3 in Document B (Module 3.2.P.4.1)" and a spec value "NMT 0.15% w/w for any individual impurity". Realistic regulatory prose (method validation, impurity profile). Min 3 paragraphs. Must NOT use mvr1381/estradiol doc names or values.
    - doc_b.docx: "Module 3.2.P.4 Analytical Procedures" analog. Contains an impurity specification table with a "Total Impurities" aggregate row and individual impurity rows. The Total cell contains a value (0.12%) that is less than a single component (0.18%), creating a planted structural inconsistency (X2 style). A second table has a "Maximum" cell (42.3 units) where the true max of the listed row values is 57.8 units (aggregate recompute error). Must NOT use Table 19/20 labels or mvr1381 values.
    - doc_c.pdf: "Analytical Specifications" document. Contains a reported result (0.22% for an unspecified impurity) that exceeds the stated NMT limit (0.10%), plus a cross-reference back to doc_a. Realistic stability/specification language.
    - Use fitz (for PDFs) and python-docx (for DOCX) to create synthetic files programmatically. Write a helper script or create inline with BytesIO. Keep files small (< 50 KB each).
    - D-GRD1 constraint 3: write realistic regulatory domain sentences so bge-m3 cosine lands in usable range. Use terms like "impurity", "specification", "NMT", "accuracy", "method validation", "dissolution", "stability" in the text.

    Test scaffolds (failing imports → implement in later waves):
    - tests/rulebook/test_structural.py: import detect_structural_inconsistencies from rulebook.structural, define ONE test_aggregate_violation_detected that calls it on a minimal corpus and asserts a Fault with structural_anchor. Mark with pytest.importorskip on the module import so it FAILS but does not error on missing module.
    - tests/rulebook/test_references.py: import extract_references from rulebook.references, define test_docx_hyperlink_extraction (assert hyperlinks extracted) and test_absent_target_manifest_check (assert ABSENT_TARGET logic). Same importorskip pattern.
    - tests/rulebook/test_precedent_search.py: import search_precedents from rulebook.precedent_search, define test_same_anda_exclusion and test_above_threshold_candidate. Same pattern.
    - tests/tools/test_emit_structural_finding.py: import emit_structural_finding from tools.emit_finding, define test_structural_null_rule_span_accepted and test_structural_fabricated_claim_span_rejected. Same pattern.
    - tests/tools/test_emit_reference_finding.py: import emit_reference_finding from tools.emit_finding, define test_reference_finding_full_candidate and test_reference_finding_unanchored_rejected. Same pattern.

    Threshold JSON stubs:
    - src/evals/baseline/structural_threshold.json: {"threshold": 0.0, "measured_on": "synthetic_fixture", "measured_date": "2026-08-06", "note": "D-STR4: general threshold, not corpus-tuned. Placeholder; will be updated after structural-gate measurement."}
    - src/evals/baseline/precedent_threshold.json: {"threshold": 0.0, "measured_on": "synthetic_fixture", "measured_date": "2026-08-06", "note": "D-PRC4: general absolute dense-cosine threshold. Placeholder; will be measured after precedent-leg implementation."}
  </behavior>
  <action>
Step 1 — guard_vocab.py: Create src/rulebook/guard_vocab.py exactly as specified in PATTERNS.md § guard_vocab.py. Module docstring must explain D-GRD3 purpose. No corpus-specific token in any frozenset entry (assert-checked by later test). Import nothing from the codebase (pure config).

Step 2 — parse/docx.py hyperlink backfill: Read src/parse/docx.py. Find the function that builds and returns the parsed dict (likely extract_docx or similar). Add hyperlink extraction using zipfile + xml.etree.ElementTree:
```python
import zipfile, xml.etree.ElementTree as ET
_NS = "http://schemas.openxmlformats.org/package/2006/relationships"
_HYPERLINK_TYPE = "http://schemas.openxmlformats.org/officeDocument/2006/relationships/hyperlink"

def _extract_docx_hyperlinks(path: str) -> list[dict]:
    try:
        with zipfile.ZipFile(path) as z:
            if "word/_rels/document.xml.rels" not in z.namelist():
                return []
            rels_xml = z.read("word/_rels/document.xml.rels")
        root = ET.fromstring(rels_xml)
        links = []
        for rel in root.findall(f"{{{_NS}}}Relationship"):
            if rel.get("Type") == _HYPERLINK_TYPE:
                links.append({"rId": rel.get("Id", ""), "target": rel.get("Target", ""), "paragraph_index": None})
        return links
    except Exception:
        return []
```
Then in the main extraction function, add `result["hyperlinks"] = _extract_docx_hyperlinks(path)` before returning. If the existing function does not have the original file path as an argument (it may receive a BytesIO), add a `source_path: str | None = None` parameter and use it for hyperlink extraction when available.

Step 3 — parse/pdf.py link backfill: Read src/parse/pdf.py. Find where fitz doc pages are iterated. After text extraction per page, collect:
```python
links = []
for page_num, page in enumerate(doc):
    for link in page.get_links():
        links.append({"kind": link.get("kind", 0), "uri": link.get("uri", None), "page": page_num})
result["links"] = links
```

Step 4 — Synthetic fixture: Create src/evals/dataset/synthetic_fixture/ directory. Use fitz to create doc_a.pdf and doc_c.pdf with real text and a cross-reference link from doc_a to doc_b. Use python-docx to create doc_b.docx with two tables (one with planted Total aggregate error, one with planted Max aggregate error). Write realistic regulatory domain prose throughout. Files must pass fitz/docx open without error.

Key planted deficiencies (specified from rulebook/structural semantics, BLIND to implementation):
- doc_b.docx Table 1: Row headers "Impurity A", "Impurity B", values "0.10%", "0.18%". Row "Total Impurities" with value "0.12%". True sum = 0.28%, max = 0.18%. Both aggregate violations.
- doc_b.docx Table 2: Row "Maximum Observed Value" = "42.3 units". Individual rows: "Run 1: 38.2", "Run 2: 57.8", "Run 3: 44.1". True max = 57.8.
- doc_c.pdf: paragraph "The unspecified impurity was found at 0.22% w/w, which meets the NMT 0.10% specification." (limit exceedance).
- doc_a.pdf → doc_b.docx: hyperlink or textual reference "See Table 1 in Analytical Procedures document" with a value "NMT 0.15% for any single impurity". In doc_b.docx the corresponding cell says "0.18%" (value contradiction X1 analog).

Step 5 — Test scaffolds: Write each test file using pytest.importorskip so missing modules cause SKIP not ERROR:
```python
# tests/rulebook/test_structural.py
from __future__ import annotations
import pytest
structural = pytest.importorskip("rulebook.structural")
detect_structural_inconsistencies = structural.detect_structural_inconsistencies
```
Then define stub tests that call the function. The tests should fail (ImportError → skip OR assertion error) when the module is not yet implemented.

Step 6 — Threshold JSONs: Write both JSON stubs to src/evals/baseline/.

Step 7 — Verify: Run `PYTHONPATH=src uv run python -c "from schemas.faults import StructuralAnchor, ReferenceAnchor, PrecedentAnchor; from rulebook.guard_vocab import AGGREGATE_LEXICON, REFERENCE_CUE_WORDS; print('OK')"` and `PYTHONPATH=src uv run python -c "from parse.docx import extract_docx; print('OK')"`.
  </action>
  <verify>
    <automated>PYTHONPATH=src uv run python -c "from schemas.faults import StructuralAnchor, ReferenceAnchor, PrecedentAnchor, Fault; from rulebook.guard_vocab import AGGREGATE_LEXICON, REFERENCE_CUE_WORDS; assert 'total' in AGGREGATE_LEXICON; assert 'see' in REFERENCE_CUE_WORDS; print('schema+vocab OK')" && PYTHONPATH=src uv run python -c "import pathlib; assert pathlib.Path('src/evals/dataset/synthetic_fixture/doc_b.docx').exists(); print('fixture OK')" && PYTHONPATH=src uv run python -c "import json; t=json.loads(open('src/evals/baseline/structural_threshold.json').read()); assert 'threshold' in t; print('threshold stubs OK')"</automated>
  </verify>
  <done>
    guard_vocab.py created with AGGREGATE_LEXICON + REFERENCE_CUE_WORDS. docx.py emits "hyperlinks" key. pdf.py emits "links" key. Synthetic fixture has 3 files with planted deficiencies. 5 test scaffold files exist and can be collected by pytest (skipping on missing module, not erroring). 2 threshold JSON stubs exist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic fixture authoring | Content is entirely synthetic (no real FDA data); must pass schema validation but is not parsed from external input |
| parse/docx.py hyperlink extraction | Reads word/_rels/document.xml.rels from a DOCX zip — attacker could craft malicious XML |
| parse/pdf.py link extraction | Reads link annotations from untrusted PDF bytes via fitz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05W0-01 | Tampering | docx.py hyperlink XML parsing (ET.fromstring on rels_xml) | mitigate | Wrap ET.fromstring in try/except; return [] on any XML parse failure. The rels file is already inside a ZipFile we opened for other purposes; no additional attack surface beyond what parse/docx.py already accepts. |
| T-05W0-02 | Tampering | pdf.py page.get_links() on untrusted PDF | mitigate | Already uses the Phase-1 security limit (ingest.limits.safe_resolve); fitz get_links() is a metadata read, not code execution. Wrap in try/except per page; return [] on error. |
| T-05W0-03 | Information Disclosure | Synthetic fixture committed to repo | accept | Fixture is entirely synthetic (no real FDA/ANDA data, no PII, no confidential submissions); safe to commit. |
| T-05W0-04 | Denial of Service | pathological DOCX with 100k relationship entries | mitigate | Cap hyperlink extraction at 1000 entries; log a warning and return the capped list. |
</threat_model>

<verification>
After Wave 0:
- `PYTHONPATH=src uv run python -c "from schemas.faults import StructuralAnchor, ReferenceAnchor, PrecedentAnchor, Fault; print('envelope OK')"` — passes
- `PYTHONPATH=src uv run python -c "from rulebook.guard_vocab import AGGREGATE_LEXICON, REFERENCE_CUE_WORDS; assert len(AGGREGATE_LEXICON) >= 6; print('vocab OK')"` — passes
- `PYTHONPATH=src uv run python -c "import pathlib; d=pathlib.Path('src/evals/dataset/synthetic_fixture'); assert (d/'doc_a.pdf').exists() and (d/'doc_b.docx').exists() and (d/'doc_c.pdf').exists(); print('fixture OK')"` — passes
- `PYTHONPATH=src uv run pytest tests/rulebook/test_structural.py tests/rulebook/test_references.py tests/rulebook/test_precedent_search.py --collect-only -q` — collects tests without ImportError (may skip on missing module)
- `PYTHONPATH=src uv run pytest tests/ -q -x -m "not slow"` — existing tests still green (no regression)
</verification>

<success_criteria>
- Shared candidate envelope (D-ENV1) implemented and importable: StructuralAnchor, ReferenceAnchor, PrecedentAnchor, Fault.leg_tag, Fault.*_anchor fields
- Guard vocab registry (D-GRD3) created: guard_vocab.py with AGGREGATE_LEXICON + REFERENCE_CUE_WORDS
- Parse-layer hyperlink/link extraction added to docx.py (D-REF1 DOCX side) and pdf.py (D-REF1 PDF side)
- Synthetic multi-doc fixture committed (D-GRD1/D-GRD4) with planted structural + cross-ref + precedent-matchable deficiencies using realistic regulatory prose
- All 5 Wave-0 test scaffold files exist and are collected by pytest
- Threshold JSON stubs created
- Existing test suite still fully green
</success_criteria>

<output>
After completion, create `.planning/phases/05-deterministic-structural-cross-document-recall/05-01-SUMMARY.md` following the summary template.
</output>
