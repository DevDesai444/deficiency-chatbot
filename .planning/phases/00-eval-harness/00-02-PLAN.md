---
phase: 00-eval-harness
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/evals/dataset/documents.json
  - src/evals/dataset/minispec.deficiencies.json
  - src/evals/dataset/heldout32s41.deficiencies.json
  - src/evals/make_docx_fixture.py
  - src/evals/dataset/docs/mini_spec.docx
  - tests/evals/test_breadth.py
autonomous: true
requirements: [EVAL-01]

must_haves:
  truths:
    - "The eval set spans >=3 documents and includes both a PDF and a DOCX source"
    - "At least one held-out document (never used in the estradiol tuning set) is registered and labeled"
    - "Every added deficiency's evidence_anchor verbatim-resolves in its own source document"
  artifacts:
    - path: "src/evals/make_docx_fixture.py"
      provides: "Deterministic generator for the planted-deficiency DOCX fixture"
      contains: "python-docx"
      min_lines: 20
    - path: "src/evals/dataset/docs/mini_spec.docx"
      provides: "A DOCX source document with planted, hand-verified deficiencies"
    - path: "src/evals/dataset/heldout32s41.deficiencies.json"
      provides: "Hand-verified labels for the held-out 32s41 specification PDF"
      contains: "held"
  key_links:
    - from: "src/evals/dataset/documents.json"
      to: "the DOCX fixture + held-out PDF"
      via: "EvalDocument entries with format=docx and held_out=true"
      pattern: "docx|held_out"
---

<objective>
Expand the ground truth beyond the single estradiol PDF (the n=1 overfitting trap called out in
PITFALLS.md #11) to a multi-document set with a DOCX source and a held-out corpus — the real
de-risking work the ROADMAP flags inside this phase.

Purpose: A held-out, multi-format set is the only defense against Goodhart overfitting; a DOCX
source establishes the labeled target that Phase 1's DOCX parse path (INGEST-02) will be gated on.
Output: a planted-deficiency DOCX fixture + labels, the real 32s41 spec PDF registered as a
held-out document with anchor-resolving labels, and an expanded documents.json.

NOTE — do NOT build DOCX parsing here. DOCX parse is Phase 1 (INGEST-02); at Phase 0 the DOCX
document exists in the SET as a labeled target that the harness records as a parse-fidelity miss
until Phase 1 lands. Do not clobber the uncommitted planner/summariser redesign on this branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# The schema + loader this plan feeds (Plan 01 output):
@src/evals/schema.py
@src/evals/dataset/documents.json

<interfaces>
<!-- Reuse from Plan 01 (do not redefine): FailureFamily, Confidence, GroundTruthDeficiency,
     EvalDocument{doc_id,path,format:"pdf"|"docx",held_out}, EvalSet, load_eval_set().
     documents.json is a JSON list of EvalDocument dicts; the loader globs *.deficiencies.json —
     add new documents by appending to documents.json and dropping a new *.deficiencies.json file
     (no schema.py edit). Existing PDF parse entrypoint for verifying held-out anchors:
       parse.pdf.extract_pdf(path) -> doc dict (pages->blocks->tables)
     python-docx (1.2) is already a project dependency. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate a planted-deficiency DOCX fixture + its labels</name>
  <files>src/evals/make_docx_fixture.py, src/evals/dataset/docs/mini_spec.docx, src/evals/dataset/minispec.deficiencies.json</files>
  <read_first>
    - src/evals/schema.py (GroundTruthDeficiency + EvalDocument field names)
    - docs/eval/MEASUREMENT.md ("Summary/total integrity oracle" recommended checks: `stated max == max(rows)`, `total >= max(component)` — plant exactly these)
  </read_first>
  <action>
    Create `src/evals/make_docx_fixture.py` — a deterministic script using `python-docx` (`from docx import Document`) that writes `src/evals/dataset/docs/mini_spec.docx`. The document is a one-page mock impurities specification containing a heading, a short narrative, and a table with columns `Impurity | Result (%) | Limit`. Plant these hand-verifiable deficiencies with EXACT cell values so anchors are stable:
    - Row "Total impurities" = `0.14`, while a component row "Impurity B" = `0.15` — plants `total (0.14) < largest component (0.15)`, an arithmetic impossibility (family = cross_reference_integrity).
    - A "Maximum" row stating `0.15` while the true max across component rows is `0.19` (a stated-max mismatch; family = cross_reference_integrity).
    - A narrative sentence: "Accuracy of the method was established at three concentration levels." with NO accuracy result table anywhere (family = absence_of_evidence).
    Make the script idempotent (overwrites the file) and add `if __name__ == "__main__": main()`. Run it to produce the .docx, then commit the generated file.

    Create `src/evals/dataset/minispec.deficiencies.json` = a JSON list of the 3 GroundTruthDeficiency dicts (doc_id="minispec"), each with an `evidence_anchor` copied verbatim from the text/cells written above (e.g. `0.14`, `Maximum`, `Accuracy of the method was established`), correct `failure_family`, `confidence: certain` for the two arithmetic ones, `tp_required: false`.

    Register the document: append to `src/evals/dataset/documents.json` the entry
    `{"doc_id": "minispec", "path": "src/evals/dataset/docs/mini_spec.docx", "format": "docx", "held_out": false}`.
  </action>
  <acceptance_criteria>
    - `grep -c "python-docx\|from docx import" src/evals/make_docx_fixture.py` >= 1
    - `test -f src/evals/dataset/docs/mini_spec.docx && echo present` prints `present`
    - `PYTHONPATH=src uv run python -c "from docx import Document; t=' '.join(c.text for tb in Document('src/evals/dataset/docs/mini_spec.docx').tables for r in tb.rows for c in r.cells); assert '0.14' in t and '0.15' in t; print('planted')"` prints `planted`
    - `grep -c '"doc_id": "minispec"' src/evals/dataset/documents.json` returns 1
    - `PYTHONPATH=src uv run python -c "import json;print(len(json.load(open('src/evals/dataset/minispec.deficiencies.json'))))"` prints `3`
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src uv run python src/evals/make_docx_fixture.py && PYTHONPATH=src uv run python -c "from docx import Document; print('docx-ok')"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Register the 32s41 specification PDF as a held-out labeled document</name>
  <files>src/evals/dataset/heldout32s41.deficiencies.json, src/evals/dataset/documents.json</files>
  <read_first>
    - src/evals/schema.py (field names)
    - src/parse/pdf.py (extract_pdf signature — used to confirm anchors resolve in the parsed doc)
    - Sample Data/32s41-Specification.pdf (the held-out source — a specification doc, a different type from the method-validation tuning doc)
  </read_first>
  <action>
    Register `data/32s41-Specification.pdf` as a HELD-OUT document: append to `src/evals/dataset/documents.json`
    `{"doc_id": "spec32s41", "path": "data/32s41-Specification.pdf", "format": "pdf", "held_out": true}`.

    Parse it once with `from parse.pdf import extract_pdf` to read its real content, then hand-verify and record >= 3 concrete deficiencies in `src/evals/dataset/heldout32s41.deficiencies.json` (doc_id="spec32s41"), each with a verbatim `evidence_anchor` that substring-appears in the parsed text/tables (e.g. a specification attribute with a missing/blank limit -> spec_limits family -> regulatory_framing or cross_reference_integrity; a required element referenced but absent -> absence_of_evidence). Set `confidence` honestly (`likely` unless arithmetically certain) and `tp_required: false` for all (held-out is never a tp_required anchor).

    FALLBACK (only if fewer than 3 verifiable deficiencies can be found in 32s41): extend `src/evals/make_docx_fixture.py` to also emit a held-out synthetic PDF/DOCX under `src/evals/dataset/heldout/` with >=3 planted deficiencies, register it with `held_out: true`, and label those instead. Either path must end with >=1 held_out document carrying >=3 anchor-resolving labels.
  </action>
  <acceptance_criteria>
    - `grep -c '"held_out": true' src/evals/dataset/documents.json` >= 1
    - `PYTHONPATH=src uv run python -c "import json;print(len(json.load(open('src/evals/dataset/heldout32s41.deficiencies.json'))))"` >= 3
    - Each anchor resolves in its source — `PYTHONPATH=src uv run python -c "import json;from parse.pdf import extract_pdf;d=extract_pdf('data/32s41-Specification.pdf');txt=' '.join(b.get('text','') for p in d['pages'] for b in p.get('blocks',[]));gt=json.load(open('src/evals/dataset/heldout32s41.deficiencies.json'));miss=[g['id'] for g in gt if g['evidence_anchor'] not in txt];print('missing:',miss);assert not miss"` prints `missing: []` (fallback path substitutes its own source)
  </acceptance_criteria>
  <verify>
    <automated>PYTHONPATH=src uv run python -c "from evals.schema import load_eval_set; s=load_eval_set(); assert any(dd.held_out for dd in s.documents); print('heldout-ok')"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Validate the expanded, multi-format, held-out set</name>
  <files>tests/evals/test_breadth.py</files>
  <read_first>
    - src/evals/schema.py (EvalSet helpers)
    - src/evals/dataset/documents.json (now 3 documents after Tasks 1-2)
  </read_first>
  <action>
    Create `tests/evals/test_breadth.py` with tests asserting the expanded set (via `load_eval_set()`):
    - `len(s.documents) >= 3`;
    - the set of `d.format` values includes BOTH `"pdf"` and `"docx"`;
    - `>= 1` document has `held_out is True`;
    - every deficiency belongs to a registered `doc_id` (referential integrity: `{g.doc_id} <= {d.doc_id}`);
    - total deficiencies `>= 34` (28 estradiol + 3 DOCX + >=3 held-out);
    - the estradiol tuning set is unchanged: exactly 2 `tp_required` across the whole set (breadth docs add none).
    Add a one-line module docstring noting the DOCX doc is a labeled target that the harness records
    as a parse-fidelity miss at Phase 0 (DOCX parse lands in Phase 1 / INGEST-02).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/evals/test_breadth.py -x` exits 0
    - `grep -c "held_out\|docx\|pdf" tests/evals/test_breadth.py` >= 3
    - `PYTHONPATH=src uv run python -c "from evals.schema import load_eval_set; s=load_eval_set(); assert {d.format for d in s.documents}>={'pdf','docx'}; assert len(s.tp_required())==2; print('breadth-ok')"` prints `breadth-ok`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/evals/test_breadth.py -x</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Internal eval tooling. Boundary = local filesystem only (reads in-repo PDFs, writes in-repo
fixtures). No network, no external services, no untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02 | Tampering | generated mini_spec.docx fixture | accept | Deterministic generator committed alongside output; regenerable and PR-reviewed |
| T-00-03 | Information disclosure | 32s41 spec content copied into anchors | accept | Already an in-repo sample document; anchors are short verbatim tokens, no new exposure |
</threat_model>

<verification>
- `uv run pytest tests/evals/ -x` passes (Plan 01 + Plan 02 tests).
- Set spans >=3 docs, includes pdf + docx, has >=1 held-out doc; every added anchor resolves in its source.
- No DOCX parsing code was added (Phase 1 scope); the uncommitted redesign is untouched.
</verification>

<success_criteria>
The ground-truth set is multi-document, multi-format (PDF + DOCX), and includes a held-out
corpus with anchor-resolving labels — satisfying ROADMAP Phase 0 success criterion 1.
</success_criteria>

<output>
After completion, create `.planning/phases/00-eval-harness/00-02-SUMMARY.md`.
</output>
