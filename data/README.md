# data/

Intentionally empty in git. Corpus files live outside the repo (see `$DATA_DIR` in `.env.example`).

Anything written here at runtime (FAISS index `.bin`, sidecar SQLite `.db`, intermediate parquet) is gitignored.
